# Test Coverage Gaps — Scheduled Task Journal

_Audit model: claude-sonnet-4-6_
_Action model: claude-opus-4-6_
_Audit cadence: weekly — Monday_
_Last audited: 2026-08-31_
_Last action: 2026-08-12_

---

## In Progress

_Nothing currently in progress._

---

## Open

### MEDIUM SSRF-guarded proxy Cloud Functions have zero tests

- **Detected:** 2026-08-24
- **File:** `functions/src/embedProxy.ts` (219 lines, no `embedProxy.test.ts`)
- **Detail:** Not covered by the existing Cloud Function item, whose remaining target is only `spotifyOAuth.ts`. `fetchExternalProxy` + `checkUrlCompatibility` are the app's only server-side outbound fetchers and carry load-bearing security logic that no test pins: the 3-host https allowlist, `maxRedirects: 0` (the comment explicitly says this is what keeps the allowlist load-bearing), the 1 MB `maxContentLength`/`maxBodyLength` cap with its `resource-exhausted` translation, and a 13-pattern private/reserved-IP blocklist including bracketed IPv6 forms (`^\[::`, `^\[f[cd]`, `^\[fe[89a-f]`).
- **Fix:** Table-drive the blocklist regexes (`localhost`, `127.x`, `10.x`, `172.16-31.x`, `192.168.x`, `169.254.169.254`, `metadata.google.internal`, `[::1]`, `[::ffff:127.0.0.1]`, `[fd00::1]`, `[fe80::1]` → `invalid-argument`; a public host → passes); assert non-https and malformed URLs rejected; assert `axios.get/head` is called with `maxRedirects: 0`; assert an off-allowlist host with an allowlisted *prefix* (e.g. `api.openweathermap.org.evil.com`) is rejected; assert unauthenticated calls throw before any axios call; assert `maxContentLength` errors map to `resource-exhausted` while generic errors map to `internal`; assert `x-frame-options: deny|sameorigin` and CSP `frame-ancestors 'self'|'none'` → `isEmbeddable:false`, and that a HEAD failure returns `{isEmbeddable:true, uncertain:true}` (fail-open — worth locking in deliberately).

### MEDIUM `archiveActivityWallPhoto` ownership and size guards untested

- **Detected:** 2026-08-24
- **File:** `functions/src/driveArchive.ts` (317 lines, no test file)
- **Detail:** The only authorization check is `sessionId.startsWith(\`${request.auth.uid}_\`)` — a one-line string-prefix guard on cross-teacher access to `activity_wall_sessions/*/submissions/*`. Also untested: the fail-closed `!Number.isFinite(sizeBytes) || sizeBytes > 50MB` guard (whose comment notes that `NaN > limit` would silently bypass it), the mime→extension mapping, the Drive folder-name escaping in `getOrCreateDriveFolder` (`\` and `'` escaping in the `q=` query — a query-injection surface), and the failure path that writes `archiveStatus:'failed'` + a 180-char-truncated `archiveError` while re-throwing `HttpsError` instances unwrapped.
- **Fix:** Mock `admin.firestore`/`admin.storage` + `fetch`; assert permission-denied for a foreign `sessionId` prefix *before* any Firestore write; assert each missing param → `invalid-argument`; assert oversize and non-numeric `metadata.size` both throw `invalid-argument` with their two distinct messages and that `file.download()` is never called; assert the happy path writes the `lh3.googleusercontent.com/d/{id}` URL, deletes `storagePath`/`archiveStartedAt`, and calls `file.delete({ignoreNotFound:true})`; assert an `HttpsError` from the size guard survives the catch un-rewrapped while a plain `Error` becomes `internal`.

### MEDIUM Admin analytics HTTP endpoint authz matrix untested

- **Detected:** 2026-08-24
- **File:** `functions/src/adminAnalyticsEndpoint.ts` (177 lines, no test file)
- **Detail:** An `onRequest` endpoint with `invoker: 'public'` whose entire tenancy isolation is the `ORG_ADMIN_ROLE_IDS` set (`super_admin`, `domain_admin`, `building_admin`) plus the `/admins/{email}` super-admin bypass. The `orgId`-required change exists specifically because the prior version leaked foreign-domain accounts — that regression has no test.
- **Fix:** Assert 401 for a missing/non-`Bearer` header, a `verifyIdToken` rejection, and a token with no `email`; assert email is lowercased before the `admins`/`members` doc lookups; assert 400 when `orgId` is missing/blank/non-string; assert 403 for a member with a non-admin `roleId` and for a caller with neither doc; assert each of the three admin role ids and the super-admin path succeed; assert 503 `not-yet-computed` when `readAnalyticsSnapshot` returns null; assert 500 on a throw; assert the `X-Request-Id` header matches the `requestId` in every error body and that `meta` is merged with `computedAt`/`nextRecomputeAt`/`computeDurationMs`.

### MEDIUM ClassLink roster fetch (student PII) untested

- **Detected:** 2026-08-24
- **File:** `functions/src/classlinkRoster.ts` (168 lines, no test file)
- **Detail:** `getClassLinkRosterV1` is `invoker: 'public'` and returns student PII. Untested: the `isSafeEmailForOneRosterFilter` guard that must return `{classes:[],studentsByClass:{}}` *before* building the `filter: email='...'` OneRoster query (quote-injection defense), the missing-secret → `internal` path, the trailing-slash tenant-URL normalization, the `chunk(classes, 15)` fan-out limiter, and the per-class `catch` that logs a warning and yields `[]` rather than failing the whole roster.
- **Fix:** Mirror the harness already built in `studentIdentity.test.ts` (which stubs `axios`/`secrets` and uses the real `classlinkShared`); assert the unauth / no-email / missing-secret guards, that a quote-bearing email short-circuits with zero axios calls, that no matching user yields an empty result, that a 40-class teacher issues exactly 3 batches of ≤15 concurrent student calls, that one failing class logs `[getClassLinkRosterV1] students fetch failed for class` and still returns the other classes, and that an `AxiosError` surfaces as `internal` with the axios message appended.

### MEDIUM `CustomWidgetsContext` rule-mirroring query split is untested

- **Detected:** 2026-08-24
- **File:** `context/CustomWidgetsContext.tsx` (203 lines, zero tests)
- **Detail:** Context providers are not currently tracked as a coverage dimension in this journal. The provider branches on `isAdmin`: admins get an unconstrained `custom_widgets` listener; non-admins get two separate queries (public + beta with `array-contains user.email.toLowerCase()`) merged client-side, deliberately shaped to match Firestore rules — the comment states a single combined query would fail. It then applies a building filter (`buildings.length === 0 || buildings.some(b => selectedBuildings.includes(b))`). A silent regression here either hides widgets or surfaces unpublished ones.
- **Fix:** Mock `firebase/firestore` + `useAuth`; assert the admin path registers ONE unconstrained listener and non-admin registers exactly two with the expected `where` constraints; assert email is lowercased in the `array-contains`; assert a user with no `email` registers only the public listener and still resolves `loading:false` via the `merge()` fallback; assert de-dup by id when a widget appears in both snapshots; assert the building filter keeps `buildings:[]` and drops non-matching buildings; assert `customTools` only includes `published && enabled` docs and carries `defaultWidth`/`customWidgetId`/`customWidgetIcon`; assert each listener's `onError` clears `loading`; assert both unsubs fire on unmount and that `saveCustomWidget`/`setPublished`/`deleteCustomWidget` no-op (but still return an id) under `isAuthBypass`.

### MEDIUM `seatingChartLayouts.ts` (283 lines of pure geometry) has no tests

- **Detected:** 2026-08-24
- **File:** `components/widgets/SeatingChart/seatingChartLayouts.ts`
- **Detail:** The single most trivially-testable untested module found this cycle: pure functions (`generateColumnsLayout`, plus the pods/rows generators) over numbers, no I/O, no React. Fiddly edge cases are already visible in the source — `numStudents <= 0 || numColumns <= 0 → []`, the `actualColumns` recomputation, the single-column/single-row centering branches (`canvasW/2 - DESK_W/2`), and `snapToGrid` rounding.
- **Fix:** Assert empty output for 0/negative students and columns; assert the item count always equals `numStudents` (including when `numStudents < numColumns`); assert every x/y is a multiple of `gridSize`; assert the single-column and single-row branches center the desk; assert no desk exceeds `canvasW - DESK_W` / `canvasH - DESK_H`; snapshot one representative 24-student/4-column grid; repeat for the pods layout including `PODS_HEADER_OFFSET`.

### MEDIUM Pure PLC analytics and mention logic untested

- **Detected:** 2026-08-24
- **File:** `components/common/library/plcAnalyticsAggregate.ts` (177 lines: `SCORE_BUCKETS`, `aggregateGroup`, `groupBySchema`), `components/plc/comments/mentionUtils.ts` (80 lines: `MENTION_QUERY_RE`, `filterMentionCandidates`, `resolveMentions`)
- **Detail:** Both are dependency-free pure modules feeding teacher-visible numbers and @-mention notification targeting (a wrong `resolveMentions` silently mis-notifies or fails to notify a colleague). Neither has a colocated or `tests/` file.
- **Fix:** Aggregate — assert bucket boundary values land in the correct `SCORE_BUCKETS` slot (test the exact edges), plus empty-contribution input, all-null-score input, and that `groupBySchema` groups by schema identity and preserves ordering. Mentions — assert `MENTION_QUERY_RE` matches `@`, `@ab`, `@a.b-c` at start-of-string and after whitespace but NOT mid-word (`foo@bar`); assert `filterMentionCandidates` is case-insensitive, matches on both name and handle, and returns `[]` for no match; assert `resolveMentions` maps display text back to uids and handles duplicate mentions, unknown handles, and a name that is a prefix of another.

### MEDIUM `gisOAuth.ts` token-popup helpers untested

- **Detected:** 2026-08-24
- **File:** `components/classroomAddon/gisOAuth.ts` (206 lines, no test file)
- **Detail:** `ensureGis()` (script-load memoization) and `requestAccessToken()` gate every Google Classroom scope grant, including the three composed scope constants (`CLASSROOM_ADDON_TEACHER_SCOPE`, `CLASSROOM_COURSES_READONLY_SCOPE`, `CLASSROOM_COURSEWORK_STUDENTS_SCOPE`) and the "single consent screen" combined-token flow. A silently-wrong scope string means a grade push fails at the API boundary rather than at consent.
- **Fix:** Stub `window.google.accounts.oauth2`; assert `ensureGis()` injects the script once and de-dups concurrent callers; assert `requestAccessToken` passes the exact scope string, `login_hint`, and `prompt: 'none'` for the silent path; assert `resolve` on `access_token`, `reject(new Error('No access token returned.'))` on an empty response, and rejection on `resp.error`; pin each exported scope constant with an exact-string assertion so a scope typo fails a test rather than a teacher's grade push.

### MEDIUM `useMonitorData.ts` quiz-monitor state derivation has no tests

- **Detected:** 2026-08-31
- **File:** `components/widgets/QuizWidget/components/monitor/useMonitorData.ts` (248 lines, new since 2026-08-24, no test anywhere)
- **Detail:** This hook drives the live quiz-monitor view and has real branching logic worth pinning: the period-filter memo (`periodNames.length <= 1` short-circuit vs. `classIdToPeriod` lookup), the ClassLink-name vs. LTI-name merge in `byStudentUid` (LTI names fill gaps but never override ClassLink), the 30s ticker that only arms while `session.status === 'active' && anyInProgress`, the notStarted/inProgress/done bucketing plus `needsHelpCount`/`answeredCurrent` tallies, and the `onQuestion` cap (`min(answers.length+1, totalQuestions)`).
- **Fix:** Assert bucket counts sum to `totalStudents`; assert `awaitingGrade`/`bandScore` stay `null` for non-scoreable responses; assert duplicate detection flags the right `responseTeamId`s; assert the ticker interval is cleared on unmount/session-end.

### LOW `utils/quizSearchText.ts` untested

- **Detected:** 2026-08-31
- **File:** `utils/quizSearchText.ts` (27 lines, new since 2026-08-24)
- **Detail:** `buildQuizSearchText` truncates at 2000 chars and normalizes whitespace/case for quiz-library search, and `quizQuestionDedupeKey` builds a stable JSON key (sorted incorrect-answers array) used for exact-duplicate detection when merging quizzes.
- **Fix:** Assert truncation at the exact boundary, empty/whitespace-only question text filtered out, and that the dedupe key is order-independent for `incorrectAnswers` but sensitive to `type`/`correctAnswer` differences.

### LOW `utils/rubricPoints.ts` untested

- **Detected:** 2026-08-31
- **File:** `utils/rubricPoints.ts` (17 lines, new since 2026-08-24)
- **Detail:** `rubricMaxPoints` sums each criterion's highest-point level (assumes ascending `levels`, could silently give a wrong max if a criterion's max isn't the last entry), and `sumRubricScorePoints` explicitly guards non-finite `points` (`NaN`/`Infinity`) down to 0.
- **Fix:** Test `rubricMaxPoints` with unsorted levels to confirm/deny the ordering assumption; test `sumRubricScorePoints` with `NaN`/`Infinity` inputs to lock in the corrupted-score defense.

### LOW PLC sync-group join/leave transaction handlers untested

- **Detected:** 2026-08-24
- **File:** `functions/src/syncedQuizGroups.ts` (205 lines), `functions/src/syncedVideoActivityGroups.ts` (192 lines) — neither has a test file
- **Detail:** Flagged once in the 2026-07-22 audit note as "already in the CF item", but that item's current Detail lists only the three Spotify functions, so this is untracked in practice. Both modules deliberately export testable handlers (`handleJoinSyncedQuizGroup` / `handleLeaveSyncedQuizGroup`) separately from their `onCall` wrappers, so harness cost is low.
- **Fix:** Assert `not-found` for a missing share and a missing group, `failed-precondition` for a non-synced share, the participant-array upsert being idempotent on re-join inside `runTransaction`, leave removing only the caller, the last-participant case, and the wrapper-level `unauthenticated` / missing-`shareId` / missing-`groupId` guards.

### LOW `SavedWidgetsContext` has zero tests

- **Detected:** 2026-08-24
- **File:** `context/SavedWidgetsContext.tsx` (126 lines)
- **Detail:** Untested: the adjusting-state-while-rendering uid-change reset (`prevUid` compare in the render body — a pattern explicitly called out in CLAUDE.md and easy to break), the `createdAt` preservation in `saveSavedWidget` (`existing?.createdAt ?? now` — a re-save must not reset `createdAt`), and the `orderBy('createdAt','asc')` listener.
- **Fix:** Assert a signed-out mount has `loading:false` and no listener; assert a uid change clears `savedWidgets` and re-enters loading in the same render; assert `saveSavedWidget` throws when signed out, generates a uuid when no id is passed, and preserves `createdAt` while bumping `updatedAt` on re-save; assert `setPinnedToDock`/`deleteSavedWidget` no-op when signed out; assert the listener error path logs and clears `loading`.

_2026-08-31: Weekly coverage audit (Audit A1 — Monday), delegated to a dedicated sub-agent. `pnpm test` exit 0 — **686 files / 7901 tests** (root) all passing (366s), plus `functions/` 43 files / 867 tests passing (6s). Only benign stderr from intentional error-path logging. Zero HIGH severity issues. Three new items filed (1 MEDIUM, 2 LOW): `useMonitorData.ts` (new quiz-monitor hook, 248 lines, zero tests), `utils/quizSearchText.ts`, and `utils/rubricPoints.ts` (both new pure-logic files since the last audit). **One item fully resolved:** `useNutrislice.ts` now has `useNutrislice.test.tsx` (541 lines, 12 tests) covering exactly what the tracked item's Fix line asked for — moved to Completed. **Two items partially resolved:** the hooks/ untested-file count drops from 24 to 21 (`useBuildingSelection`, `useStudentAssignments`, `useAssignmentPseudonyms` now covered), and the widget fully-untested count drops from 11 to 9 (`HotspotImage` and `MathTools` now have test files). All other existing Open items re-verified unchanged._

_2026-08-24: Weekly coverage audit (Audit A1 — Monday), delegated to a dedicated sub-agent. `pnpm test` exit 0 — **638 files / 7514 tests all passing** (up from 620/7429 on 2026-08-17: +18 files, +85 tests), 417s. Only benign stderr (intentional error-path logging in `googleCalendarService.test.ts` and similar). **Zero HIGH severity issues.** Twelve new items filed (8 MEDIUM, 4 LOW). The new findings come from widening the audit beyond the four dimensions the prompt lists (utils/, hooks/, widget dirs, `functions/src/index.ts`): the `functions/src/` sweep was done per-module rather than against the barrel, which surfaced four untested security-relevant Cloud Function modules (`embedProxy`, `driveArchive`, `adminAnalyticsEndpoint`, `classlinkRoster`) that the existing CF item — scoped to `spotifyOAuth.ts` — does not cover; and pure-logic modules living under `components/` rather than `utils/` (`seatingChartLayouts.ts`, `plcAnalyticsAggregate.ts`, `mentionUtils.ts`, `gisOAuth.ts`, `useNutrislice.ts`) fall through every existing dimension. Context providers were also confirmed to be an untracked dimension entirely. **Stale-count corrections applied to the existing items below** (re-derived by script, as those items' own Fix lines requested): utils/ untested count is **16**, not "~20", and three files named in that item now have tests (`firestorePaging.ts`, `imageWorker.ts`, `classroomGradePush.ts`) and were struck; hooks/ untested count is exactly **24**, not "~22", now enumerated in full; `MathToolInstance` is no longer fully untested (it has `Settings.test.tsx`; its `Widget.tsx` remains uncovered), dropping the fully-untested widget set from 9 to 8, and three widget dirs missing from that item's list were added. The MEDIUM Cloud Function item's `spotifyOAuth.test.ts` sub-target was re-confirmed absent and remains accurate. Zero items fully resolved this cycle._

_2026-08-17: Weekly coverage audit (Monday). pnpm test: exit 0, 620 files / 7429 tests all passing (up from 604/7321 on 2026-08-10 — net +16 files, +108 tests). Two stderr blocks (InviteAcceptance.errorEntities.test.tsx, useFirestore.test.ts) are intentional error-path logging, not failures. No HIGH severity issues. `functions/src/spotifyOAuth.test.ts` confirmed still does NOT exist — MEDIUM Cloud Function item's spotifyOAuth sub-target still fully accurate. `transcribeVideoWithGemini`/`generateGuidedLearning` confirmed still lack execution-path coverage (only appear as name-inventory entries in index.test.ts) — MEDIUM item otherwise unchanged. **Coverage improved on two fronts, correcting stale counts rather than finding new gaps:** (1) utils/ untested count is actually ~20, not ~32 — no new utils/ files were added since 2026-08-10, and several previously-assumed-untested files (activityWallNormalize.ts, miniAppNormalize.ts) turned out to already be covered under differently-named test files (activityWallLibraryNormalize.test.ts, miniAppSessionNormalize.test.ts); MEDIUM item's detail updated below to flag the stale list for refresh. (2) Widget coverage: of the previously-tracked 17, three now have direct widget tests (Calendar, First5, NextUp), five are partially covered via Settings/subcomponent/logic tests (MusicWidget, SpecialistSchedule, CustomWidget, RevealGrid, StarterPack), leaving 9 fully untested (BloomsTaxonomy, CarRiderPro, ConceptWeb, HotspotImage, MathToolInstance, NeedDoPutThen, Onboarding, SyntaxFramer, WorkSymbols) — LOW item's detail updated below. New observation, not previously tracked: roughly 22 hooks/ files (useAdminBuildings, useBackgrounds, useScreenshot, useSpotifyAuth, useSpotifySearch, usePlcPresence, usePlcMeetings, and others) have no matching test file — added as new LOW open item below since hooks/ coverage wasn't previously tracked as its own dimension._

_2026-08-17 (Monday, action): Closed the Priority 1 sub-target (`transcribeVideoWithGemini` + `generateGuidedLearning`) of the MEDIUM "functions/src/ Cloud Function execution paths lack test coverage" item — the highest-priority Open across today's reading list (daily journals: widget-registry no open items, css-scaling only LOW, typescript-eslint none; Monday weeklies firestore-rules/simplification/ai-integration top out at MEDIUM, and test-coverage is first in the reading order per the item-selection precedent set on every prior Monday run, so its first structured MEDIUM item wins). File-recency check passed: `functions/src/aiGeneration.ts` was last touched at `236ad7ef` (docs-only), `functions/src/index.ts` at `59cf8bb8`/barrel refactor lineage — neither inside the last 5 branch commits (`e4616cb6`, `b38c8b66`, `f3bc765f`, `a535d605`, `6db2d1a6`). Added 14 tests to `functions/src/index.test.ts`: 8 for `transcribeVideoWithGemini` (unauthenticated, no-email, permission-doc-missing, disabled, admin-only accessLevel rejection, beta accessLevel rejection, admin caller reaches the real `ai.models.generateContent` call, invalid video URL) and 6 for `generateGuidedLearning` (unauthenticated, no-email, non-admin rejection, no-images, too-many-images, admin caller with a valid image reaches the AI call). Added a new hoisted `audioTranscriptionPermDocGet` mock (mirroring the existing `geminiConfigDocGet` pattern) to serve the `global_permissions/video-activity-audio-transcription` doc that `transcribeVideoWithGemini`'s accessLevel gate reads — extending the shared `mockFirestore.collection('global_permissions')` handler rather than building new mock infrastructure. All error-message assertions copied verbatim from `aiGeneration.ts`. `pnpm exec vitest run src/index.test.ts` (from functions/) → 90/90 passing (76 pre-existing + 14 new, nothing broken); `pnpm run type-check:all` (exit 0); `eslint functions/src/index.test.ts --max-warnings 0` (exit 0); `prettier --check functions/src/index.test.ts` (clean). This resolves Priority 1 of the MEDIUM item — only the Priority 2 `spotifyOAuth.ts` sub-target remains open on it. PR opened to dev-paul._

_2026-08-12 (Wednesday, action): Closed the Priority 1 sub-target ("rate limit enforcement, admin vs. non-admin quota paths") of the MEDIUM "functions/src/ Cloud Function execution paths lack test coverage" item — the highest-priority Open across today's reading list (daily journals: widget-registry no open items, css-scaling only LOW, typescript-eslint none; Wednesday weeklies firestore-rules/simplification/ai-integration top out at MEDIUM, and test-coverage is first in the reading order so its first structured MEDIUM item — this one — wins). File-recency check passed: `functions/src/index.ts` was last touched at `59cf8bb8` (#2369 merge), far outside the last 5 branch commits (fdc191bc, 649e792a, 024f1d0a, 82f25f42, 3074132c). **Correction to this item's stale detail:** `functions/src/index.ts` is now a 145-line thin barrel of re-exports (F12 refactor) — the actual `generateWithAI`/`generateVideoActivity`/`transcribeVideoWithGemini`/`generateGuidedLearning` implementations live in `functions/src/aiGeneration.ts` (2309 lines), and `exchangeSpotifyAuthCode`/`refreshSpotifyAccessToken`/`revokeSpotifyAuth` live in `functions/src/spotifyOAuth.ts`. The listed "Priority 3" student-auth functions (`studentLoginV1`, `getAssignmentPseudonymV1`, `getStudentClassDirectoryV1`, `commitRosterPinIndexV1`, `pinLoginV1`) are already fully covered by `functions/src/studentIdentity.test.ts` (48 tests, closed 2026-07-29 — see Completed) and `generateVideoActivity` already has partial accessLevel-enforcement coverage in `index.test.ts`. The Priority-1 quota-classification helpers (`isExternalCaller`, `resolveOrgIdForToken`, `pickOverallLimit` — the W7 external/org daily-AI-cap split, exactly what "admin vs. non-admin quota paths" refers to) turned out to already have a dedicated test file, `functions/src/aiQuotaExternalLimit.test.ts` (22 tests) — missed by the initial grep against `index.test.ts` because these tests import directly from `./aiGeneration` rather than through the barrel. Added 5 tests closing the remaining real gaps in that file (22 → 27): a `console.warn`-on-lookup-error assertion, a cached-miss-on-hd-domain-still-falls-through-to-email-domain case (previously uncovered branch), a within-TTL cache-hit case, a TTL-expiry re-resolution case (the 5-minute cache TTL had no test at all), and an explicit `__resetOrgResolutionCache` case. `pnpm exec vitest run functions/src/aiQuotaExternalLimit.test.ts` → 27 passing; full functions suite 41 files / 803 tests passing; `pnpm run type-check:all` (exit 0); `eslint --max-warnings 0` on the changed file (exit 0, run via functions/'s own config — root ESLint ignores `functions/`); `prettier --check` (clean). **Remaining real gaps on this item** (Priority 1 is now done): `functions/src/spotifyOAuth.ts` has zero test coverage (no `spotifyOAuth.test.ts`; `googleOAuth.test.ts` exists and its mock pattern would port cleanly); `transcribeVideoWithGemini` and `generateGuidedLearning` (both in `aiGeneration.ts`) still have no execution-path coverage. Left Open below with corrected file paths and priorities. PR opened to dev-paul._

_2026-08-10 (Monday, action): Closed the final sub-target (Priority 3, `utils/plcActivity.ts`) of the MEDIUM "utils/ files with complex logic have no test coverage" item — the highest-priority Open across today's reading list (daily journals: widget-registry no open items, css-scaling only LOW, typescript-eslint none; Monday weeklies top out at MEDIUM, and test-coverage is first in the reading order so its first structured MEDIUM item wins). File-recency check passed: `utils/plcActivity.ts` was last touched at `9689dba7` (#2368), far outside the last 5 branch commits (13505cb5, c26ad917, 0e79f85f, f58cb0db, f4830ebe). Added `tests/utils/plcActivity.test.ts` (27 tests) covering all five exports: the `MENTION_ACTIVITY_TARGET_TYPE`/`COMMENT_ACTIVITY_TARGET_TYPE` sentinel constants; `isForeignMentionEvent` (mention-to-other → true, mention-to-self → false, general comment → false, no-targetType → false, signed-out uid null → mention treated as foreign); `parseActivity` (full doc with passed-id authoritative over a forged body `id`, minimal doc with absent optional keys, null on out-of-union `type` / missing `type` / non-string `actorUid` / non-string `actorName`, `createdAt` resolution via the REAL `tsToMillis` from Timestamp `toMillis` / plain number / missing→0, and non-string optional target fields dropped); `deriveUnreadCount` (null-cursor counts all incl. pending createdAt 0, strictly-after-cursor filter, pending createdAt 0 not counted against a numeric cursor, foreign mentions excluded but general comment kept, mention-to-self counted, selfUid default null → mentions foreign, empty list → 0); and `writePlcActivityEvent` (minimal doc to `plcs/{plcId}/activity` with id pinned to the auto-allocated ref and `serverTimestamp()` createdAt and no target keys, full/partial optional target field passthrough, no `logError` on success, and the fire-and-forget failure path — resolves to undefined without throwing while `logError('writePlcActivityEvent', err, { plcId, type })` fires). Built on stubbed `firebase/firestore` (collection/doc/serverTimestamp/setDoc) + `@/config/firebase` db + `@/utils/logError`, with the REAL pure `@/utils/plc` `tsToMillis` left unmocked so the timestamp branches run faithfully. `pnpm exec vitest run tests/utils/plcActivity.test.ts` → 27 passing; `pnpm run type-check`, `pnpm exec eslint --max-warnings 0`, and `pnpm exec prettier --check` all clean. This resolves the last remaining sub-target of the MEDIUM utils/ complex-logic item — moved to Completed. PR opened to dev-paul._

_2026-08-10: Weekly coverage audit (Monday). pnpm test: exit 0, 604 files / 7321 tests all passing (up from 588/7129 on the 2026-08-03 audit run — net +16 files, +192 tests from new colocated test files added since then). New dev-paul commits since 2026-08-03 absorbed via rebase: c26ad917 / 0e79f85f docs only; f58cb0db fix(a11y) #2416 (back-face settings primitives — no new untested source files); f4830ebe pr-review docs only. New source file check: utils/plcDirectorySubscriptionKey.ts (added in fix(plc) #2409 absorbed 2026-08-09) — confirmed has colocated test file utils/plcDirectorySubscriptionKey.test.ts; no coverage gap. All existing MEDIUM/LOW items confirmed unchanged: MEDIUM utils/ complex-logic gaps (plcActivity.ts still open; spotifyAuth.ts done 2026-08-03); MEDIUM Cloud Function execution paths (generateWithAI, generateVideoActivity, transcribeVideoWithGemini, generateGuidedLearning, exchangeSpotifyAuthCode, refreshSpotifyAccessToken, revokeSpotifyAuth — all still open); MEDIUM utils/ coverage untested files (~32 files); LOW widget coverage (~17 remaining). Zero new gaps detected._

_2026-08-03 (Monday, action): Closed Priority 1 of the MEDIUM "utils/ files with complex logic have no test coverage" item — `utils/spotifyAuth.ts`, the highest-priority Open across today's reading list (daily journals: widget-registry no open items, css-scaling only LOW, typescript-eslint none; Monday weeklies top out at MEDIUM, and test-coverage is first in the reading order so its first MEDIUM wins). File-recency check passed: `utils/spotifyAuth.ts` was last touched at `c185cd6b` (2026-06-27), far outside the last 5 branch commits (f7d341fa, 598cc13f, d0cef414, 67bad4cc, ee5509fc). Added `tests/utils/spotifyAuth.test.ts` (37 tests) covering: the PKCE authorize-URL construction + popup messaging protocol (`runSpotifyAuthPopup` — auth-bypass short-circuit, missing `VITE_SPOTIFY_CLIENT_ID`, popup-blocked, the S256 `code_challenge`/`state`/`show_dialog` URL params, code capture via postMessage, wrong-origin + mismatched-state rejection, `access_denied`→cancelled, non-cancel error passthrough, `callback-missing-code`, and user-closed-popup detection via the 500ms closed-poll interval under fake timers); the in-memory access-token cache with its 60s skew, inflight-dedup, and cache-generation guard (`getValidAccessToken`/`getValidAccessTokenOrNull` — cache hit, near-expiry refresh, empty-cache refresh-then-serve, needs-consent `FunctionsError`, transient generic error, concurrent-dedup, mid-flight cache-clear `no-cache-bump`, auth-bypass); and the callable wrappers (`exchangeSpotifyCode` success/needs-consent/error, `disconnectSpotify` ok/failure/auth-bypass with cache-clear verification). Built on a partial `firebase/functions` mock (per-function-name handler registry via `httpsCallable`, REAL `FunctionsError` kept via `importOriginal` so `err instanceof FunctionsError` + `err.details` branches run faithfully) and a `@/config/firebase` mock with a live-getter `isAuthBypass`. `pnpm exec vitest run tests/utils/spotifyAuth.test.ts` → 37 passing; `pnpm run type-check`, `pnpm exec eslint --max-warnings 0`, and `pnpm exec prettier --check` all clean. Progress note added to the MEDIUM utils/ complex-logic item; `spotifyAuth.ts` removed from its untested File list. Remaining Priority 3 (`plcActivity.ts`) still open. All other existing open items remain valid._

_2026-08-03: Weekly coverage audit (Monday). pnpm test: exit 0, 588 files / 7129 tests all passing (up from 587/7108 on the 2026-07-29 audit run — net +1 file, +21 tests from the 2026-07-29 action adding `functions/src/studentIdentity.test.ts`). New dev-paul commits since 2026-07-29 absorbed via rebase: docs(spike) multilingual pronunciation plan (#2343 — spike-doc commits only; no new source files). fix(GraphicOrganizer) hide dead fontColor picker in appearance settings (12f86641 — appearance settings panel UI change; no new untested source files). No HIGH items remain in this journal (hooks/ session coverage closed 2026-07-06; security-critical utils/hooks HIGH closed 2026-07-29). All existing MEDIUM/LOW items confirmed unchanged (MEDIUM utils/ complex-logic gaps — spotifyAuth, imageProcessing, plcActivity etc.; MEDIUM Cloud Function execution paths — generateWithAI, generateVideoActivity, Spotify OAuth funcs; MEDIUM utils/ coverage untested files; LOW widget coverage ~17 remaining). Zero new gaps detected._

_2026-07-29 (Wednesday, action): Closed the final sub-target of the HIGH "Security-critical and stateful utils/hooks with zero test coverage" item — highest-severity Open across today's reading list (daily journals: widget-registry no open items, css-scaling only LOW, typescript-eslint none; Wednesday weeklies firestore-rules/simplification/ai-integration top out at MEDIUM). HIGH wins outright. File-recency check passed: `functions/src/studentIdentity.ts` was last touched at `1b055ffe` (#2093), far outside the last 5 branch commits (59e520b8, 33e2cf0e, 2d5a7f67, edff9339, ec80a4b8). Added `functions/src/studentIdentity.test.ts` (48 tests) covering all 5 Cloud Functions: **studentLoginV1** (missing idToken → invalid-argument; unset secret → internal; unverifiable / unverified-email / malformed-email token → unauthenticated; unregistered domain → permission-denied with NO token minted; testClasses bypass mints `test:{email}` HMAC uid + `classIds` from the doc ids with zero OneRoster calls; OneRoster happy path mints `computeStudentUid(sourcedId)` + the exact `{studentRole, orgId, classIds}` claim shape; hd-claim domain preferred over the email suffix for the org gate; no-student-in-roster → not-found; quote-in-email OneRoster-filter guard → not-found before any axios call; ClassLink network failure → internal; createCustomToken failure → internal), **getAssignmentPseudonymV1** (unauth / non-student / missing / over-200-char assignmentId / unset-secret guards, and the deterministic `HMAC("asn:"+uid+":"+assignmentId)` pseudonym — stable within a tuple, unlinkable across assignments), **getStudentClassDirectoryV1** (auth/role/non-array-classIds/empty-classIds guards, ClassLink roster → directory entry with resolved teacher name, testClasses fallback for unresolved ids, unresolved ids dropped, Auth-lookup failure → empty teacher name with the row still rendered), **commitRosterPinIndexV1** (unauth / student-role-forbidden / missing-rosterId / non-array-entries / over-max / roster-not-found guards, local-roster no-op with `skippedReason:'no-classlink-class-id'`, desired-index write + stale-entry prune with the `computeStudentUid` pseudonym payload, malformed-entry skip counting), **pinLoginV1** (missing kind/pin, quiz-without-code, VA-without-sessionId, no-joinable-session / session-not-found / session-missing-teacher / no-rosters / no-index-entry `matched:false` fallbacks, quiz + video-activity happy paths minting the SSO-matching `{studentRole, orgId, classIds:[classId]}` token, createCustomToken failure → internal). Built on a path-addressed in-memory Firestore mock (supports `collection`/`collectionGroup`/`doc`/`where`(`==`/`in`/`array-contains`)/`limit`/`batch` and `ref.parent.parent.id` reconstruction), stubbed `google-auth-library`/`axios`/`./secrets`/`./functionsInit`, and the REAL `./classlinkShared` so the HMAC uid/pseudonym assertions exercise production derivation. Full functions suite green at 40 files / 775 tests (48 new); `tsc --noEmit`, `eslint --max-warnings 0`, and `prettier --check` all clean. This resolves the HIGH item entirely — moved to Completed. No HIGH items remain in this journal; highest remaining Open are the three MEDIUM coverage gaps (untested utils/ files, Cloud Function execution paths) plus the LOW widget-coverage item._

_2026-07-29: Weekly coverage audit (Wednesday). pnpm test: exit 0, 587 files / 7108 tests all passing (up from 573/6986 on 2026-07-22 — net +14 files, +122 tests, accounting for 2026-07-27 action adding shortLinksApi + useShortLinks tests plus MiniAppManager refactor on 2026-07-28). No failing tests. New dev-paul commits since 2026-07-22 absorbed via rebase: refactor(MiniAppManager) drop redundant iconClassName (back-face manager, 2-line deletion, no new source files); pr-review docs only; deps/yaml-stack-overflow (dependency patch); unifier log; b4b1f504 Unify MiniAppManager empty states (back-face). None of these introduce new untested source files. `functions/src/studentIdentity.ts` remains the lone remaining sub-target of the resolved HIGH item (5 Cloud Functions, PIN validation + auth session paths). All existing MEDIUM/LOW items confirmed unchanged. Zero new gaps detected._

_2026-07-27 (Monday, action): Selected the lone HIGH item — highest-severity Open across today's reading list (daily journals: widget-registry no open items, css-scaling only LOW, typescript-eslint none; the other Monday weekly firestore-rules tops out at MEDIUM). HIGH beats all, so this item wins outright. File-recency check passed: `utils/shortLinksApi.ts`, `hooks/useShortLinks.ts`, and `functions/src/studentIdentity.ts` were all last touched at `b778aac2` (#2092) — far outside the last 5 branch commits (6b78f79b, 8bf9feb2, 44b711a3, 58049fb8, 3117643d). Closed 2 of the 3 remaining sub-targets: added `tests/utils/shortLinksApi.test.ts` (9 tests — `resolveShortLink` null-on-missing / data-on-hit / correct doc ref / read-error propagation; `recordShortLinkClick` atomic `increment(1)` + `lastClickedAt` payload / correct ref / write-error propagation; collection-name constant) and `hooks/useShortLinks.test.ts` (23 tests covering `useCreateShortLink` — signed-out/non-admin fast-fail, destination & slug validation, taken-slug collision, full-metadata create, empty-label omission, the 5-retry random-code loop past collisions, retry-budget exhaustion, and generic error+`logError` fallback — and `useShortLinks` — non-admin no-subscribe, snapshot hydrate/error/unsubscribe, `updateShortLink` non-admin/missing-doc/invalid-destination/success/blank-label-`deleteField`/error paths, `deleteShortLink` non-admin-throw/admin-delete, and `refresh` no-op/hydrate/error). The transaction body of `createShortLinkAtomic` runs for real via a controllable `runTransaction` mock; the pure `shortLinkValidation` helpers (already covered) are used unmocked. All 32 new tests pass; `pnpm run type-check`, `pnpm exec eslint --max-warnings 0`, and `pnpm exec prettier --check` all clean. **Remaining sub-target:** `functions/src/studentIdentity.ts` (5 Cloud Functions, 1307 lines) — needs a `firebase-admin` mock harness and is a substantial standalone effort; left open below. All other existing open items remain valid._

_2026-07-22: Weekly coverage audit (Wednesday). No new dev-paul commits absorbed (rebase not performed — dev-paul diverged). pnpm test not re-run (baseline unchanged from 2026-07-20: 573 files / 6986 tests). Deep targeted audit of utils/, hooks/, and functions/src/ for high-risk untested code. Seven new HIGH-priority gaps identified and added as a new HIGH open item: (1) `utils/globalPermissionsErrors.ts` — singleton error-handler latch, `__resetForTests` export signals clear test intent, handler registration ordering matters; (2) `utils/lastActiveThrottle.ts` — injectable storage abstraction, throttle guard with stable/unstable keys — testable in isolation; (3) `utils/quizSyncMigration.ts` — pure data transformer with 4 distinct migration branches, no I/O — trivially unit-testable; (4) `utils/shortLinksApi.ts` — public no-auth Firestore ops (short link creation, collision detection) — security-adjacent; (5) `utils/snapshotThrottle.ts` — stateful leading/trailing throttle for Firestore snapshot listeners, flush-on-teardown path; (6) `hooks/useStudentIdleTimeout.ts` — security-critical: idle auto-signout on shared Chromebooks, timer reset on user interaction; (7) `hooks/useShortLinks.ts` — 5-retry collision-avoidance loop for short link slug generation. New HIGH item added below. Additional MEDIUM gaps confirmed: new hooks `useImageUpload`, `useStudentClassDirectory`, `usePlcPresence`, `useTestClasses`, `useAdminBuildings` have zero test coverage — added to existing MEDIUM utils/ coverage item. Cloud Functions: `syncedQuizGroups`, `syncedVideoActivityGroups`, `finalizeIdleQuizAttempts`, `expireSubShares`, `transcribeVideoWithGemini` (access-level gate), Spotify OAuth functions — all still zero coverage (already in MEDIUM CF item). `functions/src/studentIdentity.ts` — `studentLoginV1`, `getAssignmentPseudonymV1`, `getStudentClassDirectoryV1`, `commitRosterPinIndexV1`, `pinLoginV1` already listed in MEDIUM CF item; these are now confirmed HIGH security-critical priority and elevated in the new HIGH item below. All existing open items remain valid._

_2026-07-20: Full test run. pnpm test exit 0 — 573 files / 6986 tests, all passing (up from 561/6808 on 2026-07-13 — 12 new test files, 178 new tests). New dev-paul commits since 2026-07-13 (absorbed via rebase): pr-review 7 PRs (docs only), refactor(types) GraphicOrganizerLayoutType (types.ts interface refactor — no new utils/hooks/functions files), fix(admin-config) GraphicOrganizer template override (DashboardContext/admin panel only), feat(admin-config) GraphicOrganizer building appearance defaults (admin panel only). No new untested utils/, hooks/, or functions/ files introduced by these commits. New gap identified this audit: `functions/src/spotifyOAuth.ts` — exports `exchangeSpotifyAuthCode`, `refreshSpotifyAccessToken`, and `revokeSpotifyAuth`, three Cloud Functions with no test coverage. Added to MEDIUM Cloud Functions item below. All other existing open items remain valid._

_2026-07-13 (Monday, action): Added `tests/utils/quizAudio.test.ts` (13 tests) — Priority 2 of the MEDIUM "utils/ files with complex logic" item. Covers `utils/quizAudio.ts`: the six synthesized sound effects (`playCorrectChime`, `playIncorrectBuzz`, `playCountdownTick`, `playPodiumFanfare`, `playQuizCompleteCelebration`, `playStreakSound`) asserting per-effect oscillator counts and gain/destination wiring; AudioContext lifecycle (resume on suspended, no-resume on running, single-instance singleton reuse, `webkitAudioContext` Safari fallback); and graceful degradation (no throw when no AudioContext implementation exists and when the constructor throws). Tests use `vi.resetModules()` + dynamic import to reset the module-level singleton per case. `pnpm exec vitest run tests/utils/quizAudio.test.ts` → 13 passing; `pnpm run type-check`, `pnpm exec eslint --max-warnings 0`, and `pnpm exec prettier --check` all clean. Progress note added to the MEDIUM utils/ complex-logic item; `quizAudio.ts` removed from its untested File list. Remaining Priority 1 (`spotifyAuth.ts`) and Priority 3 (`plcActivity.ts`) still open._

_2026-07-13: Full test run. pnpm test exit 0 — 561 files / 6808 tests, all passing. No failing tests. New dev-paul commits since 2026-07-01 (absorbed via rebase): docs-only changes. Four new untested utils/ files identified since 2026-07-01 audit: `utils/activityWallNormalize.ts` (activity wall post normalization), `utils/guidedLearningMedia.ts` (guided learning media handling), `utils/ltiCourseLinks.ts` (LTI course link management), `utils/miniAppNormalize.ts` (mini-app normalization). Progress note added to MEDIUM utils/ coverage open item. All other existing open items remain valid._

_2026-07-06 (Monday, action): Closed the HIGH hooks/ session-coverage item — added `tests/hooks/useRosters.test.ts` (35 tests) for the last remaining untested Priority-3 session hook (`hooks/useRosters.ts`: roster metadata Firestore listener + Drive-backed student PII sync, `commitRosterPinIndexV1` pin-index sidecar, and the one-time PII migration). `pnpm exec vitest run tests/hooks/useRosters.test.ts` → 35 passing; tsc/eslint/prettier all clean. Item moved to Completed. Highest remaining Open items are the two MEDIUM coverage gaps (utils/ untested files; Cloud Function execution paths) plus the LOW widget-coverage item — no HIGH items remain in this journal._

_2026-07-01: Full test run. pnpm test exit 0 — all tests passing. No failing tests. New dev-paul commit since 2026-06-22 (rebased onto scheduled-tasks): style(docs) fix prettier formatting in unifier.md — docs-only, no new test files added or removed. All existing open items remain valid: HIGH hooks/ session hooks gaps (useRosters.ts, useScreenRecord.ts remain untested); MEDIUM utils/ coverage gaps (spotifyAuth.ts, imageProcessing.ts, plcActivity.ts, quizAudio.ts, guidedLearningDriveService.ts, shortLinksApi.ts, pexelsService.ts, previewMode.ts, lastActiveThrottle.ts, classroomCourses.ts); MEDIUM Cloud Function execution path gaps (generateWithAI execution paths, generateVideoActivity, transcribeVideoWithGemini, generateGuidedLearning, student auth functions). No new gaps identified._

_2026-06-22: Full audit. Test suite: 516 files / 5697 tests, all passing (up from 473/4937 on 2026-06-17 — 43 new test files, 760 new tests). No failing tests. New untested utils confirmed: `spotifyAuth.ts` (Spotify PKCE OAuth flow — security-critical), `imageProcessing.ts` (web worker image trimming), `plcActivity.ts` (fire-and-forget PLC activity log writer), `quizAudio.ts` (Web Audio API sound effects), `guidedLearningDriveService.ts` (Google Drive GL content), `shortLinksApi.ts` (short link routing), `pexelsService.ts` (Pexels image search), `previewMode.ts` (teacher preview toggle), `lastActiveThrottle.ts` (activity write throttle), `classroomCourses.ts` (Google Classroom course fetching). Top untested hooks: `useQuiz.ts` (Drive+Firestore quiz sync — complex), `useGuidedLearning.ts` (personal vs building set logic), `useStudentAssignments.ts` (real-time assignment filtering). Top untested Cloud Functions: `expireSubShares` (scheduled deletion), `ltiLogin/ltiLaunch/ltiExchange` (LTI 1.3 OAuth endpoints), `finalizeIdleQuizAttempts` (scheduled cleanup). Added new MEDIUM open item for utils coverage gaps. Existing open items (Cloud Function execution paths, hooks session coverage) remain valid and unchanged._

### MEDIUM functions/src/ Cloud Function execution paths lack test coverage

- **Detected:** 2026-06-17
- **File:** functions/src/aiGeneration.ts, functions/src/spotifyOAuth.ts (corrected 2026-08-12 — `functions/src/index.ts` is now a 145-line thin barrel of re-exports; the real implementations live in these leaf modules, plus `functions/src/index.test.ts` and `functions/src/aiQuotaExternalLimit.test.ts` for existing coverage)
- **Progress (2026-08-12, action):** Priority 1 (`generateWithAI` rate-limit/quota-classification paths) is DONE — `functions/src/aiQuotaExternalLimit.test.ts` (27 tests) covers `isExternalCaller`/`resolveOrgIdForToken`/`pickOverallLimit` including cache TTL and fail-safe behavior. Priority 3 (student auth functions) is DONE — `functions/src/studentIdentity.test.ts` (48 tests, closed 2026-07-29). `generateVideoActivity` has partial coverage (accessLevel enforcement) in `index.test.ts`. Remaining gaps: `transcribeVideoWithGemini` and `generateGuidedLearning` (both in `aiGeneration.ts`) have no execution-path coverage; `functions/src/spotifyOAuth.ts` (`exchangeSpotifyAuthCode`, `refreshSpotifyAccessToken`, `revokeSpotifyAuth`) has zero test coverage — no `spotifyOAuth.test.ts` exists, though `googleOAuth.test.ts` establishes a portable OAuth-mock pattern for the sibling `googleOAuth.ts` module.
- **Progress (2026-08-17, action):** Priority 1 (`transcribeVideoWithGemini` + `generateGuidedLearning`) is DONE — added 14 tests to `functions/src/index.test.ts` (8 for `transcribeVideoWithGemini`: unauthenticated, no-email, permission-doc-missing, disabled, admin-only accessLevel rejection, beta accessLevel rejection, admin caller reaches the real `ai.models.generateContent` call, invalid video URL; 6 for `generateGuidedLearning`: unauthenticated, no-email, non-admin rejection, no-images, too-many-images, admin caller with a valid image reaches the AI call). Extended the shared `mockFirestore.collection('global_permissions')` handler with a new hoisted `audioTranscriptionPermDocGet` mock (mirroring `geminiConfigDocGet`) to serve the `video-activity-audio-transcription` doc that `transcribeVideoWithGemini`'s accessLevel gate reads. All error-message assertions copied verbatim from `aiGeneration.ts`. `functions/src/index.test.ts` now 90/90 passing (76 pre-existing + 14 new). `pnpm run type-check:all` exit 0; `eslint functions/src/index.test.ts --max-warnings 0` exit 0; `prettier --check` clean. **Remaining gaps on this item:** `functions/src/spotifyOAuth.ts` (`exchangeSpotifyAuthCode`, `refreshSpotifyAccessToken`, `revokeSpotifyAuth`) still has zero test coverage.
- **Detail:** The following exported Cloud Functions still have no test coverage for their execution paths:
  - `exchangeSpotifyAuthCode` — Spotify PKCE code exchange (security-critical OAuth step). Untested.
  - `refreshSpotifyAccessToken` — Spotify token refresh. Expiry handling, error paths untested.
  - `revokeSpotifyAuth` — Spotify token revocation. Firestore cleanup, revocation API call untested.
  - Note: `classroomAddonAuth.ts` functions have a separate colocated test file (`classroomAddonAuth.test.ts`) and are NOT a gap. `studentIdentity.ts`, the W7 quota-classification helpers in `aiGeneration.ts`, and (as of 2026-08-17) `transcribeVideoWithGemini`/`generateGuidedLearning` are also NOT gaps (see Progress above).
- **Fix:** Priority 2 (next, and only remaining) — `spotifyOAuth.ts` (port the `googleOAuth.test.ts` mock pattern).

### MEDIUM utils/ coverage — untested utility files

- **Detected:** 2026-04-13
- **Progress (2026-08-24, audit):** Count re-derived by script as the 2026-08-17 note asked. Actual untested count is **16**, not "~20". Three files still named in the stale lists below now have tests and are struck: `utils/firestorePaging.ts`, `utils/imageWorker.ts`, `utils/classroomGradePush.ts`. The 2026-08-17 note that `activityWallNormalize.ts`/`miniAppNormalize.ts` are covered (under `activityWallLibraryNormalize.test.ts` / `miniAppSessionNormalize.test.ts`) is confirmed correct. **Current true list (16, supersedes every list below):** `classroomCourses`, `collectionColor`, `deleteDrawingPageSubcollection`, `guidedLearningDriveService`, `guidedLearningMedia`, `imageProcessing`, `logError`, `ltiCourseLinks`, `periodCompat`, `pexelsService`, `plcWriteNotifications`, `previewMode`, `soundboardConfig`, `spotifyPlaybackSdk`, `spotifyPremiumNotice`, `viewTransition`.
- **Progress (2026-08-17, audit):** Count is smaller than last tracked, not larger — no new utils/ files added since 2026-08-10. Two files previously assumed untested are actually covered under differently-named test files: `activityWallNormalize.ts` → `activityWallLibraryNormalize.test.ts`; `miniAppNormalize.ts` → `miniAppSessionNormalize.test.ts`. Actual untested count is now ~20 (down from ~32). The list below is stale and should be re-derived from a fresh utils/-vs-tests/ diff on the next action pass rather than trusted as-is.
- **Progress (2026-07-13, audit):** Four new untested utils/ files added since 2026-07-01: `activityWallNormalize.ts` (activity wall post normalization utilities — post merging/deduplication helpers), `guidedLearningMedia.ts` (guided learning media fetching and processing), `ltiCourseLinks.ts` (LTI course link CRUD and validation), `miniAppNormalize.ts` (mini-app post/submission normalization). None have colocated or tests/ suite test files. Total untested files in utils/ is approximately 32 (up from ~28 at 2026-07-01).
- **Progress (2026-06-10, audit):** Two new untested files added since 2026-06-08: `utils/firestorePaging.ts` (52 lines — page-cursor loop for score-publish paths; testable as a pure async iterator) and `utils/snapshotThrottle.ts` (106 lines — leading/trailing throttle for Firestore snapshot listeners; flush-on-teardown path is untested). `utils/mapWithConcurrency.ts` was added concurrently and already has a colocated test. Full untested list now includes: `firestorePaging.ts`, `snapshotThrottle.ts`, `collectionColor.ts`, `globalPermissionsErrors.ts`, `guidedLearningDriveService.ts`, `imageProcessing.ts`, `imageWorker.ts`, `lastActiveThrottle.ts`, `logError.ts`, `periodCompat.ts`, `pexelsService.ts`, `plcWriteNotifications.ts`, `previewMode.ts`, `quizAudio.ts`, `quizSyncMigration.ts`, `shortLinksApi.ts`, `soundboardConfig.ts`, `spotifyAuth.ts`, `spotifyPlaybackSdk.ts`, `spotifyPremiumNotice.ts`, `viewTransition.ts`, `classroomGradePush.ts`, `deleteDrawingPageSubcollection.ts`.
- **Progress (2026-06-01, audit):** utils/ continues to grow. Two newly confirmed untested files since 2026-05-27: `classroomGradePush.ts` (grade push logic and field mapping) and `deleteDrawingPageSubcollection.ts` (subcollection deletion, handles missing docs gracefully). Both should be tested for happy path + error/missing-doc edge cases. Previously tracked untested files remain: `collectionColor.ts`, `globalPermissionsErrors.ts`, `guidedLearningDriveService.ts`, `imageProcessing.ts`, `imageWorker.ts`, `lastActiveThrottle.ts`, `logError.ts`, `periodCompat.ts`, `pexelsService.ts`, `plcWriteNotifications.ts`, `previewMode.ts`, `quizAudio.ts`, `quizSyncMigration.ts`, `shortLinksApi.ts`, `soundboardConfig.ts`, `spotifyAuth.ts`, `spotifyPlaybackSdk.ts`, `spotifyPremiumNotice.ts`, `viewTransition.ts`.
- **Progress (2026-05-27, audit):** utils/ now has 102 files (excl. mocks/tests). 20 files confirmed untested (compared against both tests/utils/ and colocated \*.test.ts files): `collectionColor.ts`, `globalPermissionsErrors.ts`, `guidedLearningDriveService.ts`, `imageProcessing.ts`, `imageWorker.ts`, `lastActiveThrottle.ts`, `logError.ts`, `periodCompat.ts`, `pexelsService.ts`, `plcWriteNotifications.ts`, `previewMode.ts`, `quizAudio.ts`, `quizSyncMigration.ts`, `shortLinksApi.ts`, `soundboardConfig.ts`, `spotifyAuth.ts`, `spotifyPlaybackSdk.ts`, `spotifyPremiumNotice.ts`, `viewTransition.ts`. **Resolved since 2026-05-18:** `contentEditableDragSelect.ts` gained `utils/contentEditableDragSelect.test.ts` ✓; `writtenAnnotations.ts` gained `tests/utils/writtenAnnotations.test.tsx` ✓ (net count: 16 → 20, 5 new gaps added, 2 resolved = net +3 unique). New mock files added (`mockGuidedLearningDriveService.ts`, `mockQuizDriveService.ts`) are test infrastructure themselves — no tests expected. New since 2026-05-18 (no test): `collectionColor.ts` (hex color manipulation — `hexToRgba`, `pickReadableForeground`, `collectionTextColor`), `globalPermissionsErrors.ts` (error handler registration — `setGlobalPermissionsErrorHandler`, `reportGlobalPermissionsError`), `spotifyAuth.ts` (OAuth popup flow — `runSpotifyAuthPopup`, `exchangeSpotifyCode`, `getSpotifyRedirectUri`), `spotifyPlaybackSdk.ts` (Spotify Web Playback SDK loader — `loadSpotifySdk`), `spotifyPremiumNotice.ts` (localStorage-backed dismiss flag — `hasDismissedSpotifyPremiumNotice`, `setSpotifyPremiumNoticeDismissed`).
- **Progress (2026-05-18, audit):** utils/ now has 101 files (excl. mocks/tests). 16 files confirmed untested (compared against both tests/utils/ and colocated \*.test.ts files): `contentEditableDragSelect.ts`, `guidedLearningDriveService.ts`, `imageProcessing.ts`, `imageWorker.ts`, `lastActiveThrottle.ts`, `logError.ts`, `periodCompat.ts`, `pexelsService.ts`, `plcWriteNotifications.ts`, `previewMode.ts`, `quizAudio.ts`, `quizSyncMigration.ts`, `shortLinksApi.ts`, `soundboardConfig.ts`, `viewTransition.ts`, `writtenAnnotations.ts`. New since 2026-05-13: `contentEditableDragSelect.ts` (drag-select for content-editable areas), `viewTransition.ts` (View Transitions API wrapper), `writtenAnnotations.ts` (quiz written-response annotation utilities). Files added since 2026-05-13 that already have tests: `adminBuildingConfig.ts`, `collectionTemplateHydration.ts`, `collectionsMigration.ts`, `dashboardSanitize.ts`, `googleOAuthRefresh.ts`, `pickInitialBoard.ts`, `contentEditableBlocks.ts`, `contentEditableLists.ts`.
- **Progress (2026-05-13, audit):** Test suite: 229 files / 2375 tests all passing. utils/ now has 83 files (not mocks/tests). 13 files confirmed untested (compared against both tests/utils/ and colocated \*.test.ts files): `guidedLearningDriveService.ts`, `imageProcessing.ts`, `imageWorker.ts`, `lastActiveThrottle.ts`, `logError.ts`, `periodCompat.ts`, `pexelsService.ts`, `plcWriteNotifications.ts`, `previewMode.ts`, `quizAudio.ts`, `quizSyncMigration.ts`, `shortLinksApi.ts`, `soundboardConfig.ts`. Previously unlisted new additions: `imageWorker.ts` (image processing web worker), `lastActiveThrottle.ts` (auth throttle helper), `plcWriteNotifications.ts` (PLC notification writer), `previewMode.ts` (preview toggle), `shortLinksApi.ts` (short link client API).
- **Progress (2026-05-04, audit):** Count unchanged at 18 untested but total file count grew from 45 to 53. Since the 2026-04-29 audit, seven new util files were added — five of those eight have tests already. Good velocity. Still untested: `logError.ts`, `quizSyncMigration.ts`, `guidedLearningDriveService.ts`, `imageProcessing.ts`, `pexelsService.ts`, `quizAudio.ts`, `soundboardConfig.ts`, `plc.ts`, `testClassAccess.ts`, `widgetDragFlag.ts`, `styles.ts`, `first5.ts`, `periodCompat.ts`.
- **Progress (2026-04-29, action):** Added test coverage for `backgrounds.ts` (21 tests) and `slug.ts` (20 tests). Also corrected prior count: `googleCalendarService.ts` already had a test file.
- **Progress (2026-04-22 → 2026-04-29):** `googleDriveService.ts`, `quizDriveService.ts`, `security.ts`, `widgetHelpers.ts`, `urlHelpers.ts` all gained test files.
- **File:** utils/ directory
- **Fix:** Next priority is `quizSyncMigration.ts` (pure mapper, trivially testable) and `logError.ts` (simple console.error wrapper). Then `soundboardConfig.ts` (pure parser) and `plc.ts` (business logic). Use vi.mock() for service wrappers.

### LOW widget test coverage — significant improvement, ~26 widgets remain untested

- **Detected:** 2026-04-13
- **Progress (2026-08-31, audit):** `HotspotImage` (one of the fully-untested-8) now has `components/widgets/HotspotImage/Widget.test.tsx`. `MathTools` (one of the three "missing from this item's list" dirs) now has `components/widgets/MathTools/Settings.test.tsx`, moving it from "fully untested" to "partially covered" (its `Widget.tsx` remains uncovered). Fully-untested count drops from 11 to **9**: BloomsTaxonomy, CarRiderPro, ConceptWeb, NeedDoPutThen, Onboarding, SyntaxFramer, WorkSymbols, Webcam, BlendingBoard.
- **Progress (2026-08-24, audit):** `MathToolInstance` is no longer fully untested — it now has `components/widgets/MathToolInstance/Settings.test.tsx` (its `Widget.tsx`, 244 lines, remains uncovered, moving it into the "partially covered" bucket with MusicWidget/SpecialistSchedule/etc.). The fully-untested set is therefore **8**, not 9: BloomsTaxonomy, CarRiderPro, ConceptWeb, HotspotImage, NeedDoPutThen, Onboarding, SyntaxFramer, WorkSymbols. A scripted `components/widgets/*/` vs `tests/` + colocated diff also found **three dirs missing from this item's list entirely**: `MathTools/` (Widget 382 + Settings 97), `Webcam/` (Widget 731 + Settings 40), `BlendingBoard/` (Widget 89 + Settings 28 + `hooks/useBlendingBoardConfig.ts` 40) — so the true untested count is 11, not 9. `Webcam/Widget.tsx` is the highest-value of the three at 731 lines: `getUserMedia` lifecycle + `enumerateDevices` camera cycling, dual-mode OCR (Gemini vs Tesseract, branched on `canAccessFeature('gemini-functions')`), and a send-to-notes path that sanitizes extracted text before writing a widget. Suggested Webcam tests: assert the `getUserMedia` constraint shape and that the stream's tracks are stopped on unmount and on device switch; assert `switchCamera` wraps around the device list; assert a `getUserMedia` rejection sets the error state without leaving a live stream; assert the OCR mode branch picks Gemini only when the feature is granted; assert `performSendToNotes` trims and sanitizes before `addWidget`; assert `clearPhotos` only clears after `showConfirm` resolves true. MathTools/BlendingBoard: standard config-round-trip + Settings-writes-config tests.
- **Progress (2026-08-17, audit):** Of the 17 widgets tracked as of 2026-08-10: three now have a direct widget test (Calendar — `Widget.test.tsx` + `tests/CalendarWidget.test.tsx`; First5 — `Widget.test.tsx`; NextUp — `Widget.test.tsx`). Five are partially covered (Settings/subcomponent/logic tested, but `Widget.tsx` itself still isn't): MusicWidget, SpecialistSchedule, CustomWidget, RevealGrid, StarterPack. Nine remain fully untested: BloomsTaxonomy, CarRiderPro, ConceptWeb, HotspotImage, MathToolInstance, NeedDoPutThen, Onboarding, SyntaxFramer, WorkSymbols. No new widget was added without a test.
- **Progress (2026-05-04, audit):** Total test suite is 185 files / 1771 tests, all passing. No new widget-level tests added since 2026-04-29. Widget count unchanged at ~26 untested. `useSessionViewCount.ts` (added after 2026-04-29) already has a test in `tests/hooks/useSessionViewCount.test.ts`.
- **Progress (2026-04-29):** Test file count grew to 61 widget test files (was 7 on 2026-04-22). New coverage includes ActivityWall, Breathing, Catalyst, Checklist, Classes, ClockWidget, Countdown, DiceWidget, DrawingWidget, Embed, ExpectationsWidget, GraphicOrganizer, GuidedLearning, InstructionalRoutines, LunchCount, MaterialsWidget, MiniApp, NumberLine, PdfWidget, PollWidget, QRWidget, RecessGear, Schedule, Scoreboard, SeatingChart, SmartNotebook, SoundWidget, SoundboardWidget, TalkingTool, TextWidget, TimeTool, TrafficLightWidget, UrlWidget, VideoActivityWidget, Weather, and math-tools/random/stickers sub-components.
- **File:** components/widgets/ directory
- **Detail:** Widgets with no test coverage include: BloomsTaxonomy, Calendar, CarRiderPro, ConceptWeb, CustomWidget (main), First5, HotspotImage, MathToolInstance, MusicWidget, NeedDoPutThen, NextUp, Onboarding, RevealGrid, SpecialistSchedule, StarterPack, SyntaxFramer, WorkSymbols and several sub-components.
- **Fix:** Focus on RevealGrid (has significant CSS scaling debt), NeedDoPutThen, WorkSymbols, and ConceptWeb. These widgets have config logic worth regression-testing. Use React Testing Library with Vitest. Mock useDashboard() and useAuth() hooks.

### LOW hooks/ coverage — ~22 hook files have no matching test file

- **Detected:** 2026-08-17
- **Progress (2026-08-31, audit):** Three of the 24 tracked files now have coverage: `useBuildingSelection` (`tests/hooks/useBuildingSelection.test.ts`), `useStudentAssignments` (three colocated `.behavior`/`.viewOnly`/`.listenerCount`/`.parsePublicationFields` test files), and `useAssignmentPseudonyms` (its sole export `useAssignmentPseudonymsMulti` is covered by `tests/hooks/useAssignmentPseudonymsMulti.test.ts` — a naming mismatch, not a real gap). Untested count drops from 24 to **21**.
- **Progress (2026-08-24, audit):** Exact list re-derived by script as this item's Fix line requested. Count is **24**, not "~22": `useAdminBuildings`, `useAssignmentPseudonyms`, `useBackgrounds`, `useBuildingSelection`, `useCatalystSets`, `useClassLinkEnabled`, `useGuidedLearning`, `useImageUpload`, `useInstructionalRoutines`, `useMiniAppAssignments`, `useOrgSubscriptionReset`, `usePlcLibraryActions`, `usePlcMeetings`, `usePlcPresence`, `usePlcSharedBoards`, `usePlcVideoActivities`, `useScaledFont`, `useScreenshot`, `useSpotifyAuth`, `useSpotifySearch`, `useStudentAssignments`, `useStudentClassDirectory`, `useTestClasses`, `useWidgetBuildingId`. `useOrgSubscriptionReset.ts` is a 36-line wrapper over the already-tested `utils/orgSubscriptionKey` — lowest priority on the list.
- **File:** hooks/ directory (full list in the 2026-08-24 progress note above)
- **Detail:** Roughly 22 files in hooks/ have no colocated `*.test.ts`/`*.test.tsx` file and no matching file under `tests/hooks/`. This dimension of coverage wasn't previously tracked as its own item (prior audits tracked hook *state density*, not hook test coverage). Severity LOW since most of these are thin Firestore-listener or API wrapper hooks rather than complex business logic, but some (useSpotifyAuth, usePlcPresence) touch auth/session state worth regression-testing.
- **Fix:** Next action pass should re-derive the exact file list via a hooks/-vs-tests/hooks/ diff (including colocated `*.test.ts` files) and prioritize any hook with security- or session-state implications first, following the pattern already established for `useRosters.test.ts`/`useShortLinks.test.ts`.

---

## Completed

### LOW `useNutrislice.ts` menu fetch/parse/cache untested

- **Detected:** 2026-08-24
- **Resolved:** 2026-08-31 — resolved outside journal workflow. `components/widgets/LunchCount/useNutrislice.test.tsx` now exists (541 lines, 12 tests).
- **File:** `components/widgets/LunchCount/useNutrislice.ts` (345 lines)
- **Detail (original):** Contains a real parser and a cache-migration state machine: `ALT_MEAL_SECTION_PATTERNS` matching, `toMenuItem` normalization, `isLegacyCachedMenu` detection of the pre-sides/images shape, and — notably — an explicit guard whose comment warns that without it a failed legacy-shape migration would "re-fire `fetchNutrislice` on the next render — pegging the proxy". That anti-hammering guard has no test.
- **Resolution:** `useNutrislice.test.tsx` covers exactly what the Fix line asked for: `isLegacyCachedMenu`/legacy-shape re-fetch, the anti-hammering guard ("does not loop when fetch fails on a legacy-shape config"), alt-meal section-name matching including the ambiguous "Alt Entree" case, same-day cache short-circuit, and bento/hotLunch dedup edge cases.

### MEDIUM utils/ files with complex logic have no test coverage

- **Detected:** 2026-06-22
- **Completed:** 2026-08-10
- **Resolution (2026-08-10, action):** Closed the final tracked sub-target (Priority 3, `utils/plcActivity.ts`) by adding `tests/utils/plcActivity.test.ts` (27 tests). All three enumerated priorities are now done (Priority 1 `spotifyAuth.ts` ✓ 2026-08-03; Priority 2 `quizAudio.ts` ✓ 2026-07-13; Priority 3 `plcActivity.ts` ✓ 2026-08-10), so the item's actionable priority list is exhausted and it retires. Coverage: the `MENTION_ACTIVITY_TARGET_TYPE`/`COMMENT_ACTIVITY_TARGET_TYPE` sentinel constants; `isForeignMentionEvent` (mention-to-other → true, mention-to-self → false, general comment → false, no-targetType → false, signed-out `uid` null → mention treated as foreign); `parseActivity` (full doc with the passed `id` authoritative over a forged body `id`, minimal doc with optional keys absent, null on out-of-union `type` / missing `type` / non-string `actorUid` / non-string `actorName`, `createdAt` resolved through the REAL `tsToMillis` from a Timestamp `toMillis` / a plain number / a missing value → 0, and non-string optional target fields dropped); `deriveUnreadCount` (null cursor counts all incl. a pending `createdAt` 0, strictly-after-cursor filter, pending `createdAt` 0 not counted against a numeric cursor, foreign mentions excluded while the general comment is kept, mention-to-self counted, `selfUid` default null → mentions treated as foreign, empty list → 0); and `writePlcActivityEvent` (minimal doc appended to `plcs/{plcId}/activity` with the `id` pinned to the auto-allocated ref and `serverTimestamp()` `createdAt` and no target keys, full and partial optional-target passthrough, no `logError` on success, and the fire-and-forget failure path — resolves to `undefined` without throwing while `logError('writePlcActivityEvent', err, { plcId, type })` fires). Built on stubbed `firebase/firestore` (collection/doc/serverTimestamp/setDoc), `@/config/firebase` db, and `@/utils/logError`, with the REAL pure `@/utils/plc` `tsToMillis` left unmocked so the timestamp branches run faithfully. `pnpm exec vitest run tests/utils/plcActivity.test.ts` → 27 passing; `pnpm run type-check`, `pnpm exec eslint --max-warnings 0`, and `pnpm exec prettier --check` all clean. NOTE: the other genuinely-untested utils/ files formerly listed in this item's File line (imageProcessing, guidedLearningDriveService, pexelsService, previewMode, classroomCourses) remain tracked under the separate open MEDIUM "utils/ coverage — untested utility files" item — no coverage tracking is lost by retiring this priority-scoped item. (`shortLinksApi.ts` and `lastActiveThrottle.ts`, also on the old File line, were already covered — `tests/utils/shortLinksApi.test.ts` 2026-07-27 and `tests/utils/lastActiveThrottle.test.ts` 2026-07-22 — so they are not gaps.)
- **Progress (2026-08-03, action):** `spotifyAuth.ts` covered by `tests/utils/spotifyAuth.test.ts` (37 tests) — Priority 1 done.
- **Progress (2026-07-13, action):** `quizAudio.ts` covered by `tests/utils/quizAudio.test.ts` (13 tests) — Priority 2 done.

### HIGH Security-critical and stateful utils/hooks with zero test coverage

- **Detected:** 2026-07-22
- **Completed:** 2026-07-29
- **Resolution (2026-07-29, action):** Closed the final sub-target — `functions/src/studentIdentity.ts` (the 5 student-identity Cloud Functions) — by adding `functions/src/studentIdentity.test.ts` (48 tests). The earlier progress notes (below) had already covered the other 7 targets across the 2026-07-22 and 2026-07-27 actions; this action retires the item. Coverage by function: **studentLoginV1** (11 tests) — missing idToken → invalid-argument, unset secret → internal, unverifiable / unverified-email / malformed-email token → unauthenticated, unregistered domain → permission-denied with NO custom token minted, testClasses bypass mints a `test:{email}` HMAC uid + `classIds` from the doc ids with zero OneRoster calls, OneRoster happy path mints `computeStudentUid(sourcedId)` with the exact `{studentRole, orgId, classIds}` claim shape, hd-claim domain preferred over the email suffix for the org gate, no-student-in-roster → not-found, quote-in-email OneRoster-filter guard → not-found before any axios call, ClassLink network failure → internal, createCustomToken failure → internal; **getAssignmentPseudonymV1** (6 tests) — unauth / non-student / missing / over-200-char assignmentId / unset-secret guards, plus the deterministic `HMAC("asn:"+uid+":"+assignmentId)` pseudonym (stable within a tuple, unlinkable across assignments); **getStudentClassDirectoryV1** (8 tests) — auth / role / non-array-classIds / empty-classIds guards, ClassLink roster → directory entry with resolved teacher name, testClasses fallback for unresolved ids, unresolved ids dropped, Auth-lookup failure → empty teacher name with the row still rendered; **commitRosterPinIndexV1** (9 tests) — unauth / student-role-forbidden / missing-rosterId / non-array-entries / over-max / roster-not-found guards, local-roster no-op with `skippedReason:'no-classlink-class-id'`, desired-index write + stale-entry prune carrying the `computeStudentUid` pseudonym payload, malformed-entry skip counting; **pinLoginV1** (14 tests) — missing kind/pin, quiz-without-code, VA-without-sessionId, the five `matched:false` fallback reasons (no-joinable-session / session-not-found / session-missing-teacher / no-rosters-on-session / no-index-entry), quiz + video-activity happy paths minting the SSO-matching `{studentRole, orgId, classIds:[classId]}` token, and createCustomToken failure → internal. The harness is a path-addressed in-memory Firestore mock (supports `collection`/`collectionGroup`/`doc`/`where` with `==`/`in`/`array-contains`/`limit`/`batch`, and `ref.parent.parent.id` reconstruction from the doc path), with stubbed `google-auth-library`, `axios`, `./secrets`, and `./functionsInit`, while the REAL `./classlinkShared` is left unmocked so every uid/pseudonym assertion exercises the production HMAC derivation. Full functions suite green at 40 files / 775 tests (48 new); `pnpm exec tsc --noEmit`, `node ../node_modules/eslint/bin/eslint.js src/studentIdentity.test.ts --max-warnings 0`, and `prettier --check` all clean. No HIGH items remain in this journal.
- **Progress (2026-07-22, action):** Added coverage for 5 of the 8 listed targets — Priorities 1, 2, 3, and 5. `tests/hooks/useStudentIdleTimeout.test.ts` (9 tests — 15-min idle sign-out, clear-first-name-before-redirect ordering, interaction reset, 5s interaction throttle, disabled teardown, unmount cleanup, custom redirect path, rejected-signOut swallow; uses `vi.useFakeTimers()` + `renderHook` and mocks `firebase/auth`/`config/firebase`/`StudentAuthContextValue`). `tests/utils/quizSyncMigration.test.ts` (14 tests — all 4 migration branches incl. version-0 numeric guard, malformed-sync strip, legacy-fold, sub-object-wins-over-legacy, null/undefined input). `tests/utils/snapshotThrottle.test.ts` (11 tests — leading-apply-immediately, latest-value buffering, trailing flush, `flush()`/`cancel()` teardown paths, falsy-value hasPending guard). `tests/utils/globalPermissionsErrors.test.ts` (6 tests — dispatch, once-per-session latch, no-latch-without-handler, null-clear, `__resetForTests`). `tests/utils/lastActiveThrottle.test.ts` (16 tests — window boundary, fail-open on null/throwing/unparseable storage, key namespacing, round-trip, ambient-localStorage fallback). All 56 tests pass; `pnpm run type-check`, `pnpm exec eslint --max-warnings 0`, and `pnpm exec prettier --check` all clean.
- **Progress (2026-07-27, action):** Covered 2 of the 3 remaining targets. `tests/utils/shortLinksApi.test.ts` (9 tests) covers `resolveShortLink` (null on missing, data on hit, correct `short_links/{code}` ref, read-error propagation) and `recordShortLinkClick` (atomic `increment(1)` + `lastClickedAt` payload, correct ref, write-error propagation). `hooks/useShortLinks.test.ts` (23 tests) covers `useCreateShortLink` (signed-out & non-admin fast-fail, destination/slug validation, taken-slug collision message, full-metadata create, empty-label omission, the 5-retry random-code collision loop, retry-budget exhaustion, generic error + `logError`) and `useShortLinks` (non-admin no-subscribe, snapshot hydrate/error/unsubscribe, `updateShortLink` non-admin/missing/invalid/success/blank-label-`deleteField`/error, `deleteShortLink` non-admin-throw/admin-delete, `refresh` no-op/hydrate/error). Real transaction body exercised via a controllable `runTransaction` mock; pure `shortLinkValidation` helpers used unmocked. 32 tests pass; tsc/eslint/prettier clean.
- **Detail (original targets):**
  - `utils/globalPermissionsErrors.ts` — module-level singleton handler latch with `__resetForTests`; registration ordering, single-fire semantics, latch reset. (covered 2026-07-22)
  - `utils/lastActiveThrottle.ts` — stateful throttle guard over an injectable storage abstraction; stable/unstable key disambiguation. (covered 2026-07-22)
  - `utils/quizSyncMigration.ts` — pure data transformer with 4 migration branches. (covered 2026-07-22)
  - `utils/snapshotThrottle.ts` — leading/trailing throttle with flush-on-teardown. (covered 2026-07-22)
  - `hooks/useStudentIdleTimeout.ts` — security-critical idle auto-signout on shared Chromebooks. (covered 2026-07-22)
  - `utils/shortLinksApi.ts` — public no-auth short-link creation with collision detection. (covered 2026-07-27)
  - `hooks/useShortLinks.ts` — 5-retry slug collision-avoidance loop. (covered 2026-07-27)
  - `functions/src/studentIdentity.ts` — the 5 student-identity auth Cloud Functions. (covered 2026-07-29)

### HIGH hooks/ coverage — session hooks now fully covered

- **Detected:** 2026-04-13
- **Completed:** 2026-07-06
- **Resolution (2026-07-06, action):** Added `tests/hooks/useRosters.test.ts` (35 tests) covering `hooks/useRosters.ts` — the last remaining untested Priority-3 session hook (roster metadata Firestore listener + Drive-backed student PII sync). Tests verify: **subscription** — no listener without a user, `collection(users/{uid}/rosters)` + `orderBy('name')` wiring, unmount unsubscribe, `validateRosterMeta` dropping name-less docs, Drive-student merge with `assignPins` zero-padding, per-roster cache success/no-cache-on-failure (retry on next snapshot), non-array Drive payload treated as a load failure, `loadError` when Drive is unavailable, cache invalidation on `driveFileId` change vs. cache reuse when unchanged, and the `failed-precondition` fallback to an unordered listener; **addRoster** — no-user throw, metadata-only Firestore write (no `students` PII) + Drive upload + `driveFileId` back-fill, ClassLink provenance pass-through, resolve-anyway on Drive upload failure, no upload for an empty roster, and the `commitRosterPinIndexV1` pin-index sidecar sync (only for students with a ClassLink id + PIN, skipped for local rosters); **updateRoster** — no-user no-op, in-place Drive `updateFileContent` + count patch + optimistic state, optimistic revert + throw on Drive failure, Firestore-count-only when Drive is unavailable, and metadata-only writes; **setAbsentStudents** — no-user no-op, optimistic set + persist, revert + rethrow on write failure; **deleteRoster/setActiveRoster** — no-user no-op, Drive-file + Firestore-doc delete with active-roster clear (and untouched when a different roster is deleted), localStorage hydration + clear; **PII migration** — one-time move of legacy `students[]` from Firestore into Drive with `deleteField()` + persisted per-user flag, and no re-run once the flag is set. Mocks `firebase/firestore`, `firebase/functions` (`httpsCallable`), `@/config/firebase` (`isAuthBypass:false`), and `@/hooks/useGoogleDrive` (a swappable mock `driveService`) per the `useMiniAppSession.test.ts` / `useLtiSessionNames.test.ts` reference patterns; keeps the real `mapWithConcurrency` and `getLocalIsoDate`. `pnpm exec vitest run tests/hooks/useRosters.test.ts` → 35 passing; `pnpm exec tsc --noEmit`, `pnpm exec eslint tests/hooks/useRosters.test.ts --max-warnings 0`, and `pnpm exec prettier --check tests/hooks/useRosters.test.ts` all clean. This closes the HIGH hooks/ session-coverage item — all named Priority-3 session hooks (`useVideoActivitySession`, `useMiniAppSession`, `useScreenRecord`, `useRosters`, plus the earlier `useSubstituteShares` / `usePresetSubEmails` / `useDragScroll` / `useTemplateStore` / `useStarterPacks`) now have coverage.
- **Progress (2026-07-01, action):** Added `tests/hooks/useScreenRecord.test.ts` (15 tests) covering `hooks/useScreenRecord.ts` — the `getDisplayMedia` + `MediaRecorder` screen-recording lifecycle hook (no Firestore; pure browser-media wiring over refs, one `setInterval` duration timer, and an unmount cleanup effect). Tests verify: **idle state** — `isRecording:false`, `duration:0`, `error:null` on mount; **start** — `getDisplayMedia` requested exactly once with `{video:{displaySurface:'browser'}, audio:true}`, `recorder.start()` called, `isRecording` flips true, error cleared; **mime negotiation** — prefers `'video/webm;codecs=vp9,opus'` when `MediaRecorder.isTypeSupported` returns true and falls back to plain `'video/webm'` when it returns false; **duration timer** — increments once per second under fake timers (`advanceTimersByTime(3000)` → `duration:3`) and stops ticking after `stopRecording` (stays `0` across a further `advanceTimersByTime(5000)`); **chunk assembly** — `ondataavailable` keeps non-empty chunks and drops zero-size ones, and `onstop` assembles a `Blob` of the recorder's mime type (asserts `blob.size` sums only the kept chunks) delivered through `onSuccess`; **stop/reset** — `stopRecording` resets `isRecording:false`/`duration:0` and stops every stream track (video + audio); **active-guard** — `recorder.stop()` is invoked only while state is not `'inactive'` (a second stop is a no-op) and `stopRecording` before any start does not throw; **browser-UI end** — the video track's `onended` handler drives `stopRecording`; **error paths** — a `getDisplayMedia` rejection surfaces through `error` state + `onError(err)` + `console.error('Screen recording failed:', err)` with `isRecording` staying false, a non-`Error` rejection is wrapped as `new Error('Failed to start recording')`, and a prior error is cleared when a new recording starts; **cleanup** — unmount stops the active stream's tracks. Uses controllable `FakeMediaRecorder`/`FakeMediaStream` fakes stubbed via `vi.stubGlobal('MediaRecorder', …)` + `Object.defineProperty(navigator,'mediaDevices',…)`, jsdom's native `Blob` for real `.size`/`.type`, and `vi.useFakeTimers()` for the duration interval; `console.error` spied with `mockImplementation(() => undefined)` per the `useActivityWallLibrary.test.ts` reference. `pnpm exec vitest run tests/hooks/useScreenRecord.test.ts` → 15 passing; `pnpm exec tsc --noEmit`, `pnpm exec eslint tests/hooks/useScreenRecord.test.ts --max-warnings 0`, and `pnpm exec prettier --check tests/hooks/useScreenRecord.test.ts` all clean. Remaining untested session hook from this item (Priority 3): `useRosters.ts`.
- **Progress (2026-06-29, action):** Added `tests/hooks/useDragScroll.test.ts` (22 tests) covering `hooks/useDragScroll.ts` — the pointer-drag-to-scroll gesture hook used on horizontal/vertical scroll containers (no React state, no Firestore; pure DOM event wiring over a ref). Tests verify, across both axes: **commit + scroll** — a primary-axis drag sets `scrollLeft`/`scrollTop` to `startScroll - delta` and keeps following subsequent moves measured from the original start (not incrementally), and the default axis is `x` when none is supplied; **direction disambiguation** — a move below `minDistance` (default 8px, plus a custom `minDistance:20` case) stays undecided with no scroll and no pointer capture, a secondary-axis-dominant move enters `passthrough` and ignores all later moves (hand-off to collapse/dnd-kit), and a perfectly diagonal drag (`adx === ady`) commits to scroll via the `>=` tie-break; **pointer-capture lifecycle** — `setPointerCapture(pointerId)` is called on commit and `releasePointerCapture(pointerId)` on `pointerup` after which the gesture resets (further moves ignored), a thrown `setPointerCapture` is swallowed and scrolling still works, and `pointercancel` plus a stray window-level `pointerup` both reset the gesture; **event suppression** — `preventDefault`/`stopPropagation` are called on a scrolling move but NOT on an undecided below-threshold move; **guards** — a non-primary mouse button (`button:1`) is ignored, a second pointer is ignored while a gesture is active (original pointer's start preserved), a `pointermove` with a non-matching `pointerId` is ignored, and `disabled:true` attaches no listeners; **cleanup** — unmount removes listeners (post-unmount events no-op) and a `disabled:true → false` rerender re-attaches and resumes scrolling. Drives synthetic `PointerEvent`s via the jsdom `PointerEvent` mock from `tests/setup.ts`, assigning `button` explicitly (the mock doesn't carry it) and asserting against jsdom's settable `scrollLeft`/`scrollTop`; per-element `vi.fn()` mocks for `setPointerCapture`/`releasePointerCapture` avoid the shared `Element.prototype` mock that accumulates calls across tests. `pnpm exec vitest run tests/hooks/useDragScroll.test.ts` → 22 passing; `pnpm exec tsc --noEmit`, `pnpm exec eslint tests/hooks/useDragScroll.test.ts --max-warnings 0`, and `pnpm exec prettier --check tests/hooks/useDragScroll.test.ts` all clean. Remaining untested session hooks from this item (Priority 3): `useRosters.ts`, `useScreenRecord.ts`.
- **Progress (2026-06-22, action):** Added `tests/hooks/useTemplateStore.test.ts` (15 tests) covering the `mockTemplateStore` singleton in `hooks/useTemplateStore.ts` — the in-memory + `sessionStorage`-backed mock for `/dashboard_templates/` used in auth-bypass / E2E mode (not a React hook despite living in `hooks/`; a module-level singleton with a synchronous `save`/`remove`/`getAll` surface). Tests verify: **`getAll`** — empty store returns `[]`, returns saved templates, sorts by `createdAt` **descending** (inserted out of order to prove sort beats insertion order, matching the production `orderBy('createdAt','desc')`), and returns Board (`DashboardTemplate`) + Collection (`CollectionTemplate`) entries together in the union; **`save`** — upserts by id (same id replaces, no duplicate) and persists the array to `sessionStorage` under `mock_dashboard_templates`; **`remove`** — deletes by id, is a no-op for a missing id, and persists the removal; **hydration** — pre-existing `sessionStorage` entries are hydrated + sorted on first access, a newly saved template merges with hydrated ones, and the one-shot `hydrated` guard means a later external `sessionStorage` edit is ignored (no re-hydrate); **error resilience** — a throwing `getItem` falls back to in-memory (still empty, later writes work), malformed JSON in storage is swallowed (falls back to empty), and a throwing `setItem` (quota) keeps the in-memory value since the backing `Map` is written before `persist()`. Each test re-imports the module via `vi.resetModules()` + dynamic `import()` to get a fresh un-hydrated singleton; `sessionStorage.clear()` in `beforeEach`/`afterEach` and `Storage.prototype.getItem`/`setItem` spied (then restored) for the error paths. No Firebase mocking needed — the store is pure logic over `sessionStorage`. `pnpm exec vitest run tests/hooks/useTemplateStore.test.ts` → 15 passing; `pnpm exec tsc --noEmit`, `pnpm exec eslint tests/hooks/useTemplateStore.test.ts --max-warnings 0`, and `pnpm exec prettier --check tests/hooks/useTemplateStore.test.ts` all clean. Remaining untested session hooks from this item (Priority 3): `useRosters.ts`, `useScreenRecord.ts`, `useDragScroll.ts`.
- **Progress (2026-06-17, action):** Added `tests/hooks/usePresetSubEmails.test.ts` (11 tests) covering `hooks/usePresetSubEmails.ts` — the `/preset_sub_emails/{buildingId}` single-doc listener feeding the ShareLinkCreatorModal sub-email picker. Tests verify: **listener wiring** — targets `doc(db,'preset_sub_emails',canonical)` with loading-true until the first snapshot, canonicalizes a legacy building ID (`'orono-high-school'` → `'high'` via the real `canonicalBuildingId`/`config/buildings`, not mocked, so the alias path is verified end-to-end), and the empty-`buildingId` short-circuit (no listener, `{emails:[], loading:false}`); **snapshot mapping** — exposes the `emails` array + clears loading on first snapshot, resolves a missing doc (`snap.data()===undefined`) to an empty list, resolves a non-array `emails` field to an empty list, and filters non-string entries (numbers/null/undefined/objects) out of the array; **error handling** — `console.error('[usePresetSubEmails] snapshot error:', err)` logged and state resolved to `{emails:[], loading:false}`; **building change & cleanup** — building change tears down the prior listener and re-enters loading because the stale snapshot building no longer matches the new canonical (asserts `mockDoc` last-called with `'middle'` and a second listener registered), a legacy alias that canonicalizes to the same building keeps the cached snapshot resolved (no spurious loading flash), and unmount cleanup fires the unsub. Mocks `firebase/firestore` (`doc`/`onSnapshot`) and `@/config/firebase` (`db`) per the `useSubstituteShares.test.ts` reference pattern; an `onSnapshot` handler-capture array lets each test drive the success/error callback of a specific subscription and assert its `unsub` teardown. `pnpm exec vitest run tests/hooks/usePresetSubEmails.test.ts` → 11 passing; `pnpm exec tsc --noEmit`, `pnpm exec eslint tests/hooks/usePresetSubEmails.test.ts --max-warnings 0`, and `pnpm exec prettier --check tests/hooks/usePresetSubEmails.test.ts` all clean. Remaining untested session hooks from this item (Priority 3): `useRosters.ts`, `useScreenRecord.ts`, `useDragScroll.ts`, `useTemplateStore.ts` (the latter is a sessionStorage-backed mock singleton, not a Firestore listener).
- **Progress (2026-06-15, action):** Added `tests/hooks/useSubstituteShares.test.ts` (20 tests) covering both read hooks in `hooks/useSubstituteShares.ts` — the named Priority-3 session hook `useSubstituteShares` plus its single-doc sibling `useSubstituteShare` (the `/subs` portal listeners). Tests verify: **`useSubstituteShares(buildingId)`** — listener wiring (`collection(db,'shared_boards')` scoped by `where('intendedMode','==','substitute')` + `where('buildingId','==',canonical)`, loading-true until the first snapshot), legacy-ID canonicalization (`'orono-high-school'` → `'high'` via the real `canonicalBuildingId`), the empty-`buildingId` short-circuit (no listener, not-loading), snapshot mapping carrying `shareId` from `doc.id`, the client-side expiry filter (`expiresAt <= now` dropped — including the exact-`now` boundary and missing/non-numeric `expiresAt` treated as `0`/expired), `sharedAt`-descending sort (missing `sharedAt` treated as `0` → last), error handling (`permission-denied`→friendly message + `logError('useSubstituteShares.snapshot', …, {buildingId})`, unknown code→generic fallback, shares cleared + not-loading), building-change re-subscribe (prior listener torn down, stale-snapshot guard re-enters loading, new `where('buildingId',…,'middle')`), and unmount cleanup; **`useSubstituteShare(shareId)`** — null-`shareId` short-circuit (no listener), `doc(db,'shared_boards',shareId)` target + loading-true, the not-found path (`'Share not found'`), the wrong-mode rejection (`intendedMode !== 'substitute'` → `'Not a substitute share'`), valid-doc mapping carrying `shareId` from `snap.id`, listener-error friendly mapping (`unavailable` → connection message + `logError('useSubstituteShare.snapshot', …, {shareId})`), shareId-change re-subscribe re-entering loading, and unmount cleanup. Mocks `firebase/firestore` (`collection`/`doc`/`onSnapshot`/`query`/`where` — `where` returns marker objects so constraints are assertable), `@/config/firebase` (`db`), and `@/utils/logError`, per the `useActivityWallLibrary.test.ts`/`usePlcNotes.test.ts` reference patterns; an `onSnapshot` handler-capture array lets each test drive the success/error callback of a specific subscription and assert its `unsub` teardown, and the real `canonicalBuildingId` loads so the alias path is verified end-to-end. `pnpm exec vitest run tests/hooks/useSubstituteShares.test.ts` → 20 passing; full `tests/hooks/` suite green at 60 files / 668 tests; `pnpm exec tsc --noEmit`, `pnpm exec eslint tests/hooks/useSubstituteShares.test.ts --max-warnings 0`, and `pnpm exec prettier --check tests/hooks/useSubstituteShares.test.ts` all clean. Also corrected: `useReconcileExpiredSubShares.ts` already has a colocated `hooks/useReconcileExpiredSubShares.test.ts` — it is NOT a gap despite being listed; removing it from the remaining list. Remaining untested session hooks from this item (Priority 3): `useRosters.ts`, `useScreenRecord.ts`, plus `useDragScroll.ts`, `usePresetSubEmails.ts`, `useTemplateStore.ts`.
- **Progress (2026-06-17, audit):** Test suite: 473 files / 4,937 tests all passing (up from 422/4,343 on 2026-06-10 — 51 new test files, 594 new tests). No failing tests. New test files added since 2026-06-10: `tests/hooks/useSubstituteShares.test.ts` (covers `useSubstituteShares` + `useSubstituteShare`) — the two remaining substitute-portal session hooks from this item are now covered. Audit agent also flagged `utils/ai_security.ts` and `utils/rosterRestrictions.ts` as potential gaps — both already have colocated test files (`tests/utils/ai_security.test.ts`, `tests/utils/rosterRestrictions.test.ts`); not gaps. Updated remaining untested hooks list: `useRosters.ts`, `useScreenRecord.ts`, `useDragScroll.ts`, `usePresetSubEmails.ts`, `useTemplateStore.ts`.
- **Progress (2026-06-10, audit):** Test suite: 422 files / 4,343 tests all passing (up from 411/4,205 on 2026-06-08). No failing tests. New test files added since 2026-06-08: `tests/utils/mapWithConcurrency.test.ts`, `tests/utils/videoActivitySessionNormalize.test.ts`, `tests/utils/reducedMotion.test.ts`. New utility files added since 2026-06-08 WITHOUT tests: `utils/firestorePaging.ts` (52 lines — page-cursor loop used in quiz/VA/GL score-publish paths; the `pageSize >= 1` guard and loop-termination logic are correctness-critical), `utils/snapshotThrottle.ts` (106 lines — leading-edge + trailing-flush throttle for live session listeners; flush-on-teardown state management edge cases are untested). `utils/mapWithConcurrency.ts` has a test (`tests/utils/mapWithConcurrency.test.ts`) — no gap. Existing session-hook and utils-file gaps unchanged from 2026-06-08 audit. Added two new files to the utils coverage tracking below.
- **Progress (2026-06-10, action):** Added `tests/hooks/useVideoActivitySession.test.ts` (27 tests) covering `useVideoActivitySessionTeacher` — the named Priority-3 session hook that mirrors the 2026-06-08 mini-app work. Tests verify: `createSession` (writes the full `VideoActivitySession` payload to `video_activity_sessions/{sessionId}` and returns the `crypto.randomUUID()` id, trims the assignment name, falls back to a title-prefixed `${activityTitle} ${date}` name when blank, merges partial `settings` over the `{autoPlay:false, requireCorrectAnswer:true, allowSkipping:false}` defaults, includes `classIds` AND mirrors `classId=classIds[0]` when present, includes `periodNames`/`rosterIds`/`classPeriodByClassId`/`sessionOptions` when present, omits ALL optional targeting fields when none provided, and passes the `mode` through — note VA's `createSession` does NOT filter empty entries out of `classIds`/`rosterIds`, unlike the mini-app hook, so the suite asserts pass-through not cleaning); `subscribeToActivitySessions` (sets `sessionsLoading`, wires the query with `where('activityId','==',…)` + `where('teacherUid','==',…)` + `orderBy('createdAt','desc')` against `video_activity_sessions`, maps docs through the real `normalizeVideoActivitySession` filling `allowedPins`/`questions` defaults, uses the Firestore `doc.id` over a stale `id`, logs + clears sessions/loading on error via `'[useVideoActivitySessionTeacher] Activity session list error:'`, and tears down a prior listener on re-subscribe); `unsubscribeFromActivitySessions` (unsub + reset); `renameSession` (`updateDoc` trimmed name); `endSession` (`updateDoc` with `status:'ended'` + `endedAt` + `expiresAt`, both `Date.now()`); `subscribeToSession` (arms BOTH a responses-subcollection listener and a session-doc listener, sets loading + clears error, maps responses carrying `_responseKey` from the snapshot doc id, clears loading on first snapshot via the real `createLeadingTrailingThrottle` leading edge, sets `error` to the message on a responses-listener failure, mirrors the session doc into `liveSession`/clears it when the doc is gone, sets `error` on a session-doc failure, and clears stale `responses`/`liveSession` + tears down both prior listeners on re-subscribe); `unsubscribeFromSession` (tears down both listeners, resets `responses`/`liveSession`/`error`); `unlockStudentAttempt` (throws when the response doc is missing with no `batch.commit`, refunds one attempt on both the response doc — `completedAt:null, score:null, completedAttempts:max(0,n-1), unlocked:true, unlockedAt` — AND the `video_activity_attempt_ledger/${sessionId}__${studentUid}` doc when the ledger exists, and updates only the response doc when no ledger entry exists); and unmount cleanup (all three listeners' unsubs fire). Mocks `firebase/firestore` (full set incl. `writeBatch`/`getDoc`/`arrayUnion`/`increment`/`runTransaction`), `firebase/auth`, `firebase/functions`, and `@/config/firebase` (`db`/`auth`/`functions`) per the `useMiniAppSession.test.ts` reference pattern; lets the real `normalizeVideoActivitySession` and `snapshotThrottle` load so wiring is verified end-to-end, while `useQuizSession` (re-exported helpers) loads transitively without issue since it shares the same mocked firebase deps. `pnpm exec vitest run tests/hooks/useVideoActivitySession.test.ts` → 27 passing; full `tests/hooks/` suite green at 59 files / 641 tests; `pnpm exec tsc --noEmit`, `pnpm exec eslint … --max-warnings 0`, and `pnpm exec prettier --check …` all clean. Also noted: `useFirestore.ts` already has `hooks/useFirestore.test.ts` (colocated) — it is NOT a gap despite being listed; removing it from the remaining list below. Remaining untested session hooks from this item (Priority 3): `useRosters.ts`, `useScreenRecord.ts`, plus `useDragScroll.ts`, `usePresetSubEmails.ts`, `useReconcileExpiredSubShares.ts`, `useSubstituteShares.ts`, `useTemplateStore.ts`.
- **Progress (2026-06-08, action):** Added `tests/hooks/useMiniAppSession.test.ts` (21 tests) covering `useMiniAppSessionTeacher` — one of the named Priority-3 session hooks. Tests verify: `createSession` (writes the full `MiniAppSession` payload to `mini_app_sessions/{sessionId}` and returns the `crypto.randomUUID()` id, trims the assignment name, falls back to a `${title} — ${date}` name when blank, defaults `mode` to `'submissions'` and derives `submissionsEnabled=true`, derives `submissionsEnabled=false` for `'view-only'`, cleans/keeps non-empty `classIds`/`rosterIds` and omits both keys entirely when nothing survives cleaning); `subscribeToAppSessions` (sets loading, wires the query with `where('appId','==',…)` + `where('teacherUid','==',…)` + `orderBy('createdAt','desc')` against the `mini_app_sessions` collection, maps docs through `normalizeSession` and clears loading, logs + clears sessions/loading on listener error via the `'[useMiniAppSessionTeacher] Session list error:'` message, and tears down a prior listener when re-subscribing); `normalizeSession` (sparse-doc defaults incl. the `'Mini App'` title and derived fallback name, unknown `status` coerced to `'active'` while `'ended'`+`endedAt` are preserved, `endedAt` kept only when numeric, `classIds`/`rosterIds` filtered to non-empty strings and omitted when empty or non-array, `submissionsEnabled` kept only when strictly `true`, `mode` kept only for the two valid literals, and the Firestore `doc.id` overriding any stale `id` in the data); `unsubscribeFromAppSessions` (calls the active unsub and resets state); `renameSession` (`updateDoc` with the trimmed name at the session path); `endSession` (`updateDoc` with `status:'ended'` + `endedAt: Date.now()`); and unmount cleanup (active listener unsub fires). Mocks `firebase/firestore` (`collection`/`doc`/`onSnapshot`/`setDoc`/`updateDoc`/`query`/`where`/`orderBy`) and `@/config/firebase` (`db`) per the `useActivityWallLibrary.test.ts` reference pattern; the `where`/`orderBy` mocks return marker objects so query constraints are assertable, and an onSnapshot handler-capture lets each test drive the success/error callback independently. Note the `createSession` blank-name fallback uses `new Date().toLocaleString()` (live clock, not `Date.now()`), so that test asserts the `^Quiz Maker — ` prefix rather than an exact timestamp. `pnpm exec vitest run tests/hooks/useMiniAppSession.test.ts` → 21 passing; full `tests/hooks/` suite green at 56 files / 595 tests; `pnpm exec tsc --noEmit`, `pnpm exec eslint tests/hooks/useMiniAppSession.test.ts --max-warnings 0`, and `pnpm exec prettier --check tests/hooks/useMiniAppSession.test.ts` all clean. Remaining untested session hooks from this item (Priority 3): `useVideoActivitySession.ts`, `useRosters.ts`, `useFirestore.ts`, `useScreenRecord.ts`, plus `useDragScroll.ts`, `usePresetSubEmails.ts`, `useReconcileExpiredSubShares.ts`, `useSubstituteShares.ts`, `useTemplateStore.ts`.
- **Progress (2026-06-01, action):** Added `tests/hooks/useStarterPacks.test.ts` (14 tests) covering one of the remaining Priority-3 untested hooks, `useStarterPacks.ts`. Tests verify: listener wiring (subscribes to the public collection at `artifacts/{appId}/public/data/starterPacks` and the per-user collection at `artifacts/{appId}/users/{uid}/starterPacks`, subscribes to public only when `userId` is undefined, starts in a loading state, unsubscribes both listeners on unmount); snapshot mapping (maps both public and user docs with the Firestore `doc.id` overriding any stale `id` in the data, clears loading only after BOTH the public and user snapshots arrive, clears loading after the public snapshot alone when there is no userId); listener-error handling (logs `'Failed to subscribe to public starter packs:'` / `'Failed to subscribe to user starter packs:'` and still counts each errored listener as resolved so `loading` clears); `executePack` (calls `deleteAllWidgets` once before adding when `cleanSlate` is true and skips it when false, calls `addWidget` once per pack widget with the correct widget `type`, assigns each added widget a fresh `crypto.randomUUID()` id rather than reusing the seed id, deep-clones each widget config via `structuredClone` so the original is not mutated, and is an addWidget no-op for an empty-widget pack while still honoring `cleanSlate`); and the `userId`→undefined transition (clears `userPacks` and returns to `loading` via the adjusting-state-while-rendering branch). The `appId` used in path assertions is recomputed in the test with the same `import.meta.env` expression the hook uses, so the assertions hold regardless of injected env. Mocks `firebase/firestore` (`collection`/`query`/`onSnapshot`) and `@/config/firebase` (`db`, `isAuthBypass: false`) per the `useActivityWallLibrary.test.ts` reference pattern; the fake snapshot exposes a `forEach` because the hook iterates via `snapshot.forEach`, and an onSnapshot handler-capture array lets each test drive the public vs. user success/error callbacks independently. `pnpm exec vitest run tests/hooks/useStarterPacks.test.ts` → 14 passing; full `tests/hooks/` suite green at 51 files / 549 tests; `pnpm exec tsc --noEmit`, `pnpm exec eslint tests/hooks/useStarterPacks.test.ts --max-warnings 0`, and `pnpm exec prettier --check tests/hooks/useStarterPacks.test.ts` all clean. Remaining untested hooks from this item (Priority 3): `useVideoActivitySession.ts`, `useMiniAppSession.ts`, `useRosters.ts`, `useFirestore.ts`, `useScreenRecord.ts`, plus `useDragScroll.ts`, `usePresetSubEmails.ts`, `useReconcileExpiredSubShares.ts`, `useSubstituteShares.ts`, `useTemplateStore.ts`.
- **Progress (2026-05-25, action):** Expanded `tests/hooks/useSharedCollection.test.ts` from 4 → 12 tests, covering the Priority-1 target (`useSharedCollection` integration paths). New tests verify: `shareCollection` writes both the parent doc and board sub-docs (path assertions) and `shareSubstituteCollection` carries `intendedMode='substitute'` + `expiresAt` + `buildingId` (existing happy paths, expanded with path assertions); `shareCollection` partial-write recovery — when the boards `writeBatch.commit()` rejects after the parent doc has landed, `commitBoardBatches` re-throws with the descriptive `"Failed to upload all boards (N of M committed)"` message and best-effort `deleteDoc`s the parent (verified via `deletedPaths` tracker on the firestore mock); `loadSharedCollection` discriminated-result paths — `not-found` when the parent doc is missing, `ok=true` for a valid copy share and for a non-expired substitute share, `expired` when `expiresAt < Date.now()` on a substitute share (pre-existing), `unauthorized` when the SDK rejects with `{ code: 'permission-denied' }`, and `error` on a generic rejection; `loadSharedCollectionBoards` integration — returns boards in `boardIds[]` order (pre-existing), filters out missing board ids so partial subcollections don't crash the recipient, and returns `[]` when `getDocs` itself rejects (logged via the mocked `logError`). Mocks the firestore SDK with a small in-memory doc store plus `failNext*` injection helpers (`failNextGetDoc`, `failNextGetDocs`) so each test can drive its failure path without touching the others; the `shareCollection` partial-write test drives the writeBatch path with `mockImplementationOnce` instead. The `shareCollection` cleanup test uses `writeBatch.mockImplementationOnce` twice to let the parent commit succeed and the board commit reject — vitest's `mockImplementationOnce` queue ensures the override doesn't leak to subsequent tests. `pnpm exec vitest run tests/hooks/useSharedCollection.test.ts` → 12 passing; `pnpm exec tsc --noEmit` clean; `pnpm exec eslint tests/hooks/useSharedCollection.test.ts --max-warnings 0` clean (one `prefer-promise-reject-errors` rule disabled on two lines that intentionally reject with the non-Error `{ code: 'permission-denied' }` shape the Firestore SDK actually produces, documented inline); `pnpm exec prettier --check tests/hooks/useSharedCollection.test.ts` clean. Per the journal note, `importSharedCollection` rollback was already covered in `tests/DashboardContext_importSharedCollection.test.tsx`, so Priority 1 is now fully closed. Remaining untested hooks from this item: `useDragScroll.ts`, `usePresetSubEmails.ts`, `useReconcileExpiredSubShares.ts`, `useSubstituteShares.ts`, `useTemplateStore.ts`, plus the core session hooks `useVideoActivitySession.ts`, `useMiniAppSession.ts`, `useRosters.ts`, `useFirestore.ts`, `useStarterPacks.ts`, `useScreenRecord.ts`.
- **Progress (2026-05-20, action):** Added `tests/hooks/useActivityWallLibrary.test.ts` (13 tests) covering the Priority-2 target. Tests verify: listener wiring (orders by `updatedAt desc`, targets `users/{uid}/activity_wall_activities`, skips listener + not-loading when `userId` is undefined, loading-true while a uid is present), snapshot mapping (full doc passthrough, sparse-doc defaults falling back to the Firestore doc id / empty strings / `'text'` / `'anonymous'` / `0` timestamps, `classId` included only for non-empty strings, error path setting `'Failed to load activities'` + clearing loading), `saveActivity` (full payload to the entry-id doc path, `classId` included when present, empty `classId` stripped per the student-class-gate rule, throws + no write when signed out), `deleteActivity` (deletes the activity-id path, throws + no delete when signed out), and the userId→undefined transition clearing activities + loading. Mocks `firebase/firestore` + `@/config/firebase` per the `usePlcNotes.test.ts` reference pattern. `pnpm exec vitest run` (file) → 13 passing; full suite green at 291 files / 2862 tests; `pnpm type-check`, `pnpm exec eslint --max-warnings 0`, and `pnpm exec prettier --check` on the new file all clean. Remaining untested hooks from this item: `useDragScroll.ts`, `usePresetSubEmails.ts`, `useReconcileExpiredSubShares.ts`, `useSubstituteShares.ts`, `useTemplateStore.ts`, plus the core session hooks `useVideoActivitySession.ts`, `useMiniAppSession.ts`, `useRosters.ts`, `useFirestore.ts`, `useStarterPacks.ts`, `useScreenRecord.ts`.
- **Progress (2026-06-08, audit):** Test suite: 411 files / 4205 tests all passing (up from 379/3880 on 2026-06-01). No failing tests. New test files added since 2026-06-01: `components/widgets/QuizWidget/utils/quizScoreboard.test.ts`, `components/widgets/math-tools/CalculatorTool.test.tsx`, `functions/src/index.test.ts` (additions for widget-builder/explainer tracking), `tests/components/layout/Dock.test.tsx`, `tests/i18n/widgetWeatherLocales.test.ts`. No new hooks added in this merge — existing untested hook list unchanged from 2026-06-01 audit.
- **Progress (2026-06-01, audit):** Test suite: 379 files / 3880 tests all passing (up from 347/3395 on 2026-05-27). No failing tests. New hooks added since 2026-05-27 WITHOUT tests: none confirmed new — existing gaps persist. New test files added since 2026-05-27: `MaterialsConfigurationPanel.test.tsx`, `DraggableWindow.test.tsx`, `SettingsPanel.test.tsx`, `RandomWidget.test.tsx`, `dockBoardNavLocales.test.ts`, `sidebarClassesRestrictionsLocales.test.ts`, `plcContributions.test.ts`, `useVideoActivity.test.ts` (behavior), `Countdown/Widget.test.tsx`. Previously known untested hooks that remain untested: `useFirestore.ts`, `useVideoActivitySession.ts`, `useMiniAppSession.ts`, `usePlcContributions.ts`, `useDragScroll.ts`, `usePresetSubEmails.ts`, `useReconcileExpiredSubShares.ts`, `useSubstituteShares.ts`, `useTemplateStore.ts`, `useRosters.ts`, `useStarterPacks.ts`, `useScreenRecord.ts`, `useAdminBuildings.ts`, `useBackgrounds.ts`, `useBuildingSelection.ts`, `useCatalystSets.ts`, `useClassLinkEnabled.ts`, `useGlobalStyleEditor.ts`, `useGoogleCalendar.ts`, `useImageUpload.ts`, `useInstructionalRoutines.ts`, `useLongPress.ts`, `useMiniAppAssignments.ts`, `usePlcLibraryActions.ts`, `usePlcSharedBoards.ts`, `usePlcVideoActivities.ts`, `useScaledFont.ts`, `useScreenshot.ts`, `useShortLinks.ts`, `useStudentClassDirectory.ts`, `useStudentIdleTimeout.ts`, `useTestClasses.ts`, `useWidgetBuildingId.ts`.
- **Progress (2026-05-27, audit):** Test suite: 347 files / 3395 tests all passing (up from 275/2727 on 2026-05-18). No failing tests. New hooks added since 2026-05-18 WITHOUT tests: `useAdminBuildings.ts`, `useBackgrounds.ts`, `useGlobalStyleEditor.ts`, `useNotebookSharing.ts`, `useQuiz.ts`, `useSpotifyAuth.ts`, `useSpotifySearch.ts`, `useSyncedVideoActivityGroups.ts` (`.behavior` test exists but hook itself is new), `useVideoActivity.ts` (`.behavior` test exists), `useVideoActivitySession.ts`. New hooks added since 2026-05-18 WITH tests already in place: `useChangelog.ts`, `useCollections.ts`, `useDebouncedCallback.ts`, `useFocusLossPoll.ts`, `useGoogleDrive.ts`, `usePlcAssignments.ts`, `usePlcDocs.ts`, `usePlcQuizzes.ts`, `usePlcResources.ts`, `usePlcs.ts`, `useSpotifyLibrary.ts` (has `.test.tsx`), `useSpotifyWebPlayback.ts`, `useSyncedQuizGroups.ts`, `useVideoActivityAssignments.ts`. Previously known untested hooks that remain untested: `useDragScroll.ts`, `usePresetSubEmails.ts`, `useReconcileExpiredSubShares.ts`, `useSubstituteShares.ts`, `useTemplateStore.ts`, `useMiniAppSession.ts`, `useRosters.ts`, `useFirestore.ts`, `useStarterPacks.ts`, `useScreenRecord.ts`. Additional older hooks still lacking any test: `useAssignmentPseudonyms.ts` (single-roster variant; multi covered), `useBackgrounds.ts`, `useBuildingSelection.ts`, `useBusyIdSet.ts` (has `.test.tsx`), `useCatalystSets.ts`, `useClassLinkEnabled.ts`, `useGoogleCalendar.ts`, `useGuidedLearning.ts`, `useImageUpload.ts`, `useInstructionalRoutines.ts`, `useLongPress.ts`, `useMiniAppAssignments.ts`, `usePlcContributions.ts`, `usePlcLibraryActions.ts`, `usePlcSharedBoards.ts`, `usePlcVideoActivities.ts`, `useScaledFont.ts`, `useScreenshot.ts`, `useShortLinks.ts`, `useSpotifySearch.ts`, `useStudentAssignments.ts` (2 partial tests exist), `useStudentClassDirectory.ts`, `useStudentIdleTimeout.ts`, `useTestClasses.ts`, `useWidgetBuildingId.ts`.
- **Progress (2026-05-18, audit):** Test suite: 275 files / 2727 tests all passing (up from 229/2375 on 2026-05-13). New hooks added since 2026-05-13 without tests: `useActivityWallLibrary.ts`, `useDragScroll.ts`, `usePresetSubEmails.ts`, `useReconcileExpiredSubShares.ts`, `useSubstituteShares.ts`, `useTemplateStore.ts`. New hooks added with tests already in place: `useChangelog.ts`, `useCollections.ts`, `useDriveReconnected.ts`, `useMountedBoardCache.ts`, `useResultsTabWarnings.ts`, `useSharedCollection.ts`. No failing tests. The 6 new untested hooks are relatively lower-risk (state counts 0–5) but should receive coverage. Core session hooks (`useVideoActivitySession.ts`, `useMiniAppSession.ts`, `useRosters.ts`, `useFirestore.ts`, `useStarterPacks.ts`, `useScreenRecord.ts`) remain untested from prior audit.
- **Progress (2026-04-27):** Added test coverage for `useQuizSessionTeacher` (16 tests). Covers `removeStudent` (deleteDoc-by-responseKey path + null-sessionId no-op), `revealAnswer` (dotted-path updateDoc), `hideAnswer` (deleteField sentinel at dotted path), `endQuizSession` (status='ended' + endedAt + autoProgressAt-null patch, finalizeAllResponses sweep that touches only joined/in-progress responses and skips already-completed ones, no batch.commit when nothing needs finalizing, null-sessionId no-op), and `advanceQuestion` (no-op before session loads, review-phase gate when `showPodiumBetweenQuestions` is enabled, student-paced mode skipping review, advance-to-next-index path, `startedAt` set on first advance and preserved on later ones, pass-through when already in `reviewing` phase, and the advance-past-end path that flips to 'ended' + clears `questionPhase` via `deleteField` + invokes `finalizeAllResponses`). Test count for this file is now 40 (24 prior + 16 new). The auto-progress effect is the remaining untested teacher-side path; deferred because driving it requires a real-time mock of the responses subcollection callback in tandem with the session state.
- **Progress (2026-04-22):** Added test coverage for `useQuizSession.ts` (24 tests). Covers pure helpers (`normalizeAnswer`, `gradeAnswer` across MC/FIB/Matching/Ordering, `toPublicQuestion` including `correctAnswer` strip-off for each question type) and `useQuizSessionStudent` student-join logic (`lookupSession` empty/no-match/all-ended/joinable-picker paths; `joinQuizSession` invalid-code/empty-PIN/no-session/all-ended throws, most-recent-joinable selection, PIN truncation to 10 chars, code normalization, and `classPeriod` backfill on existing responses).
- **Progress (2026-04-20):** Added initial test coverage for `useLiveSession.ts` (9 tests covering `joinSession` input validation and sanitization — code normalization, PIN truncation, duplicate-PIN rejection, self-rejoin allowance, and all error paths). Also confirmed pre-existing coverage for `useGuidedLearningSession.ts` (pure-helper test) and `useGoogleDrive.ts` / `useStorage.ts` (full test files in `tests/hooks/`). Remaining critical hooks with zero coverage:
  - `useQuizSession.ts` — teacher-side action tests landed 2026-04-27 (advance/end/reveal/remove). Auto-progress effect (responses-listener-driven) still untested.
  - `useVideoActivity.ts` / `useVideoActivitySession.ts` — video activity session management
  - `useMiniAppSession.ts` — mini-app assignment session lifecycle
  - `useRosters.ts` — roster CRUD, student list management
  - `useImageUpload.ts` — image upload handling with Firebase Storage
  - `useFirestore.ts` — core Firestore CRUD for dashboards
  - `useStarterPacks.ts` — starter pack template management
  - `useScreenRecord.ts` — screen recording lifecycle
  - `useLiveSession.ts` — needs deeper coverage (teacher actions: `startSession`, `updateSessionConfig`, `endSession`, `toggleFreezeStudent`, `toggleGlobalFreeze`)
- **File:** hooks/ directory
- **Fix:** Priority 1: cover `useSharedCollection` integration paths (copy + substitute-view-only + importSharedCollection rollback, noting that tests already exist for importSharedCollection rollback). ~~Priority 2: cover `useActivityWallLibrary.ts` (library persist path).~~ ✅ done 2026-05-20 (`tests/hooks/useActivityWallLibrary.test.ts`, 13 tests). Priority 3: cover the 6 remaining session hooks. Use Vitest with mock Firebase adapters. See `tests/hooks/useActivityWallLibrary.test.ts`, `tests/hooks/usePlcNotes.test.ts`, `tests/hooks/useLiveSession.test.ts`, and the `useQuizSessionTeacher` block in `tests/hooks/useQuizSession.test.ts` as reference patterns.

### MEDIUM utils/backgrounds.ts and utils/slug.ts have no tests (subset of utils/ coverage gap)

- **Detected:** 2026-04-13 (under the parent "utils/ coverage" item)
- **Completed:** 2026-04-29
- **File:** utils/backgrounds.ts, utils/slug.ts
- **Resolution:** Added colocated test files `utils/backgrounds.test.ts` and `utils/slug.test.ts`. Both files are pure with no external deps, so tests are deterministic.
  - `backgrounds.test.ts` (21 tests): `isExternalBackground` for http/https/data:/blob:/Tailwind/empty, `isCustomBackground` for `custom:` prefix and case sensitivity, `getCustomBackgroundStyle` for 3-/6-digit hex (lower/upper/mixed case), `rgb()`/`rgba()`, `linear-gradient(...)`, plus rejection of invalid hex (wrong length, non-hex chars), `radial-gradient`, `not-a-color`, and empty value after the `custom:` prefix.
  - `slug.test.ts` (20 tests): `slugify` lowercases, strips a single leading `@`, collapses non-alphanumerics into `-`, trims leading/trailing `-`, caps at 48 chars, returns `''` for inputs with no alphanumerics (incl. `'¿¿¿'`), and treats accented characters as separators. `slugOrFallback` returns the slug when non-empty, falls back to `crypto.randomUUID()` truncated to 24 chars, and falls back to `${prefix}-${Date.now()}` truncated to 24 chars when `randomUUID` is unavailable.
- **Verification:** `pnpm test -- --run utils/backgrounds.test.ts utils/slug.test.ts` → 41 passing tests; full suite remains green at 1576 tests / 166 files (was 1535 / 164). `pnpm type-check` clean. `pnpm exec eslint utils/backgrounds.test.ts utils/slug.test.ts --max-warnings 0` clean. `pnpm exec prettier --check ...` clean. The parent "utils/ coverage" Open item has been updated to reflect the new untested count (18 of 45) and a refreshed next-priority list.
