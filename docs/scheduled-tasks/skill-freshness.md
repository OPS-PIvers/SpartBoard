# Skill File Freshness — Scheduled Task Journal

_Audit model: claude-sonnet-4-6_
_Action model: claude-opus-4-6_
_Audit cadence: weekly — Tuesday_
_Last audited: 2026-09-22_
_Last action: 2026-09-22 — HIGH `SpecialistSchedule` stale `Settings.tsx` reference fixed in both new-widget and admin-widget-config skills (all 4 mirror files): file-layout callout rewritten to the current schema-driven files, `featurePermissions`-read example repointed to `settingsFields.tsx`. Moved to Completed._

---

## In Progress

_Nothing currently in progress._

---

## Open

_2026-09-22: Weekly audit (Tuesday; branch HEAD `bc0a535`). Read this journal in full, both `.claude/skills/{new-widget,admin-widget-config}/SKILL.md` files, and diffed both against their `.agents/skills/` mirrors — still only cosmetic Prettier/naming/"SPART Board" vs "SpartBoard" differences and the pre-existing structural gap (`.agents/new-widget` still lacks the `.claude/` copy's Step 3.5 numbering), no new substantive divergence. Checked this session's available-skills listing again for `spart-new-widget`/`spart-widget-admin-config`: neither is present (only unprefixed `new-widget`/`admin-widget-config`), and `/mnt/skills/user/` does not exist in this environment (only `/mnt/skills/examples` and `/mnt/skills/public`) — consistent with 2026-09-15 and 2026-09-17; the pre-existing HIGH `spart-` fork item remains not independently reverifiable this run. `git log 54b1bd7..HEAD` over the filtered paths shows 38 commits, dominated by six widget-settings-drawer migration waves (`e0dd38d`, `7b455e0`, `9319eac`, `b620457`, `e4cf515`, `6d59b65`, all merged 2026-09-19–21) that grew `WIDGET_SETTINGS_SCHEMAS` in `WidgetRegistry.ts` from 20 entries (2026-09-17) to **51**, plus two more `canonicalBuildingId()` retrofit commits (`ed54082` "canonicalize building ids across 22 more config panels," 2026-09-18, and `83538a2` "...in VideoActivityConfigurationModal," 2026-09-19), bringing the count of `components/admin/` files referencing `canonicalBuildingId` to 47 (up from 23). **Major new finding:** `e0dd38d` ("migrate next five widget drawers," 2026-09-19) deleted `components/widgets/SpecialistSchedule/Settings.tsx` (731 lines) and moved its content to `settings.schema.ts` + `settingsFields.tsx`, registering `specialist-schedule` in `WIDGET_SETTINGS_SCHEMAS` with zero remaining entries in `WIDGET_SETTINGS_COMPONENTS`/`WIDGET_APPEARANCE_COMPONENTS`; `index.ts` now only re-exports `SpecialistScheduleWidget` (the `export * from './Settings'` line is gone), and the named exports `SpecialistScheduleSettings`/`SpecialistScheduleAppearanceSettings` no longer exist anywhere in the codebase (zero grep matches). This breaks `new-widget/SKILL.md`'s dedicated "SpecialistSchedule file layout (reference note)" callout (lines 605–617, both mirrors) and `admin-widget-config/SKILL.md`'s "How Configs Reach the Widget" example (line 222, both mirrors), which cites `SpecialistSchedule/Settings.tsx` reading `featurePermissions` — that read survives, only relocated to `settingsFields.tsx` (and unchanged in `SpecialistScheduleWidget.tsx`), so the pattern is intact but the cited path is wrong. See new HIGH item below. Separately, all seven "still-valid non-schema" replacement candidates the 2026-09-15 HIGH item listed for the recurring Checklist reference (`Schedule`, `Countdown`, `Stations`, `NumberLine`, `MathTools`, `Calendar`, `WorkSymbols`) have now **all** been migrated to schema-only settings across these same six waves — a repo-wide scan found only four widgets left anywhere with a genuine non-schema `Settings.tsx`: `Projects`, `ActivityWall`, `SmartNotebook` (appearance-half only), and `TalkingTool`, three of which (`Projects`, `ActivityWall`, `TalkingTool`) still have co-located `*Settings`/`*AppearanceSettings` exports and could serve as a replacement — though `docs/plans/widget-settings-inventory.md` already inventories `activity-wall`, `talking-tool` and `smartNotebook` as future migration candidates, so any one of them risks the exact same recurrence. Re-verified the standing HIGH `Checklist` item: `.claude/skills/new-widget/SKILL.md`'s "Settings + Appearance split" row (line 601) and Step 3.5's "Already cut over" sentence (still naming only `clock`/`time-tool`/`text`/`embed`/`lunchCount`) remain uncorrected and are now more stale than ever (5 named vs. 51 actually schema-only); `components/widgets/Checklist/` still has no `Settings.tsx`. All 3 MEDIUM items reconfirmed open and unchanged in substance: `canonicalBuildingId()` (`config/buildings.ts`) still absent from admin-widget-config's Building selector section (now reinforced by 47 files, up from 23, across two more systematic retrofit commits this week); `APPEARANCE_CONFIG_KEYS` (`utils/widgetConfigPersistence.ts`) still unmentioned in new-widget; `getAdminBuildingConfig()` (`utils/adminBuildingConfig.ts:166`, now 34 `case` branches, up from ~33) still unmentioned in admin-widget-config. All 6 LOW items reconfirmed open: `BUILDING_CONFIG_PANELS` still `Partial<Record<string, BuildingConfigPanel>>` (`FeatureConfigurationPanel.tsx:94`) vs. the skill's `WidgetType | InternalToolType` claim; `QRWidget/Widget.tsx` still has no `ScaledEmptyState`/empty-state branch; the admin-config-step-undercounts-changes item unchanged; the `SpecialistScheduleConfig.fontFamily: string` (`types.ts:6514`) vs. `GlobalFontFamily` item unchanged; the exclusion-array example still missing the same 8 types from the real 14-type array (`FeaturePermissionsManager.tsx:989–1002`, confirmed unchanged); and the `BlendingBoardAppearanceSettings` suppressor item is reconfirmed open but its underlying facts have shifted further — wave 12 (`6d59b65`, 2026-09-21) deleted `BlendingBoard/Settings.tsx` entirely, and `BlendingBoardAppearanceSettings` no longer exists anywhere in the codebase; the "centrally managed" notice this item describes now lives as a `type: 'custom'` field (`managedNotice`) inside `BlendingBoard/settings.schema.ts`, not a `WIDGET_APPEARANCE_COMPONENTS` suppressor component, so a future fix to this item must also update its own cited example, not just add documentation. Widget-creation checklist files (`types.ts`, `config/tools.ts`, `config/widgetDefaults.ts`, `config/widgetGradeLevels.ts`, `WidgetRegistry.ts`), the admin config path/hierarchy (`FeaturePermissionsManager.tsx`, `FeatureConfigurationPanel.tsx`, `SpecialistScheduleConfigurationModal.tsx`, `ClockConfigurationPanel.tsx`, `DiceConfigurationPanel.tsx`, `ScheduleConfigurationPanel.tsx`, `VideoActivityConfigurationModal.tsx`), the "Wiring location 3" exclusion list (`FeatureConfigurationPanel.tsx:687–699`, verbatim match), the `pnpm run new-widget-settings` scaffold script, and `lazyNamed()` (`WidgetRegistry.ts:42`) all reconfirmed present and matching skill documentation exactly. Total open after this run: 6 LOW + 3 MEDIUM (unchanged) + 3 HIGH (1 pre-existing packaging item + 1 recurring Checklist reference + 1 new SpecialistSchedule reference) = 12 open, up from 11.

**2026-09-22 action:** Resolved the new HIGH SpecialistSchedule item (see Completed) — the highest-priority Open item across today's full reading list (widget-registry: 3 LOW; css-scaling: 1 MEDIUM + 15 LOW; typescript-eslint: 0 open; dependency-audit: MEDIUM/LOW only in its Open section). Selected over this journal's other two HIGH items since it was the freshest finding (detected by today's own audit pass) and directly actionable as a documentation fix, unlike the pre-existing `spart-` fork item, whose own Fix field states it "needs a decision at the skill-packaging/sync level" rather than a text edit. Total open after this action: 6 LOW + 3 MEDIUM + 2 HIGH = 11._

_2026-09-17: Weekly audit (Thursday, per this cycle's schedule; branch HEAD `54b1bd7`). Read this journal in full plus both `.claude/skills/{new-widget,admin-widget-config}/SKILL.md` files, diffed both against their `.agents/skills/` mirrors (still only cosmetic Prettier/naming differences, no substantive divergence), and reverified all 11 standing open items directly against the codebase rather than trusting 2026-09-15's confirmations. New dev-paul commits since the last audit touching filtered paths: `cde6a0c` "Migrate five widgets to schema settings drawers (#3073)" (2026-09-16) added `qr`, `scoreboard`, `calendar`, `poll`, and `instructionalRoutines` to `WIDGET_SETTINGS_SCHEMAS` (now 20 entries total, up from 15), deleting their `Settings.tsx` files — none of these five were cited anywhere in either skill (confirmed via grep for `Calendar`, `InstructionalRoutines`, `PollWidget`, `Scoreboard`, `QRWidget` — only `QRWidget/Widget.tsx`, a front-face file untouched by this commit, is cited), so this migration wave introduces **no new stale citation**, but it does make the standing HIGH item's "already cut over" list even more stale (15 → 20 schema-only widgets), reinforcing that item's suggested fix of pointing at `WIDGET_SETTINGS_SCHEMAS` directly rather than enumerating names. Also new: `57252d7` "canonicalize building id in TrafficLightConfigurationPanel" (2026-09-15) and `ff8db65` "canonicalize building id in ClassesConfigurationPanel" (2026-09-14) — two more retrofits of the exact bug class the standing MEDIUM `canonicalBuildingId()` item warns about (now 23 files under `components/admin/` reference `canonicalBuildingId`), reinforcing rather than resolving that item. (1) **HIGH — Checklist Settings+Appearance reference re-verified still broken**: `.claude/skills/new-widget/SKILL.md`'s "Reference Implementations" table (line 601) and the `.agents/` mirror still cite `components/widgets/Checklist/Settings.tsx`; `components/widgets/Checklist/` confirmed to contain only `settings.schema.ts`, `settingsFields.tsx`, and an orphaned `Settings.test.tsx` — no `Settings.tsx`. Item remains open and uncorrected. (2) **Pre-existing HIGH `spart-` fork — still not independently reverifiable**: this session's available-skills listing again contains `new-widget`/`admin-widget-config` only, no `spart-new-widget`/`spart-widget-admin-config` entries, and `/mnt/skills/user/` does not exist in this environment (only the in-repo `.claude/skills/` and `.agents/skills/` mirrors were checked, and remain content-identical modulo formatting). Left open and unchanged pending a run that can confirm the fork's status. (3) All 3 MEDIUM items reconfirmed still open and accurate: `canonicalBuildingId()` (`config/buildings.ts`) still unmentioned in admin-widget-config's Building selector section (strengthened by this week's two new retrofit commits); `APPEARANCE_CONFIG_KEYS` (`utils/widgetConfigPersistence.ts`) still unmentioned in new-widget; `getAdminBuildingConfig()` (`utils/adminBuildingConfig.ts`) still unmentioned in admin-widget-config — zero grep matches for any of the three across both skill trees. (4) All 6 LOW items reconfirmed still valid: `BUILDING_CONFIG_PANELS` still `Partial<Record<string, BuildingConfigPanel>>` in code (`FeatureConfigurationPanel.tsx:92`) vs skill's `WidgetType | InternalToolType` claim; `QRWidget/Widget.tsx` still has no `ScaledEmptyState`/empty-state branch (renders a hardcoded default URL unconditionally); admin-config Step 7 wording unchanged; `BlendingBoardAppearanceSettings` suppressor pattern (`BlendingBoard/Settings.tsx:24`, registered `WidgetRegistry.ts:428`) still undocumented; `SpecialistScheduleConfig.fontFamily` still plain `string` (`types.ts:6241`) against the `GlobalFontFamily` majority; the admin-widget-config exclusion-array code example (7 items incl. placeholder) still missing the same 8 types from the real 14-type array in `FeaturePermissionsManager.tsx:988-1002` (blooms-taxonomy, catalyst, graphic-organizer, music, pdf, quiz, video-activity, work-symbols — unchanged since 2026-09-15). Widget-creation checklist files, `SpecialistSchedule/` file layout, `lazyNamed()` convention, and the admin config path/hierarchy (all cited `*ConfigurationPanel.tsx`/`*ConfigurationModal.tsx` files, including `SpecialistScheduleConfigurationModal.tsx`, `ClockConfigurationPanel.tsx`, `DiceConfigurationPanel.tsx`, `ScheduleConfigurationPanel.tsx`) all reconfirmed present and matching skill documentation exactly. Total open after this run: 6 LOW + 3 MEDIUM (unchanged) + 2 HIGH (1 pre-existing packaging item + 1 recurring Checklist reference) — zero new findings this cycle._

_2026-09-15: Weekly audit (Tuesday). Read this journal in full plus both `.claude/skills/{new-widget,admin-widget-config}/SKILL.md` files, then verified every checklist claim directly against the current codebase rather than trusting last week's confirmations. (1) Widget-creation checklist files reconfirmed present and unchanged in shape: `types.ts` (`WidgetType` union, `ConfigForWidget` at line 7076, still present), `config/tools.ts`, `config/widgetDefaults.ts` (`WIDGET_DEFAULTS: Record<WidgetType, Partial<WidgetData>>`), `config/widgetGradeLevels.ts` (`WIDGET_GRADE_LEVELS: Record<...>`), `components/widgets/WidgetRegistry.ts` (`WIDGET_SCALING_CONFIG: Record<WidgetType, ScalingConfig>` still exhaustive at line 541, `lazyNamed()` convention at line 42 unchanged). (2) Gold-standard reference `SpecialistSchedule/` reconfirmed unchanged: `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` (+ test) — matches the skill's documented exception note. (3) Admin config hierarchy reconfirmed: `FeaturePermissionsManager.tsx`, `FeatureConfigurationPanel.tsx`, `SpecialistScheduleConfigurationModal.tsx`, `ClockConfigurationPanel.tsx`, `DiceConfigurationPanel.tsx`, `ScheduleConfigurationPanel.tsx` all present; the "Wiring location 3" exclusion list in `FeatureConfigurationPanel.tsx:684-696` still matches the skill's code sample verbatim (calendar, expectations, guided-learning, instructionalRoutines, miniApp, quiz, stickers, talking-tool, weather, webcam, `...Object.keys(BUILDING_CONFIG_PANELS)`). (4) `lazyNamed()` still matches documented convention exactly. (5) **Found one NEW HIGH item** (see below): `git log` shows `5608faf "feat(settings): consolidate field-level widget drawer migrations (#3042)"` merged 2026-09-14 (one day before this audit, and after the skill file's last edit at `c0a5a087` on 2026-09-08) migrated `checklist`, `weather`, `expectations`, `dice`, `drawing`, `sound`, `soundboard`, `random`, `url`, and `webcam` onto the schema-driven settings drawer — confirmed via `WIDGET_SETTINGS_SCHEMAS` in `WidgetRegistry.ts` (now 15 entries total) and confirmed zero of these 15 types appear in `WIDGET_SETTINGS_COMPONENTS`/`WIDGET_APPEARANCE_COMPONENTS`. This breaks last week's own fix: `Checklist/Settings.tsx`, which the skill was updated exactly one week ago to cite as the "Settings + Appearance split" reference, no longer exists (only `settings.schema.ts` + `settingsFields.tsx` + an orphaned `Settings.test.tsx` remain in `Checklist/`). The `.agents/skills/new-widget/SKILL.md` mirror carries the identical stale line (confirmed via grep, line 524) — out of scope to edit this pass per task constraints, but worth noting for the future fix. Reverified all standing items directly rather than carrying forward: all 3 MEDIUM items (`canonicalBuildingId()` at `config/buildings.ts:124` still unmentioned in admin-widget-config's Building selector section; `APPEARANCE_CONFIG_KEYS` at `utils/widgetConfigPersistence.ts:30` still unmentioned in new-widget; `getAdminBuildingConfig()` at `utils/adminBuildingConfig.ts:166` still unmentioned in admin-widget-config) confirmed still open and unchanged. All 6 LOW items reverified and still valid: `BUILDING_CONFIG_PANELS` still typed `Partial<Record<string, BuildingConfigPanel>>` in code (`FeatureConfigurationPanel.tsx:92`) vs skill's `WidgetType | InternalToolType` claim; `QRWidget/Widget.tsx` reconfirmed to have no empty/unconfigured-state branch at all now (defaults straight to a hardcoded URL, no `ScaledEmptyState` import) — the skill's "Good empty state usage" citation remains wrong; admin-config Step 7 wording unchanged; `BlendingBoardAppearanceSettings` suppressor pattern (`BlendingBoard/Settings.tsx:24`) still undocumented; `SpecialistScheduleConfig.fontFamily` still plain `string` (`types.ts:6221`) against the `GlobalFontFamily` majority; the admin-widget-config exclusion-array code example is now missing 8 types from the real 14-type array in `FeaturePermissionsManager.tsx:988-1003` (`quiz` was added to the real array since the last count, making the example slightly more stale). The pre-existing HIGH `spart-` skill-fork item was not independently reverifiable this run — this session's available-skills listing contains `new-widget`/`admin-widget-config` but no `spart-new-widget`/`spart-widget-admin-config` entries at all (unlike prior audit sessions, which found both pairs served under the `Skill` tool); left open and unchanged pending a run that can confirm whether the fork was retired or is simply not exposed in this session's plugin config. Total open after this run: 6 LOW + 3 MEDIUM (unchanged) + 2 HIGH (1 pre-existing packaging item + 1 new)._

### HIGH `.claude/skills/new-widget/SKILL.md`'s "Settings + Appearance split" reference is broken again one week after being fixed — `Checklist/Settings.tsx` was deleted by this week's schema-drawer migration wave

- **Detected:** 2026-09-15
- **File:** `.claude/skills/new-widget/SKILL.md` — "Reference Implementations" table ("Settings + Appearance split" row) and Step 3.5's "Already cut over" widget list; mirrored in `.agents/skills/new-widget/SKILL.md`.
- **Detail:** On 2026-09-08 this journal's action pass swapped the "Settings + Appearance split" reference row from the now-schema-only `ClockWidget/Settings.tsx` to `components/widgets/Checklist/Settings.tsx`, after confirming Checklist had no `settings.schema.*` file and was a stable non-schema reference at the time. Commit `5608faf` ("feat(settings): consolidate field-level widget drawer migrations (#3042)", merged 2026-09-14) migrated `checklist` — along with `weather`, `expectations`, `dice`, `drawing`, `sound`, `soundboard`, `random`, `url`, and `webcam` — onto the schema-driven settings drawer. `components/widgets/Checklist/` now contains `settings.schema.ts` and `settingsFields.tsx` but **no `Settings.tsx`** (only an orphaned `Settings.test.tsx` remains); `WidgetRegistry.ts`'s `WIDGET_SETTINGS_SCHEMAS` map now has a `checklist` entry, and `checklist` has zero entries in `WIDGET_SETTINGS_COMPONENTS`/`WIDGET_APPEARANCE_COMPONENTS`. A developer opening the cited file today finds nothing there. Separately, Step 3.5's "Already cut over" sentence still names only `clock`, `time-tool`, `text`, `embed`, and `lunchCount` (wave 2) — it is now missing 10 more schema-only widgets from this and other waves. The full current schema-only set, confirmed via `WIDGET_SETTINGS_SCHEMAS` and cross-checked against the absence of a `Settings.tsx` for each: `soundboard`, `dice`, `sound`, `webcam`, `drawing`, `text`, `embed`, `lunchCount`, `clock`, `time-tool`, `checklist`, `weather`, `expectations`, `random`, `url` (15 total). Widgets confirmed to still have a genuine non-schema `Settings.tsx` (no `settings.schema.*` sibling) as of this audit, any of which would make a stable replacement reference: `Schedule/Settings.tsx`, `Countdown/Settings.tsx`, `Stations/Settings.tsx`, `NumberLine/Settings.tsx`, `MathTools/Settings.tsx`, `Calendar/Settings.tsx`, `WorkSymbols/Settings.tsx`.
- **Fix:** Replace the "Settings + Appearance split" row's file reference with one of the still-valid non-schema examples above (e.g. `components/widgets/Schedule/Settings.tsx`). Update Step 3.5's "Already cut over" sentence to the full 15-widget list above, or — since this exact drift (a hand-picked reference widget getting migrated out from under the skill) has now recurred in back-to-back weekly audits — consider rewording Step 3.5 to point at `WIDGET_SETTINGS_SCHEMAS` in `WidgetRegistry.ts` as the authoritative, self-maintaining list of schema-only widgets instead of enumerating widget names in prose that goes stale every time another migration wave ships. Apply to both `.claude/skills/new-widget/SKILL.md` and the `.agents/skills/new-widget/SKILL.md` mirror (confirmed to carry the identical stale line at line 524).

_2026-09-08 action: Resolved the HIGH `ClockWidget`/`LunchCount` stale Settings+Appearance reference item. Selection: today is Tuesday; reading list = three dailies (widget-registry, css-scaling, typescript-eslint) + Tuesday weeklies (skill-freshness, dependency-audit). Nothing In Progress anywhere (all five journals confirmed clear). This journal's own new HIGH item (filed by today's own audit pass, above) is the only HIGH open item across the full reading list — widget-registry/typescript-eslint have no open items above LOW, css-scaling's open items are all LOW/MEDIUM, and dependency-audit's items are all MEDIUM/LOW — so HIGH wins the severity tiebreak outright. File-recency check on `.claude/skills/new-widget/SKILL.md` passed: last touched at `f5c7feb0`, well outside the last 5 branch commits (`3ec40beb`, `ea3dcc3b`, `1e292191`, `072322e0`, `2a7adf68`). Verified the finding directly: `components/widgets/ClockWidget/` and `components/widgets/LunchCount/` both confirmed to have no `Settings.tsx` file; `WidgetRegistry.ts` confirmed to have `clock`/`lunchCount` entries only in `WIDGET_COMPONENTS` and `WIDGET_SETTINGS_SCHEMAS`, none in `WIDGET_SETTINGS_COMPONENTS`/`WIDGET_APPEARANCE_COMPONENTS`. Picked a replacement reference widget with a genuine co-located `Settings.tsx`+`AppearanceSettings` split that is not schema-migrated: surveyed all widgets' `Settings.tsx` files for both a `*Settings` and `*AppearanceSettings` export and confirmed `Checklist/` has neither a `settings.schema.*` file nor any schema-drawer entry in `WidgetRegistry.ts`, so it's a stable non-schema reference. Applied to both mirrors (`.claude/skills/new-widget/SKILL.md` + `.agents/skills/new-widget/SKILL.md`): (1) swapped the "Settings + Appearance split" row from `ClockWidget/Settings.tsx` to `Checklist/Settings.tsx`; (2) added a caveat noting `clock`/`time-tool`/`text`/`embed`/`lunchCount` are schema-only and have no `Settings.tsx`/`WIDGET_APPEARANCE_COMPONENTS` entry — placed in Step 3.5 in the `.claude/` copy (which has that section) and as a note directly under the Reference Implementations table in the `.agents/` copy (which lacks a Step 3.5 section — the larger structural gap between the two mirrors, and the separate `spart-new-widget`/`spart-widget-admin-config` synced-fork drift, are out of scope for this single-item fix and remain tracked in the standing notes above). Did not touch the "Clean scaling (hero number)" or "List with cqmin rows" rows — those cite `ClockWidget/Widget.tsx`/`LunchCount/Widget.tsx` (front-face files, which still exist and remain valid scaling references; only the legacy `Settings.tsx` panel reference was stale). `prettier --check` on both changed files: clean. Documentation-only, zero code/behavioral impact — no type-check/lint/test run needed. Moved item to Completed. PR opened against dev-paul. Remaining open: 6 LOW + 2 MEDIUM (1 dependency-audit-adjacent skill item + 1 admin-config item) + 1 pre-existing HIGH (the `spart-`/unprefixed skill-fork drift, unresolved — see 2026-09-01 entry)._

_2026-09-08: Weekly audit (Tuesday), delegated to a dedicated sub-agent. Re-verified 5 standing checks against the current codebase rather than trusting prior runs: (1) PASS — `components/widgets/SpecialistSchedule/` still exactly `SpecialistScheduleWidget.tsx`/`Settings.tsx`/`index.ts`/`utils.ts` (+ test). (2) PASS — the checklist's required files (`config/tools.ts`, `config/widgetDefaults.ts`, `config/widgetGradeLevels.ts`, `WidgetRegistry.ts`, `types.ts`) all exist and match current requirements; `WIDGET_SCALING_CONFIG` reconfirmed exhaustive `Record<WidgetType, ScalingConfig>` at `WidgetRegistry.ts:556`. (3) PASS — `components/admin/` structure matches the skill's description (`ClockConfigurationPanel.tsx`, `DiceConfigurationPanel.tsx`, `SpecialistScheduleConfigurationModal.tsx`, `FeatureConfigurationPanel.tsx` with `BUILDING_CONFIG_PANELS`, all present). (4) PASS — `lazyNamed()` at `WidgetRegistry.ts:42-60` still matches the documented convention exactly. (5) PASS (in-repo skills only) — every backtick-wrapped file path cited in both `.claude/skills/*/SKILL.md` files resolves to a real file. Two new discrepancies surfaced by this week's dev-paul rebase (the widget settings drawer wave 2, PR #2896, merged 2026-09-06) — see the new HIGH and MEDIUM items below. Standing items reconfirmed unchanged (not re-litigated in full): the `spart-new-widget`/`spart-widget-admin-config` synced-skill fork remains stale (still missing the Step 3.5 settings-schema section, `WIDGET_SETTINGS_SCHEMAS`, the transparency-ownership rules, and the SpecialistSchedule naming-exception note; its Common Mistakes table still misdescribes a missing `WIDGET_SCALING_CONFIG` entry as a silent runtime fallback rather than a compile error); neither `new-widget` skill copy documents the `APPEARANCE_CONFIG_KEYS` board-isolation allowlist in `utils/widgetConfigPersistence.ts`. Total open after this run: 6 LOW + 2 MEDIUM (1 pre-existing + 1 new) + 2 HIGH (1 pre-existing + 1 new)._

### MEDIUM `.claude/skills/admin-widget-config/SKILL.md`'s "Building selector" guidance omits `canonicalBuildingId()` — a recurring source of building-id indexing bugs

- **Detected:** 2026-09-08
- **File:** `.claude/skills/admin-widget-config/SKILL.md` — "Building selector" section
- **Detail:** The skill instructs `config.buildingDefaults[buildingId] = {...}` with no mention of `canonicalBuildingId()` (`config/buildings.ts:124`). At least four recent commits (`2a7adf68` "Fix canonicalBuildingId bug in TimeToolConfigurationPanel", plus prior fixes to `UrlConfigurationPanel`, `ClockConfigurationPanel`, `DiceConfigurationPanel`) had to retrofit `canonicalBuildingId()` into admin config panels after shipping with raw, non-canonicalized building IDs as the object key — a recurring failure mode (building IDs can have multiple equivalent string forms) the skill should warn about but doesn't.
- **Fix:** Add a line to the "Building selector" section: always key `buildingDefaults` (and any building-scoped admin config map) by `canonicalBuildingId(buildingId)`, not the raw ID from the selector — cite `TimeToolConfigurationPanel.tsx` as the corrected reference and note the class of bug this prevents.

_2026-09-01: This run's task instructions directed loading the skills via the `Skill` tool by name `spart-new-widget` and `spart-widget-admin-config`, asserting these are "the canonical widget-creation and admin-config-wiring documentation used by engineers and other automated agents." This is the first audit run to fetch skill content this way rather than reading `.claude/skills/{new-widget,admin-widget-config}/SKILL.md` directly. The fetched content resolved to files under `/root/.claude/skills/synced/.../{spart-new-widget,spart-widget-admin-config}/SKILL.md` — a plugin/marketplace-synced copy, **not** the in-repo files this journal has been auditing and fixing since 2026-04-14. This environment's available-skills listing shows FOUR distinct widget-skill entries: `new-widget`, `admin-widget-config` (unprefixed) AND `spart-new-widget`, `spart-widget-admin-config`. The unprefixed pair's listed descriptions match the in-repo `.claude/skills/{new-widget,admin-widget-config}/SKILL.md` front-matter `description:` fields verbatim (confirmed via direct read) — i.e. `new-widget`/`admin-widget-config` are the up-to-date, repo-sourced project skills this journal's fixes have actually been landing in. The `spart-` pair is a stale fork carrying an expanded/reworded description and, per direct diff, none of this journal's ~4 months of applied fixes. See the new HIGH item below. Beyond that fork-drift discovery, direct codebase verification against the fetched `spart-` skill content confirmed everything else already tracked remains accurate and unchanged: (1) `SpecialistSchedule/` still exactly `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts`, `Settings.test.tsx`. (2) `ClockWidget/`, `Weather/` (not `WeatherWidget/`), `LunchCount/`, `QRWidget/` all confirmed present at cited paths; `QRWidget/Widget.tsx` re-confirmed still hand-rolled empty-state markup with no `ScaledEmptyState` import (LOW item stands). (3) `FeaturePermissionsManager.tsx` exclusion array (lines 971–986) still the same 13 types. (4) `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` still `Partial<Record<string, BuildingConfigPanel>>` (line 92) vs both skill copies' `WidgetType | InternalToolType` claim — LOW item stands. (5) `SpecialistScheduleConfigurationModal.tsx` confirmed present and still wired via the `activeModalTool?.type === 'specialist-schedule'` conditional block in `FeaturePermissionsManager.tsx` (lines 899–905), matching Path A. (6) `lazyNamed()` convention and all `specialist-schedule` registry entries (lines 177, 356, 478, 875) confirmed correct; `WIDGET_SCALING_CONFIG` confirmed still `Record<WidgetType, ScalingConfig>` (exhaustive, line 563) and no other top-level registry map exists in `WidgetRegistry.ts` beyond the 4 the skill documents. (7) `utils/adminBuildingConfig.ts`'s `getAdminBuildingConfig()` confirmed still absent from both skill copies (zero grep matches) — the existing MEDIUM item below stands unchanged. (8) One existing LOW item ("does not document hardcoded exclusion list in `FeatureConfigurationPanel.tsx`") was re-verified and found already resolved in **both** the repo file and the fetched `spart-` copy — a "Wiring location 3" subsection fully covering this now exists in both (git history shows it present at least since 2026-06-19, the oldest commit visible in this shallow clone; prior weekly audits apparently kept re-confirming the underlying exclusion-array *contents* without re-checking whether the *documentation-gap* claim itself was still true). Moved to Completed below. (9) New MEDIUM item: neither skill copy mentions `utils/widgetConfigPersistence.ts` / `APPEARANCE_CONFIG_KEYS`, the board-isolation allowlist CLAUDE.md flags as an important rule for any new widget config key — see below. Total open after this run: 6 LOW + 2 MEDIUM (1 pre-existing + 1 new) + 1 new HIGH._

### HIGH `spart-new-widget` and `spart-widget-admin-config` (the skills actually served under those names) are a stale fork of the up-to-date `new-widget`/`admin-widget-config` project skills — missing ~4 months of fixes and a whole newer section

- **Detected:** 2026-09-01
- **File:** N/A (skill file) — the `spart-new-widget` and `spart-widget-admin-config` skills as resolved by the `Skill` tool in this environment (backed by files under `/root/.claude/skills/synced/.../spart-{new-widget,widget-admin-config}/SKILL.md`), versus the in-repo `.claude/skills/{new-widget,admin-widget-config}/SKILL.md` (and `.agents/skills/` mirrors) that this journal has audited and fixed since 2026-04-14.
- **Detail:** This environment exposes two independently-versioned skill pairs with nearly identical trigger descriptions for the same job: `new-widget` / `admin-widget-config` (unprefixed — confirmed via direct file read to be the in-repo, actively-maintained skills, front-matter description verbatim-matching the available-skills listing) and `spart-new-widget` / `spart-widget-admin-config` (the pair this run's task instructed be loaded via the `Skill` tool, described as "canonical"). Diffing the fetched `spart-` content against the current repo files shows the `spart-` pair is stale by months and reproduces bugs this journal already fixed in the repo copy:
  - `spart-new-widget`'s Common Mistakes table still reads "Skipping `WIDGET_SCALING_CONFIG` entry → Widget won't render — falls to `DEFAULT_SCALING_CONFIG` silently" — the exact incorrect claim corrected in the repo file on 2026-07-07 (repo now correctly says this fails `pnpm type-check` and blocks CI, since the map is an exhaustive `Record`, not a `Partial` with a runtime fallback).
  - `spart-new-widget`'s Reference Implementations table still cites the non-existent `components/widgets/SpecialistSchedule/Widget.tsx` for "Building-defaults consumption" — fixed in the repo file on 2026-05-26 to `SpecialistScheduleWidget.tsx`, with a follow-up clarifying note about the co-located `Settings.tsx` appearance-panel pattern added 2026-07-09. Both fixes are entirely absent from `spart-new-widget`.
  - `spart-new-widget` is missing the repo file's "Transparency Rules For Widget.tsx" section entirely (the bg-transparent-ownership requirements now called out at the top of the repo skill and matching CLAUDE.md's "Default widget roots should remain visually transparent" standard). A developer relying solely on `spart-new-widget` has no guidance against shipping a widget with a full-size opaque inner background.
  - `spart-widget-admin-config`'s "How Configs Reach the Widget" section still references the non-existent `SpecialistScheduleSettings.tsx` — fixed in the repo file on 2026-06-23 to the correct path `components/widgets/SpecialistSchedule/Settings.tsx`.

  In short: every fix this journal has landed against the new-widget/admin-config skill surface over the past ~4 months has gone into a copy that is apparently not what actually gets served under the `spart-` names in this environment. If `spart-new-widget`/`spart-widget-admin-config` are what engineers or other automated agents actually trigger (via slash command or by matching the "Triggers include: build a new widget…" wording), all of that fix work has had no real-world effect, and the originally-reported bugs are still live today.
- **Fix:** This is not a text edit inside one skill file — it needs a decision at the skill-packaging/sync level: either (a) regenerate/republish the `spart-new-widget` and `spart-widget-admin-config` synced skill definitions from the current, already-fixed content of `.claude/skills/new-widget/SKILL.md` and `.claude/skills/admin-widget-config/SKILL.md`, and set up a mechanism to keep them in sync going forward, or (b) retire the `spart-` duplicates in favor of the single project-local `new-widget`/`admin-widget-config` skills so there is exactly one source of truth and no drift risk. Until one of those happens, treat any fix this journal lands in `.claude/skills/` as unverified until it's confirmed reachable via the actual `spart-` skill names too.

### MEDIUM Neither new-widget skill documents the CLAUDE.md board-isolation / `APPEARANCE_CONFIG_KEYS` allowlist rule for widget config keys

- **Detected:** 2026-09-01
- **File:** `.claude/skills/new-widget/SKILL.md` (and its `spart-new-widget` counterpart) — Step 1b (config interface) / Step 5 (`config/widgetDefaults.ts`)
- **Detail:** CLAUDE.md's "Board isolation: config keys are per-board unless explicitly allowlisted" section documents `utils/widgetConfigPersistence.ts`'s `APPEARANCE_CONFIG_KEYS` (confirmed present in the codebase, imported by `context/DashboardContext.tsx` and `context/AuthContext.tsx`) as the closed allowlist controlling which top-level widget config keys persist as account-wide defaults across boards versus staying scoped to a single board. This is called out in CLAUDE.md as consequential: adding a content-bearing key to the allowlist by mistake leaks a teacher's data (roster, notes, checklist items, etc.) across class-section boards. Neither skill's config-interface template (Step 1b) or defaults template (Step 5) mentions this rule at all — a developer defining a new widget's config fields (e.g. `someText`, `themeColor`, `fontFamily`) gets no signal that most fields are safely per-board by default, or that only purely-visual fields belong in the allowlist, or that the allowlist even exists.
- **Fix:** Add a short callout to Step 1 or Step 5 of the new-widget skill: new config keys are per-board by default and require no registration; only add a key to `APPEARANCE_CONFIG_KEYS` in `utils/widgetConfigPersistence.ts` if it is purely visual (font, color, opacity, layout) and carries no lesson content or student data — link to CLAUDE.md's "Board isolation" section for the full rule.

_2026-08-25: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs). Full codebase-side verification via direct file reads of both `.claude/skills/{new-widget,admin-widget-config}/SKILL.md` mirrors. New dev-paul commits since 2026-08-18 touching filtered paths: `690f2729` (feat(admin-config): add Calendar building-default appearance config — adds a `case 'calendar'` to `utils/adminBuildingConfig.ts`'s `getAdminBuildingConfig()`, extends `CalendarConfigurationModal.tsx` and `types.ts`) and `3cb4bcf4` (fix(css-scaling): cqmin conversion across 11 widgets, including `SpecialistScheduleWidget.tsx` — spacing/icon-size only, no structural/file-layout change, no new patterns introduced). Verified directly: (1) `SpecialistSchedule/` still exactly `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` (+ `Settings.test.tsx`) — matches skill's reference note. (2) `WidgetRegistry.ts` `lazyNamed()` convention and all 3 `specialist-schedule` registry entries confirmed correct (lines 177, 356, 478, 874). (3) `BUILDING_CONFIG_PANELS` still `Partial<Record<string, BuildingConfigPanel>>` (`FeatureConfigurationPanel.tsx:92`) vs skill's `WidgetType | InternalToolType` — pre-existing LOW mismatch persists, unchanged. (4) `FeaturePermissionsManager.tsx` exclusion array (lines 971–986) still the same 13 types — pre-existing LOW item unchanged. (5) `FeatureConfigurationPanel.tsx` bottom exclusion list (lines 684–696) still matches the skill's Path B code sample verbatim. (6) `QRWidget/Widget.tsx` re-confirmed still hand-rolled empty-state markup (no `ScaledEmptyState` import) — skill's Reference Implementations table still cites it; pre-existing LOW item remains open and still uncorrected in the skill file itself. (7) Reference implementations spot-checked and confirmed accurate: `ClockWidget/Widget.tsx` (hero-number cqmin scaling), `Weather/Widget.tsx` (icon+text cqmin), `LunchCount/Widget.tsx` (cqmin list rows), `ExpectationsWidget/Widget.tsx` (confirmed transparent root — `bg-transparent` at line 446). (8) Mirror diff (`.claude/skills/` vs `.agents/skills/`) re-run for both files: only cosmetic differences remain (Prettier-style table/line-break formatting, "SPART Board" vs "SpartBoard" naming, arrow-fn parens/semicolons) — no substantive content divergence in either file. **ONE NEW MEDIUM finding:** the `admin-widget-config` skill's "How Configs Reach the Widget" section documents only the `SpecialistSchedule`-style pattern (widget/settings component reads `featurePermissions` directly via `useAuth()`), but the codebase's actual dominant mechanism — confirmed live via this week's Calendar commit — is a centralized, per-widget-type-validated switch statement, `getAdminBuildingConfig()` in `utils/adminBuildingConfig.ts` (in production since 2026-06-08, ~33 `case` branches including `calendar`, `checklist`, `stations`, `schedule`, `time-tool`, etc.), called from `context/DashboardContext.tsx` (`getAdminBuildingConfigPure`, lines 98, 4382–4395, 4534, 5285) to seed a new widget instance's initial config from admin building defaults. Neither skill mirror mentions `adminBuildingConfig.ts` or `getAdminBuildingConfig` anywhere (`grep` confirmed zero matches in both `.claude/skills` and `.agents/skills`). A developer adding admin building-defaults support for a new widget by following the skill literally would wire up the `ConfigurationModal`/`ConfigurationPanel` (correct) but never add a validated `case` to `getAdminBuildingConfig()`, so the admin-set defaults would save to Firestore but never actually seed a newly-added widget instance. See new Open item below. All 7 pre-existing LOW items remain valid. Total open: 7 LOW + 1 new MEDIUM._

### MEDIUM `spart-widget-admin-config` does not document the centralized `getAdminBuildingConfig()` seeding mechanism in `utils/adminBuildingConfig.ts`

- **Detected:** 2026-08-25
- **File:** `.claude/skills/admin-widget-config/SKILL.md` (+ `.agents/skills/admin-widget-config/SKILL.md`) — "How Configs Reach the Widget" section
- **Detail:** The skill's "How Configs Reach the Widget" section presents the `SpecialistSchedule/Settings.tsx`-style direct `featurePermissions.find(...)` read (inside the widget component itself) as the working example for how admin building defaults reach a new widget instance. In practice, that per-widget in-component read is now the exception — the dominant, actively-maintained mechanism is `getAdminBuildingConfig()` in `utils/adminBuildingConfig.ts` (present since 2026-06-08), a single exhaustive `switch (type)` that validates and extracts admin `buildingDefaults` per widget type, called from `context/DashboardContext.tsx` (aliased there as `getAdminBuildingConfigPure`) at the moment a widget is added to a dashboard, to merge admin building defaults into the new widget's initial `config`. This week's commit `690f2729` added a `case 'calendar'` to this exact switch to fix exactly the failure mode this gap risks: a widget's `ConfigurationModal` saved rich defaults to Firestore, but with no matching `case`, `getAdminBuildingConfig()` silently returned `{}` for that type and new widget instances never picked up the defaults. Neither skill mirror mentions this file or function at all (confirmed via `grep -r "adminBuildingConfig\|getAdminBuildingConfig"` — zero matches in `.claude/skills` and `.agents/skills`).
- **Fix:** Add a subsection to "How Configs Reach the Widget" documenting `utils/adminBuildingConfig.ts`'s `getAdminBuildingConfig()` as the primary/required mechanism: new widget types with admin building defaults must add a validated `case '<widget-type>':` branch there (type-checking each field the way the existing `numberLine`/`checklist`/`calendar` cases do), in addition to — not instead of — building the `ConfigurationModal`/`ConfigurationPanel` UI. Keep the `SpecialistSchedule`-style in-component `featurePermissions` read documented as a legacy/alternative pattern, but stop presenting it as the primary path, since a widget's `ConfigurationModal` saving to Firestore alone does nothing for new-instance seeding without a matching `case` in this switch.

_2026-08-18: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs). Confirmed both skill pairs live in-repo at `.claude/skills/{new-widget,admin-widget-config}/SKILL.md` and `.agents/skills/{new-widget,admin-widget-config}/SKILL.md`; mirrors remain content-identical (only cosmetic table-formatting/wording differences — "SPART Board" vs "SpartBoard" — same as prior runs, not tracked as a finding). Full codebase-side verification via direct file reads. New dev-paul commits since 2026-08-06 touching filtered paths: `337420d6` (Schedule widget full-title feature, adds `expandActiveItem?: boolean` to `ScheduleConfig` in `types.ts` — unrelated to the `SpecialistSchedule`/admin-config skill surface) and `1733784f` (a11y retrofit touching `SpecialistSchedule/Settings.tsx` group-heading markup only — no structural/file-layout change). Neither affects any skill-documented pattern. Verified directly: (1) `SpecialistSchedule/` still exactly `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` (+ `Settings.test.tsx`) — no `Appearance.tsx`, matching the skill's reference note. (2) `WidgetRegistry.ts` `lazyNamed()` pattern and all 3 SpecialistSchedule registry entries (Widget/Settings/AppearanceSettings) confirmed correct. (3) `BUILDING_CONFIG_PANELS` still `Partial<Record<string, BuildingConfigPanel>>` (`FeatureConfigurationPanel.tsx:92`) vs skill's `WidgetType | InternalToolType` — mismatch persists. (4) `FeaturePermissionsManager.tsx` exclusion array (lines 971–986) still 13 types, still missing the 7 the skill's example lacks (blooms-taxonomy, catalyst, graphic-organizer, music, pdf, video-activity, work-symbols). (5) `FeatureConfigurationPanel.tsx` bottom exclusion list (lines 684–696) matches the skill's Path B code sample verbatim — no drift there. (6) `QRWidget/Widget.tsx` re-confirmed: still hand-rolled empty-state markup, no `ScaledEmptyState` import — the skill's Reference Implementations table (`.claude/skills/new-widget/SKILL.md:561`) still cites it as "Good empty state usage" — **this item was flagged 2026-07-23 as needing correction but the skill file itself has NOT been edited; the stale reference is still live.** (7) `types.ts` SpecialistSchedule `fontFamily?: string` (line ~4973) confirmed still the sole `string`-typed outlier against the `GlobalFontFamily` majority — item still valid. (8) `BlendingBoardAppearanceSettings` (`WidgetRegistry.ts:426`, `BlendingBoard/Settings.tsx:24`) confirmed present and still undocumented as a suppressor pattern in the skill. (9) All cited admin files spot-checked and confirmed present: `SpecialistScheduleConfigurationModal.tsx`, `ClockConfigurationPanel.tsx`, `ScheduleConfigurationPanel.tsx`, `DiceConfigurationPanel.tsx`. All 7 pre-existing LOW open items remain valid. Zero new findings._

_2026-08-06: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs). Codebase-side verification. No new dev-paul commits since 2026-08-05 affecting skill-relevant files (scheduled-tasks branch carries only `package.json` + `pnpm-lock.yaml` changes from the @hono/node-server dependency override — no FeaturePermissionsManager.tsx, FeatureConfigurationPanel.tsx, WidgetRegistry.ts, types.ts, or admin config panel changes). `SpecialistSchedule/` confirmed still present (SpecialistScheduleWidget.tsx, Settings.tsx, index.ts, utils.ts). `spart-new-widget` skill re-confirmed fully accurate — all 8 registered locations verified correct. `spart-widget-admin-config` skill: all 7 LOW open items remain valid (BUILDING_CONFIG_PANELS type annotation mismatch, exclusion array example missing 7 types, secondary exclusion gate undocumented, QRWidget empty-state reference stale, and 3 others from prior runs). Zero new findings._

_2026-08-04: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs). Codebase-side verification. New dev-paul commits since 2026-08-03: spike(pronunciation) #2363 and #2365 (spike docs only — no FeaturePermissionsManager.tsx, FeatureConfigurationPanel.tsx, WidgetRegistry.ts, types.ts, or skill file changes). `spart-new-widget` skill re-confirmed fully accurate — all 8 registered locations verified correct, all referenced files confirmed present. `spart-widget-admin-config` skill: `BUILDING_CONFIG_PANELS` declared as `Partial<Record<string, BuildingConfigPanel>>` (FeatureConfigurationPanel.tsx:92) — skill's `WidgetType | InternalToolType` annotation mismatch LOW item still valid. Exclusion array example still missing 7 types (blooms-taxonomy, catalyst, graphic-organizer, music, pdf, video-activity, work-symbols) — LOW item still valid. Secondary exclusion gate undocumented — LOW item still valid. All 7 LOW open items remain valid. Zero new findings._

_2026-07-30: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs). Codebase-side verification via sub-agent. No new dev-paul commits since 2026-07-29. Key findings: (1) `spart-new-widget` — FULLY ACCURATE. All referenced file paths confirmed present; `lazyNamed()` pattern and all 8-location registration table verified correct. `WidgetRegistry.ts` matches skill documentation exactly. (2) `spart-widget-admin-config` — ONE NEW LOW item: `BUILDING_CONFIG_PANELS` type annotation in the skill shows `Partial<Record<WidgetType | InternalToolType, BuildingConfigPanel>>` but `components/admin/FeatureConfigurationPanel.tsx:92` declares `Partial<Record<string, BuildingConfigPanel>>`. The exclusion array missing-7-types issue is already tracked as an existing LOW open item (confirmed still valid). All 6 pre-existing LOW open items remain valid. Total open: 7 LOW._

_2026-07-23: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs). Codebase-side verification via sub-agent. No new dev-paul commits since 2026-07-21 (rebase not performed). Key findings: (1) `ScheduleConfigurationPanel.tsx` — direct bash verification confirmed the file EXISTS at `components/admin/ScheduleConfigurationPanel.tsx` (and its test `ScheduleConfigurationPanel.test.tsx`). The 2026-07-21 directory scan was incorrect. LOW item moved to Completed (file is present; skill reference is valid). (2) NEW LOW: `spart-new-widget` skill cites `QRWidget/Widget.tsx` as a "Good empty state usage" reference, but QRWidget/Widget.tsx does NOT use `ScaledEmptyState` — it uses hand-rolled empty-state markup. Better reference implementations include `UrlWidget/Widget.tsx`, `Checklist/Widget.tsx`, and `RecessGear/Widget.tsx`. Remaining open: 6 LOWs._

_2026-07-21: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs). Codebase-side verification via sub-agent: new dev-paul commits since 2026-07-20 (absorbed via rebase): fix(activity-wall) photo-grid rowHeight cqmin fix (#2253 — Widget.tsx only, no skill-relevant files). Full checklist verification: all 8 required files (types.ts, config/tools.ts, config/widgetDefaults.ts, config/widgetGradeLevels.ts, components/widgets/WidgetRegistry.ts, components/admin/, components/admin/FeaturePermissionsManager.tsx) confirmed present. `SpecialistSchedule/` still has `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` — reference note from 2026-07-09 accurate. `lazyNamed()` convention correct in WidgetRegistry.ts. All 6 reference widget implementations confirmed present. ONE NEW LOW item: `spart-widget-admin-config` Path B example list references `ScheduleConfigurationPanel.tsx` — no file by that name exists in `components/admin/` (directory scan confirms only `ClockConfigurationPanel.tsx` and `DiceConfigurationPanel.tsx` among the examples; `ScheduleConfigurationPanel.tsx` is absent). All 4 pre-existing LOW items remain valid. Total open: 5 LOW._

_2026-07-16: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs). Codebase-side verifications performed against new dev-paul commits since 2026-07-09: fix(rules) moderation-bypass (#2215 — firestore.rules + test), fix(i18n) ES Comunidad (#2214 — locales), fix(classroomAddon) dedup question ids (#2213 — classroomAddon component), perf(Sidebar) code-split AdminSettings (#2212 — Sidebar.tsx), fix(GuidedLearning) folder badge (#2211 — GuidedLearningManager.tsx), docs(unifier) run 34 (#2209 — docs), D4 ESLint regex widen (#2208 — eslint.config.js), Retrofit RandomSettings SettingsLabel (#2207 — RandomSettings.tsx back-face), QuizManager unify-empty-states (#2206 — QuizManager.tsx). None of these touch FeaturePermissionsManager.tsx, FeatureConfigurationPanel.tsx, SpecialistSchedule/, WidgetRegistry.ts, types.ts, or any skill file. Verified: (1) `SpecialistSchedule/` still contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` — no `Appearance.tsx`; reference note added 2026-07-09 in both skill mirrors remains accurate. (2) `FeaturePermissionsManager.tsx` exclusion array still 13 hardcoded types — LOW item about exclusion array example remains valid. (3) `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` (line 92) still present and growing — LOW secondary-exclusion-gate item remains valid. (4) `lazyNamed()` convention confirmed correct across WidgetRegistry.ts. (5) RandomSettings SettingsLabel retrofit is consistent with the `SettingsLabel` group-heading pattern documented in the skill — no discrepancy. All 5 pre-existing LOW items remain valid. Zero new items._

_2026-07-09 action: Resolved the MEDIUM `spart-new-widget` SpecialistSchedule `Appearance.tsx` item (see Completed). SpecialistSchedule co-locates both `SpecialistScheduleSettings` and `SpecialistScheduleAppearanceSettings` as named exports in `Settings.tsx` (no separate `Appearance.tsx`); added a reference note documenting this to both skill mirrors and corrected the stale `.agents/` "Building-defaults consumption" row (`SpecialistSchedule/Widget.tsx` → `SpecialistScheduleWidget.tsx`). Documentation-only; PR opened against dev-paul. Remaining open: 5 LOW._

_2026-07-09: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs). Codebase-side verifications performed: (1) `SpecialistSchedule/` contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` — still no `Appearance.tsx`; MEDIUM item about Appearance.tsx documentation still valid. (2) `FeaturePermissionsManager.tsx` exclusion array (lines 972–987) still contains the same 13 hardcoded types as 2026-07-07 — LOW item about exclusion array example still valid. (3) `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` still present (line 92) — LOW secondary-exclusion-gate item still valid. (4) `lazyNamed()` convention confirmed correct across WidgetRegistry.ts (no changes to registry in new commits). New commits since 2026-07-07 (fix(audio) #2159, fix(announcements) #2158, fix(import-wizard) #2160, fix(privacy) #2161, fix(i18n) #2162, D3 LunchCount SubmitReportModal→SettingsLabel #2164, fix(css-scaling) #2166): none of these touch FeaturePermissionsManager, FeatureConfigurationPanel, SpecialistSchedule, WidgetRegistry, or any skill-checklist file. D3 LunchCount change (SubmitReportModal→SettingsLabel) is consistent with shared components patterns documented in the skill — no discrepancy. All 6 open items (1 MEDIUM + 5 LOW) remain valid._

_2026-07-07 action: Resolved the HIGH `WIDGET_SCALING_CONFIG` item (see Completed). On inspection the skill's Step 4 and 8-location table already **listed** `WIDGET_SCALING_CONFIG` — the finding's "checklist omits it" framing was inaccurate. The real defect was a **factually wrong consequence** in the Common Mistakes table: "Widget won't render — falls to `DEFAULT_SCALING_CONFIG` silently." That runtime-fallback behavior belongs to the `Partial` maps (`WIDGET_COMPONENTS` et al.); `WIDGET_SCALING_CONFIG` is an exhaustive `Record<WidgetType, ScalingConfig>` (verified `WidgetRegistry.ts:566`), so a missing entry is a `pnpm type-check` compile error and never reaches the `?? DEFAULT_SCALING_CONFIG` runtime fallback at `WidgetRenderer.tsx:306`. Corrected the Common Mistakes row and added an exhaustiveness callout in Step 4 across both mirrors (`.claude/` + `.agents/`). File-recency check passed: both skill files last touched at 1b27ab81 — outside the last 5 branch commits. Documentation-only; PR opened against dev-paul. Remaining open: 1 MEDIUM + 5 LOW._

_2026-07-07: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — consistent with all prior runs except 2026-05-19 and 2026-06-23). Full codebase-side verification performed. Five new findings detected (1 HIGH, 1 MEDIUM, 3 LOW) — see new open items below. Existing 2 LOW items (exclusion array example, secondary exclusion gate) re-confirmed still valid: `FeaturePermissionsManager.tsx` exclusion array and `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` both unchanged since 2026-06-30. New commits since 2026-06-30 (fix(review): a11y group labels, matching dup key, comment — touches Settings/Widget.tsx for grouping UI only) do not affect any skill-relevant patterns. Total open: 2 pre-existing LOW + 5 new items = 7 open._

_2026-06-30: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — same as prior runs). Codebase-side verifications performed: (1) `SpecialistSchedule/` confirmed: contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` — directory structure unchanged. (2) `FeaturePermissionsManager.tsx` exclusion array still contains the same 13 hardcoded types as 2026-06-23 — LOW item #1 remains valid. (3) `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` still present and growing — LOW item #2 about undocumented secondary exclusion gate remains valid. (4) `lazyNamed()` convention confirmed correct across WidgetRegistry.ts. (5) `getAdminBuildingConfig` confirmed present in codebase. `SchemaDrivenConfigurationPanel` does not exist — codebase uses explicit modal pattern (not a skill-freshness tracking item). New commits since 2026-06-23 (fix(analytics), pr-review, docs(unifier), fix(widgets), fix(layout), fix(functions), fix(state)) do not affect any skill-relevant patterns. 2 LOW open items remain valid._

_2026-06-23: Skill files accessible at `/mnt/skills/user/` in this environment. Both skill files read and verified against codebase after merging dev-paul (80 files changed). Codebase-side verifications: (1) `SpecialistSchedule/` confirmed: contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` — still no `Widget.tsx`; MEDIUM `SpecialistScheduleSettings.tsx` item remains valid. (2) `FeaturePermissionsManager.tsx` exclusion array still contains the same 13 hardcoded types (lines 968–980) — LOW item #1 remains valid. (3) `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` still growing — LOW item #2 about undocumented secondary exclusion gate remains valid. (4) `lazyNamed()` convention confirmed correct. (5) `config/tools.ts`, `config/widgetDefaults.ts`, `config/widgetGradeLevels.ts`, `components/widgets/WidgetRegistry.ts` all match skill checklist steps — patterns stable. (6) Admin config path matches actual hierarchy. (7) No stale paths or non-existent file references found in either skill beyond the already-tracked items. New commits since 2026-06-16 (external-availability, OAuth scopes, PLC meeting board, import-path unification) do not affect any skill-relevant patterns. All 3 open items remain valid._

_2026-06-16: Skill files NOT accessible at `/mnt/skills/user/` in this environment (path does not exist — same as all prior runs). Codebase-side verifications performed: (1) `SpecialistSchedule/` still contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` — `Widget.tsx` still absent and `SpecialistScheduleSettings.tsx` still absent; MEDIUM item about non-existent references remains valid. (2) `FeaturePermissionsManager.tsx` exclusion array (lines 968–980) still contains the same 13 hardcoded types as 2026-06-09: blooms-taxonomy, calendar, catalyst, graphic-organizer, instructionalRoutines, miniApp, music, pdf, specialist-schedule, starter-pack, stickers, video-activity, work-symbols — LOW item #1 remains valid. (3) `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` confirmed still present and growing — LOW item #2 about undocumented secondary exclusion gate remains valid. (4) `lazyNamed()` convention confirmed correct across WidgetRegistry.ts. New commits since 2026-06-09 do not affect any of these (only touched random/library rename guard, migration, i18n, LTI). All 3 open items remain valid._

_2026-06-09: Skill files NOT accessible at `/mnt/skills/user/` in this environment. Codebase-side verifications performed: (1) `SpecialistSchedule/` still contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` — `Widget.tsx` still absent; MEDIUM item about non-existent `SpecialistScheduleSettings.tsx` remains valid. (2) `FeaturePermissionsManager.tsx` exclusion array (lines 941–953) still contains the same 13 hardcoded types as 2026-06-02: blooms-taxonomy, calendar, catalyst, graphic-organizer, instructionalRoutines, miniApp, music, pdf, specialist-schedule, starter-pack, stickers, video-activity, work-symbols — LOW item #1 remains valid. (3) `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` now has further entries beyond 2026-06-02: confirmed checklist, blending-board, concept-web, first-5, need-do-put-then, stations all present (39+ entries) — LOW item #2 about undocumented secondary exclusion gate remains valid and increasingly stale as the panel map grows. (4) lazyNamed() convention confirmed correct across WidgetRegistry.ts. (5) `config/widgetAppearance.ts` confirmed: contains `TEXT_SIZE_PRESETS`, `SURFACE_COLOR_PRESETS`, `TEXT_COLOR_PRESETS`, `resolveTextPresetMultiplier()`, `presetFromScale()` — centralized appearance config. (6) Admin config naming uses both `*ConfigurationPanel.tsx` (dominant, ~30 files) and `*ConfigurationModal.tsx` (~8 files: BloomsTaxonomy, Calendar, Catalyst, GenericConfiguration, GraphicOrganizer, SpecialistSchedule, plus test files) — mixed naming convention is an existing undocumented inconsistency not yet tracked. All 3 open items remain valid. No new stale references introduced by recent commits._

_2026-06-02: Skill files NOT accessible at `/mnt/skills/user/` in this environment. Codebase-side verifications performed: (1) `SpecialistSchedule/` still contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts` — `Widget.tsx` still absent; MEDIUM item about non-existent `SpecialistScheduleSettings.tsx` remains valid. (2) `FeaturePermissionsManager.tsx` exclusion array (lines ~941–953) still contains the same 13 hardcoded types as 2026-05-26 — LOW item #1 remains valid. (3) `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` now has 38 entries (up from the 2026-05-19 checklist addition; `checklist` confirmed present at line 98, `classes`, `smartNotebook`, `recessGear`, `countdown`, `embed`, `poll`, `qr`, `drawing`, `breathing`, `numberLine`, `mathTools`, `nextUp`, `countdown` all present). The LOW item #2 about undocumented secondary exclusion gate remains valid and more stale as the map grows. (4) lazyNamed() convention confirmed correct across WidgetRegistry.ts. All 3 open items remain valid. No new stale references introduced._

_2026-05-26 action: Fixed MEDIUM `spart-new-widget` reference to non-existent `SpecialistSchedule/Widget.tsx` (PR pending against dev-paul). Item moved to Completed. 3 open items remain (1 MEDIUM, 2 LOW)._

_2026-05-26: Skill files NOT accessible at `/mnt/skills/user/` in this environment. Codebase-side verifications performed: (1) `SpecialistSchedule/` still contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts` — `Widget.tsx` still absent; MEDIUM item #1 remains valid. (2) `FeaturePermissionsManager.tsx` exclusion array (lines 941–953) still contains the same 13 hardcoded types as 2026-05-12/19 — LOW item #1 remains valid. (3) `FeatureConfigurationPanel.tsx` `BUILDING_CONFIG_PANELS` has grown: `checklist` was added since 2026-05-19 (new `ChecklistConfigurationPanel.tsx`). The LOW item #2 about the undocumented secondary exclusion gate remains valid and slightly more stale now that `checklist` is present in the panel map. (4) lazyNamed() convention confirmed correct across WidgetRegistry.ts. All 4 open items remain valid. No new stale references introduced by recent commits._

_2026-05-19: Skill files ARE accessible at `/mnt/skills/user/` in this environment (unlike prior runs at 2026-05-12). Both skill files read and verified. Codebase-side verification confirms: `components/widgets/SpecialistSchedule/` contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, and `index.ts` — there is NO `Widget.tsx`. This means the MEDIUM open item referencing `SpecialistSchedule/Widget.tsx` remains valid. Similarly `SpecialistScheduleSettings.tsx` does not exist (actual path: `components/widgets/SpecialistSchedule/Settings.tsx`) — MEDIUM item #2 remains valid. `FeaturePermissionsManager.tsx` exclusion array (lines 941–953) contains 13 types — LOW item #1 remains valid (array in skill still outdated). `FeatureConfigurationPanel.tsx` secondary exclusion gate undocumented in skill — LOW item #2 remains valid. lazyNamed() convention confirmed correct. No new stale references detected. All 4 open items remain valid._

_2026-05-12: Skill files not accessible at `/mnt/skills/user/` in this audit environment (path does not exist). Codebase-side verifications performed: (1) `SpecialistSchedule/SpecialistScheduleWidget.tsx` still exists; the directory contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, and `index.ts` — Widget.tsx still absent. (2) `FeaturePermissionsManager.tsx` exclusion array (lines 939–953) now contains 13 types: `blooms-taxonomy`, `calendar`, `catalyst`, `graphic-organizer`, `instructionalRoutines`, `miniApp`, `music`, `pdf`, `specialist-schedule`, `starter-pack`, `stickers`, `video-activity`, `work-symbols`. The skill example remains stale. (3) `lazyNamed()` convention confirmed correct across WidgetRegistry.ts. All four open items remain valid._

_2026-05-05: Skill files not accessible at `/mnt/skills/user/` in this audit environment. Codebase-side verifications performed: `SpecialistSchedule/SpecialistScheduleWidget.tsx` still exists (Widget.tsx does not); `FeaturePermissionsManager.tsx` exclusion list still omits the 7 types noted in the LOW item below; `FeatureConfigurationPanel.tsx` secondary exclusion gate still undocumented in skill. `blending-board` was added to `BUILDING_CONFIG_PANELS` in `FeatureConfigurationPanel.tsx` this week — the exclusion-list LOW item is now more stale. All four open items remain valid._

_2026-06-23 action: Fixed MEDIUM `admin-widget-config` reference to non-existent `SpecialistScheduleSettings.tsx`. Both skill copies (`.claude/skills/admin-widget-config/SKILL.md:222` and `.agents/skills/admin-widget-config/SKILL.md:185`) updated to reference the correct path `components/widgets/SpecialistSchedule/Settings.tsx`. Verified the target file exists and reads `featurePermissions` (Settings.tsx:48). File-recency check passed: skill files last touched at 19b6ae40 — outside the last 5 branch commits. Documentation-only change; PR opened against dev-paul. Item moved to Completed. 2 LOW open items remain._

### LOW `spart-widget-admin-config` `BUILDING_CONFIG_PANELS` type annotation in skill uses `WidgetType | InternalToolType` but actual code uses `string`

- **Detected:** 2026-07-30
- **File:** `.claude/skills/admin-widget-config/SKILL.md` — `BUILDING_CONFIG_PANELS` type annotation
- **Detail:** The skill shows `BUILDING_CONFIG_PANELS` typed as `Partial<Record<WidgetType | InternalToolType, BuildingConfigPanel>>`, but the actual declaration in `components/admin/FeatureConfigurationPanel.tsx:92` uses `Partial<Record<string, BuildingConfigPanel>>`. The code uses `string` (a broader type allowing any string key) rather than the union of specific widget type literals. A developer using the skill to add a new widget type to `BUILDING_CONFIG_PANELS` would infer the map is typed to enforce `WidgetType | InternalToolType` membership, when in fact any string key is accepted. The minor discrepancy does not cause a runtime issue, but the skill's type annotation misrepresents the actual interface.
- **Fix:** Update the skill's `BUILDING_CONFIG_PANELS` type annotation from `Partial<Record<WidgetType | InternalToolType, BuildingConfigPanel>>` to `Partial<Record<string, BuildingConfigPanel>>` to match the actual code.

### LOW `spart-new-widget` cites `QRWidget/Widget.tsx` as a "Good empty state usage" reference — but it uses hand-rolled markup, not `ScaledEmptyState`

- **Detected:** 2026-07-23
- **File:** `.claude/skills/new-widget/SKILL.md` — "Reference Implementations" section
- **Detail:** The skill's Reference Implementations table lists `QRWidget/Widget.tsx` under "Good empty state usage." However, `QRWidget/Widget.tsx` does not use the shared `ScaledEmptyState` component — it uses hand-rolled empty-state markup (a custom `div` with inline icon and text). This contradicts CLAUDE.md guidance that widgets should use `ScaledEmptyState`. Better reference implementations for empty state usage include `UrlWidget/Widget.tsx`, `Checklist/Widget.tsx`, and `RecessGear/Widget.tsx`, all of which use `ScaledEmptyState` correctly.
- **Fix:** Update the Reference Implementations table in the skill to replace `QRWidget/Widget.tsx` with a widget that genuinely uses `ScaledEmptyState` — e.g., `UrlWidget/Widget.tsx` or `RecessGear/Widget.tsx`.

### LOW `spart-new-widget` admin config step understates the number of code changes required

- **Detected:** 2026-07-07
- **File:** `.claude/skills/new-widget/SKILL.md` — admin config wiring step
- **Detail:** Adding admin config for a new widget requires two mandatory code changes: (1) create `components/admin/YourWidgetConfigurationPanel.tsx` with the panel component, and (2) add an entry to the `BUILDING_CONFIG_PANELS` partial map in `components/admin/FeatureConfigurationPanel.tsx`. Additionally, for complex widgets (e.g., SpecialistSchedule, Calendar, BloomsTaxonomy, GraphicOrganizer) a dedicated `*ConfigurationModal.tsx` is used instead of the generic modal — this is a third optional code change. If the skill describes admin wiring as a single step (e.g., "create a ConfigurationPanel"), developers may create the panel file but miss registering it in `BUILDING_CONFIG_PANELS`, resulting in the panel never appearing in the admin UI.
- **Fix:** Update the admin config wiring step to list both required changes explicitly: (a) create `YourWidgetConfigurationPanel.tsx`, (b) import it in `FeatureConfigurationPanel.tsx` and register it in `BUILDING_CONFIG_PANELS`. Also add a note that a dedicated `*ConfigurationModal.tsx` is an optional pattern for complex widgets, referencing `BloomsTaxonomyConfigurationModal.tsx` as an example.

### LOW `spart-new-widget` does not document the `BlendingBoardAppearanceSettings` suppressor pattern

- **Detected:** 2026-07-07
- **File:** `.claude/skills/new-widget/SKILL.md` — appearance panel registration step
- **Detail:** `WIDGET_APPEARANCE_COMPONENTS` in `WidgetRegistry.ts` serves two distinct purposes: (1) registering a real appearance panel with style controls, and (2) suppressing the default `UniversalStyleSettings` fallback with a custom notice (e.g., `BlendingBoardAppearanceSettings` shows "centrally managed" — no actual style controls). The default `UniversalStyleSettings` is provided automatically for any widget NOT in `WIDGET_APPEARANCE_COMPONENTS`. If the skill only documents purpose (1), developers might incorrectly add a widget to `WIDGET_APPEARANCE_COMPONENTS` to suppress the default panel (purpose 2) without realizing that's why `BlendingBoard` is there — or conversely, omit the registration because their widget "doesn't need custom appearance" while not knowing the default panel would appear.
- **Fix:** Add a note explaining both use cases: (a) "Register here to replace the default `UniversalStyleSettings` fallback with a custom appearance panel" and (b) "Register here with a custom suppressor component if you need to hide the default panel entirely (see `BlendingBoardAppearanceSettings` for the pattern)." Also note that most new widgets should just rely on the `UniversalStyleSettings` default and omit the registration unless they need custom appearance controls.

### LOW `fontFamily` config field typed inconsistently — `string` vs `GlobalFontFamily` — skill should prescribe `GlobalFontFamily`

- **Detected:** 2026-07-07
- **File:** `types.ts` — widget config interfaces; `.claude/skills/new-widget/SKILL.md` — config interface template
- **Detail:** The `fontFamily` field in widget config interfaces is typed inconsistently across widgets: `specialist-schedule` uses `fontFamily: string`, while `checklist`, `schedule`, `calendar`, and others that have appearance panels use the branded `GlobalFontFamily` type (imported from config/fonts.ts). Plain `string` allows any string value and bypasses the font-picker enforcement; `GlobalFontFamily` is the union of valid font family values and provides type safety for the font dropdown. New widgets should use `GlobalFontFamily` to stay consistent with the majority pattern and enable future font validation.
- **Fix:** Update the `spart-new-widget` skill's config interface template to prescribe `fontFamily?: GlobalFontFamily` (not `string`). Separately, `specialist-schedule`'s `fontFamily: string` field is a pre-existing inconsistency that can be tightened in a future refactor pass.

### LOW `spart-widget-admin-config` exclusion array example is missing 7 widget types

- **Detected:** 2026-04-14
- **File:** `.claude/skills/admin-widget-config/SKILL.md` — Path A wiring code example
- **Detail:** The skill shows this exclusion array in the `FeaturePermissionsManager.tsx` wiring example:
  ```
  !['instructionalRoutines', 'stickers', 'calendar', 'specialist-schedule', 'miniApp', 'starter-pack', 'your-widget-type']
  ```
  The actual array in `components/admin/FeaturePermissionsManager.tsx` (lines 941–953) is significantly longer and includes 7 additional types added since the skill was written: `blooms-taxonomy`, `catalyst`, `graphic-organizer`, `music`, `pdf`, `video-activity`, `work-symbols`. A developer following the example literally would insert their widget type into an outdated list and potentially misunderstand the full scope of widgets with dedicated config modals.
- **Fix:** Update the skill's code example to match the current exclusion array, or replace the hardcoded list in the example with a comment like `// ...existing widget types with dedicated modals...` so the example is not tied to a specific snapshot.

---

## Completed

### HIGH `SpecialistSchedule` — the widget both skills cite as the gold-standard building-defaults reference — was migrated to schema-driven settings this week; the co-located Settings+Appearance callout and the `featurePermissions`-read example both point at `Settings.tsx`, which no longer exists

- **Detected:** 2026-09-22
- **Completed:** 2026-09-22
- **File:** `.claude/skills/new-widget/SKILL.md` — "Reference Implementations" table (line 600) and the "SpecialistSchedule file layout (reference note)" callout (lines 605–617); `.claude/skills/admin-widget-config/SKILL.md` — "How Configs Reach the Widget" section (line 222). Identical stale content confirmed in both `.agents/skills/` mirrors (`.agents/skills/new-widget/SKILL.md:523,535–546`; `.agents/skills/admin-widget-config/SKILL.md:185`).
- **Detail:** Commit `e0dd38d` ("feat(settings): migrate next five widget drawers (#3171)", merged 2026-09-19) deleted `components/widgets/SpecialistSchedule/Settings.tsx` (731 lines) and replaced it with `settings.schema.ts` + `settingsFields.tsx`, adding a `specialist-schedule` entry to `WIDGET_SETTINGS_SCHEMAS` in `WidgetRegistry.ts` with zero remaining entries in `WIDGET_SETTINGS_COMPONENTS`/`WIDGET_APPEARANCE_COMPONENTS`. `SpecialistSchedule/index.ts` now reads only `export * from './SpecialistScheduleWidget';` — the `export * from './Settings';` line is gone. The named exports `SpecialistScheduleSettings` and `SpecialistScheduleAppearanceSettings`, which `new-widget/SKILL.md`'s reference-note callout described in detail as "co-located in `Settings.tsx`," no longer exist anywhere in the codebase. Separately, `admin-widget-config/SKILL.md`'s "How Configs Reach the Widget" section cited `components/widgets/SpecialistSchedule/Settings.tsx` as the working example of reading `featurePermissions` to seed a widget's config — that read pattern is intact and unchanged, but it now lives in `settingsFields.tsx` (lines 34–36) and, redundantly, in `SpecialistScheduleWidget.tsx` (lines 54–64); the cited file simply didn't exist anymore. This is notable beyond an ordinary stale-path bug: SpecialistSchedule had been reverified as a stable, non-schema "exception, don't imitate" reference in essentially every one of this journal's ~20+ weekly cycles since 2026-04-14 specifically because it was a hand-authored widget unlikely to be swept into a migration wave — this week's wave proved that assumption is no longer safe for any widget in the codebase.
- **Resolution (2026-09-22):** In `new-widget/SKILL.md` (both mirrors), rewrote the "SpecialistSchedule file layout" callout to list the current files (`SpecialistScheduleWidget.tsx`, `index.ts`, `utils.ts`, `settings.schema.ts`, `settingsFields.tsx`) and dropped the claim of a co-located non-schema `*AppearanceSettings` export, noting instead that settings are registered via `WIDGET_SETTINGS_SCHEMAS`, not `lazyNamed()`. Left the "Building-defaults consumption" table row (`SpecialistScheduleWidget.tsx`) unchanged since that front-face file is unaffected. In `admin-widget-config/SKILL.md` (both mirrors), updated the "How Configs Reach the Widget" file reference from `SpecialistSchedule/Settings.tsx` to `SpecialistSchedule/settingsFields.tsx`, which has the identical `featurePermissions` read. Did not change the per-widget-reference pattern itself (the journal's suggestion to consider dropping individual widget citations entirely, given this is the third widget in ~2 months to be picked as a "stable" reference and then migrated out from under the skill) — left as a standing consideration, not actioned this pass, since it's a larger editorial decision beyond this one broken reference. Documentation-only, zero behavioral impact; `prettier --check` clean on all four files. PR opened against dev-paul.

### HIGH `.claude/skills/new-widget/SKILL.md` cites `ClockWidget`/`LunchCount` as Settings+Appearance reference examples — both were fully migrated to schema-only settings this week, so no `Settings.tsx` file exists for either anymore

- **Detected:** 2026-09-08
- **Completed:** 2026-09-08
- **File:** `.claude/skills/new-widget/SKILL.md` — "Reference Implementations" table (Settings + Appearance split example) and Step 3.5 (claims "the schema and the legacy panel coexist during the migration" for all widgets); mirrored in `.agents/skills/new-widget/SKILL.md`.
- **Detail:** The settings-drawer wave 2 (PR #2896, merged 2026-09-06) fully migrated `clock`, `timer` (`time-tool`), `text`, `embed`, and `lunchCount` onto the schema-driven settings drawer. For `clock` and `lunchCount` specifically — both cited by the skill as the "Settings + Appearance split" / "list with cqmin rows" reference implementations — no `Settings.tsx` file exists anymore, and `WidgetRegistry.ts` has no `WIDGET_SETTINGS_COMPONENTS`/`WIDGET_APPEARANCE_COMPONENTS` entries for `clock` at all (only a `WIDGET_SETTINGS_SCHEMAS` entry pointing to `ClockWidget/settings.schema.ts`). A developer following the skill's reference table to see how a legacy Settings+Appearance panel is structured would find no such file for either cited example. Step 3.5's "coexist during migration" framing is also now stale for these 5 widgets specifically — they've fully cut over, not coexisting.
- **Resolution (2026-09-08):** Swapped the "Settings + Appearance split" reference row from `ClockWidget/Settings.tsx` to `Checklist/Settings.tsx` (confirmed to have co-located `ChecklistSettings`/`ChecklistAppearanceSettings` exports and no schema-drawer migration) in both mirrors. Added a caveat noting `clock`/`time-tool`/`text`/`embed`/`lunchCount` are schema-only with no `Settings.tsx`/`WIDGET_APPEARANCE_COMPONENTS` entry — in Step 3.5 for `.claude/` (which has that section) and as a standalone note under the Reference Implementations table for `.agents/` (which lacks Step 3.5 — a larger, separately-tracked mirror-drift gap left untouched). Left the "Clean scaling (hero number)" and "List with cqmin rows" rows unchanged since they cite the still-valid `Widget.tsx` front-face files, not the removed `Settings.tsx`. Documentation-only; `prettier --check` clean on both files. PR opened against dev-paul.

### LOW `spart-widget-admin-config` does not document hardcoded exclusion list in `FeatureConfigurationPanel.tsx`

- **Detected:** 2026-04-14
- **Completed:** 2026-09-01
- **File:** `.claude/skills/admin-widget-config/SKILL.md` — Path B description
- **Detail:** The item claimed the skill's Path B description never mentioned the secondary hardcoded exclusion list in `components/admin/FeatureConfigurationPanel.tsx` (lines 684–696: `['calendar', 'expectations', 'guided-learning', 'instructionalRoutines', 'miniApp', 'quiz', 'stickers', 'talking-tool', 'weather', 'webcam', ...Object.keys(BUILDING_CONFIG_PANELS)]`). On 2026-09-01 re-verification, both the repo file and the fetched `spart-widget-admin-config` copy now contain a dedicated "**Wiring location 3:**" subsection under Path B that documents exactly this exclusion list, shows the matching code shape, and notes that registering in `BUILDING_CONFIG_PANELS` is usually sufficient since it's covered via `...Object.keys(BUILDING_CONFIG_PANELS)`. Git history (limited to this shallow clone, back to 2026-06-19) shows this subsection already present at every commit touching the file — the underlying documentation gap was resolved before this journal's tracking could confirm it was gone; prior weekly audits kept re-verifying only the exclusion array's literal contents, not whether the "not documented" framing itself was still accurate.
- **Resolution (2026-09-01):** No skill edit needed — the described documentation gap no longer exists in either skill copy. Resolved outside journal workflow.

### LOW `spart-widget-admin-config` Path B example references `ScheduleConfigurationPanel.tsx`

- **Detected:** 2026-07-21
- **Completed:** 2026-07-23
- **File:** `.claude/skills/admin-widget-config/SKILL.md` — Path B example list
- **Detail:** The 2026-07-21 directory scan reported no file named `ScheduleConfigurationPanel.tsx` in `components/admin/`, concluding the skill reference was stale. Bash verification on 2026-07-23 confirmed the file DOES exist at `components/admin/ScheduleConfigurationPanel.tsx` (and its test `components/admin/ScheduleConfigurationPanel.test.tsx`). The 2026-07-21 scan result was incorrect.
- **Resolution (2026-07-23):** `components/admin/ScheduleConfigurationPanel.tsx` confirmed to exist — skill reference is valid. No fix needed.

### MEDIUM `spart-new-widget` documents SpecialistSchedule as having a separate `Appearance.tsx` — actual pattern co-locates appearance in `Settings.tsx`

- **Detected:** 2026-07-07
- **Completed:** 2026-07-09
- **File:** `.claude/skills/new-widget/SKILL.md` + `.agents/skills/new-widget/SKILL.md` — "Reference Implementations" section
- **Detail:** `components/widgets/SpecialistSchedule/` contains `SpecialistScheduleWidget.tsx`, `Settings.tsx`, `index.ts`, `utils.ts`. There is NO separate `Appearance.tsx`. Both `SpecialistScheduleSettings` (Settings.tsx:44) and `SpecialistScheduleAppearanceSettings` (Settings.tsx:675) are named exports co-located in `Settings.tsx`; `index.ts` re-exports both via `export * from './SpecialistScheduleWidget'; export * from './Settings';`, so the WidgetRegistry `lazyNamed(() => import('./SpecialistSchedule'), 'SpecialistScheduleAppearanceSettings')` (WidgetRegistry.ts:482–484) resolves correctly.
- **Resolution (2026-07-09):** Added a "SpecialistSchedule file layout (reference note)" callout after the Reference Implementations table in both skill mirrors documenting the co-located appearance-panel pattern and that co-location in `Settings.tsx` is a valid alternative to a dedicated `Appearance.tsx`. Also corrected the stale `.agents/` "Building-defaults consumption" row (was `SpecialistSchedule/Widget.tsx`, now `SpecialistScheduleWidget.tsx` with the "exception — do not imitate" note, matching the `.claude/` mirror). File-recency check passed: both skill files last touched at `12f0ab0f` (16th commit back — outside the last 5). Documentation-only change; PR opened against dev-paul.

### HIGH `spart-new-widget` misstates the consequence of skipping `WIDGET_SCALING_CONFIG`

- **Detected:** 2026-07-07
- **Completed:** 2026-07-07
- **File:** `.claude/skills/new-widget/SKILL.md` + `.agents/skills/new-widget/SKILL.md` — Step 4 + "Common Mistakes" table
- **Detail:** The item was originally logged as "checklist omits `WIDGET_SCALING_CONFIG`," but the skill's Step 4 and 8-location table already listed the map. The genuine defect was an incorrect consequence in the Common Mistakes table: it claimed skipping the entry means "Widget won't render — falls to `DEFAULT_SCALING_CONFIG` silently." `WIDGET_SCALING_CONFIG` is typed `Record<WidgetType, ScalingConfig>` (exhaustive; `WidgetRegistry.ts:566`), unlike the `Partial<Record<…>>` component maps (`WIDGET_COMPONENTS:86`, `WIDGET_SETTINGS_COMPONENTS:265`, `WIDGET_APPEARANCE_COMPONENTS:420`). A missing entry therefore fails `pnpm type-check` and blocks CI — the compiler rejects it before the runtime `?? DEFAULT_SCALING_CONFIG` fallback (`WidgetRenderer.tsx:306`) can fire. The stale "silent runtime fallback" wording would mislead a developer into hunting a rendering bug instead of reading the type error.
- **Resolution:** Corrected the Common Mistakes row to describe the actual consequence (`pnpm type-check` failure / CI block) and added an exhaustiveness callout in Step 4 distinguishing the exhaustive `Record` maps (`WIDGET_SCALING_CONFIG`, `WIDGET_DEFAULTS`, `WIDGET_GRADE_LEVELS`) from the `Partial` component maps. Applied identically to both mirrors (`.claude/` + `.agents/`). Verified type against `WidgetRegistry.ts` and the runtime fallback in `WidgetRenderer.tsx`. Documentation-only change.

### MEDIUM `spart-widget-admin-config` references non-existent `SpecialistScheduleSettings.tsx`

- **Detected:** 2026-04-14
- **Completed:** 2026-06-23
- **File:** `.claude/skills/admin-widget-config/SKILL.md` + `.agents/skills/admin-widget-config/SKILL.md` — "How Configs Reach the Widget" section
- **Detail:** The skill referenced `SpecialistScheduleSettings.tsx` as a working example of how to read `featurePermissions` in a widget settings component. No file named `SpecialistScheduleSettings.tsx` exists anywhere in the codebase. The correct path is `components/widgets/SpecialistSchedule/Settings.tsx`. The pattern itself is valid and the code works, but a developer following the skill literally could not find the referenced file.
- **Resolution:** Updated both skill copies (`.claude/skills/admin-widget-config/SKILL.md` and the mirrored `.agents/skills/admin-widget-config/SKILL.md`) to reference the correct path `components/widgets/SpecialistSchedule/Settings.tsx`. Confirmed the target exists and reads `featurePermissions` at `Settings.tsx:48`. Documentation-only change.

### MEDIUM `spart-new-widget` references non-existent `SpecialistSchedule/Widget.tsx`

- **Detected:** 2026-04-14
- **Completed:** 2026-05-26
- **File:** `.claude/skills/new-widget/SKILL.md` — "Building-defaults consumption" reference table row
- **Detail:** The skill's gold-standard reference table listed `components/widgets/SpecialistSchedule/Widget.tsx` as the "Building-defaults consumption" example. That file does not exist. The actual component is `components/widgets/SpecialistSchedule/SpecialistScheduleWidget.tsx` — SpecialistSchedule is the only widget that uses a named-file pattern inside its subdirectory instead of the standard `Widget.tsx` convention.
- **Resolution:** Updated the reference table row to point to the correct path `components/widgets/SpecialistSchedule/SpecialistScheduleWidget.tsx` and added an inline parenthetical noting that this is the sole exception to the `Widget.tsx` naming convention and should not be imitated. Documentation-only change.
