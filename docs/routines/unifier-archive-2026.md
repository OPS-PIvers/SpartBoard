# SpartBoard Unifier — Archived Run Log (bootstrap through 2026-08-02)

This file holds older Run Log entries moved out of `docs/routines/unifier.md` to keep the live
memory doc a manageable size (proposed in runs 71-76's Notes, executed run 77). It covers the
bootstrap run (2026-05-26) through 2026-08-02 (~runs 1-46). The Canon, Intentional Exceptions,
and Inconsistency Backlog sections all remain in the live doc — only these narrative Run Log rows
moved. Nothing here is authoritative for current canon; consult the live doc for that.

---

| Date | Branch | Dimension | Action | PR |
| ---- | ------ | --------- | ------ | -- |
| 2026-08-02 | — | D5 Toast Architecture | Aligned — fresh sweep of `components/admin/`, `lti/`, `classroomAddon/` config components, all hits matched catalogued exceptions or components already converted to canon in prior runs; found stale exceptions-table rows for D5-E6/E8/E10 (all 3 were actually converted to `addToast()` in runs 42/45/46 but the exceptions table was never updated) — fixed in this run's doc pass, rows removed | — |
| 2026-07-31 | — | D1 Empty States | Aligned — full fresh sweep (git log since 2026-07-28 showed almost no drift; repeated grep across `flex flex-col items-center justify-center` blocks and icon+"No X" text patterns), all hits resolved to already-catalogued exceptions or already-shipped fixes. Surfaced one out-of-scope near-miss (`VideoActivityEditor.tsx:146` "Paste a YouTube URL above", editor-modal placeholder) formalized as new exception D1-E25 to short-circuit future sweeps. Two VISUAL-RISK backlog rows had drifted line numbers, corrected in this run's doc pass (ActivityWall 1890→1878, SmartNotebook Library 163→173). |
| 2026-07-31 | — | D2 Brand Colors | Aligned — 39th consecutive clean run. Fresh hex + decimal-RGB + uppercase-hex sweep, every hit an already-known exception, already-shipped fix, the `DEFAULT_GLOBAL_STYLE` source-of-truth itself, a test file, or an outbound Cloud Functions email template (out of scope). `Dock.tsx:1586` NEEDS REVIEW re-confirmed present and untouched — now 17 consecutive runs unresolved since run 30, escalated to the human operator again in this run's summary notification. |
| 2026-07-31 | — | D3 Settings Labels | Aligned — repo-wide exact/permutation-tolerant grep for the canonical string plus its `text-xs`-sibling near-miss form found only the two already-catalogued open items (D3-E11 student form label, run-44 LunchCount NEEDS REVIEW) and reaffirmed D3-E16 (verified the `text-xs`/`text-xxs` size mismatch itself, matching the doc's own warning). Rejected one out-of-scope near-miss (`math-tools/PlaceValueTool.tsx:221`, front-face widget content covered by the separate daily CSS-scaling journal, not D3). Spot-checked the closed group-heading retrofit series (run 41) and confirmed the closure holds — one borderline case in `SpecialistSchedule/Settings.tsx:348` judged a defensible 1:1 label/input pairing, not converted, not added as a formal exception (not confident enough either way). |
| 2026-07-31 | — | D4 Import Paths | Aligned — repo-wide `../` and `../../` relative-import sweep (40 single-level hits, all resolved to documented gray zones/accepted conventions; zero two-level escapes) plus an ESLint-enforcement-accuracy check of all three `no-restricted-imports` rules (plc/, widgets/math-tools/, admin/Organization/views/) against their live canonical sources, including a negative-control test (planted a known-bad import, confirmed the rule still fires, reverted cleanly). No drift in either the source or the enforcement layer. |
| 2026-07-31 | `nightly/unify-d5-toast-architecture-2026-07-31` | D5 Toast Arch | Shipped — `BloomsTaxonomyConfigurationModal.tsx`, 3rd instance of the run-42/44 recurring shape (local `message` useState + `<Toast>` rendered as a JSX sibling outside `</Modal>`) → `addToast()` via `useDashboard()`; `DashboardProvider` reachable via `FeaturePermissionsManager.tsx`. Subagent's own backgrounded eslint check got stuck (orphaned process found and killed during worktree inspection); orchestrator independently ran tsc/eslint/prettier + full `pnpm run test` (587/587, 7108/7108, identical to baseline) + `pnpm run build`, all green, before committing and opening PR #2319 (draft). |
| 2026-07-30 | — | D1 Empty States | Aligned — full sweep of `components/widgets/` (~65 widget dirs). Correctly ruled out the no-icon exception family (ExpectationsWidget, MusicWidget `EmptyHint`/`PersonalSpotifyLibraryTab`), a filtered-search state (MathTools), and modal/editor-overlay states outside the container-query model (`WrittenResponseGrader.tsx` `EmptyStateShell`, QuizEditor/GuidedLearningEditor/VideoActivityEditor split-pane placeholders). All previously-blocked items (ActivityWall:1878, DrawingWidget, MiniApp modals, ScreenCaptureModal, SmartNotebook Library.tsx) re-confirmed untouched. New backlog candidate noted: `QuizWidget/components/WrittenResponseGrader.tsx:661` `EmptyStateShell` has a real icon but is a `fixed inset-0` modal overlay — same blocked class as the other modal items, not converted. Clean tree, no commit. | — |
| 2026-07-30 | — | D2 Brand Colors | Aligned — 39th consecutive clean run. Fresh hex + decimal-RGB sweep found nothing new; every hit an already-known exception. `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged — now unresolved for **17 consecutive runs** since run 30; still needs a human decision (flagged again in this run's summary notification). Clean tree, no commit. | — |
| 2026-07-30 | `nightly/unify-settings-labels-2026-07-30` | D3 Settings Labels | Shipped — extended the run-41-"closed" group-heading retrofit series to 2 new files outside the originally-audited set: `Checklist/Settings.tsx` "List Source", `MathToolInstance/Settings.tsx` "Tool Type"/"Mode"/"Units Displayed" (4 instances) → `as="span"` + `role="group"`/`aria-labelledby`, `widget.id`-scoped ids. Zero visual delta confirmed against `SettingsLabel.tsx` source by both subagent and orchestrator. validate + build green (587/587 root + 40/40 functions) — PR #2308 (draft). | #2308 |
| 2026-07-30 | — | D4 Import Paths | Aligned — plc `no-restricted-imports` regex re-diffed against current `PLC_SECTIONS` (10 ids), still in sync. Fresh repo-wide sweep including every `components/` subdirectory and recently-added files (since 2026-07-01) found zero genuine new cross-directory relative imports. Clean tree, no commit. | — |
| 2026-07-30 | `nightly/unify-toast-arch-2026-07-30` | D5 Toast Arch | Shipped — 3rd instance of the run-42/44 shape: `StarterPackConfigurationModal.tsx` local `message` useState + Fragment-wrapped `<Toast>` sibling → `addToast()` via `useDashboard()`; Fragment wrapper removed. Orchestrator independently re-diffed and grepped for leftover Toast/message references (none). validate + build green (587/587 root, 7108 tests + 40/40 functions) — PR #2307 (draft). | #2307 |
| 2026-07-28 | `nightly/unify-d1-empty-states-2026-07-28` | D1 Empty States | Shipped — MiniAppManager.tsx `personalEmpty`/`globalEmpty` dashed-badge+icon empty states → `ScaledEmptyState` with color overrides (light-surface manager, dark-tone defaults would be low-contrast); dashed badge dropped per QuizManager PR #2284 precedent; sibling text-only `activeEmpty`/`archiveEmpty` correctly left alone, formalized as new exception D1-E24. validate + build green — PR #2298 (draft). | #2298 |
| 2026-07-28 | — | D2 Brand Colors | Aligned — 38th consecutive clean run. Fresh full-repo sweep (hex + decimal-RGB) found nothing new; every hit an already-known exception (including newly-noticed `AnalyticsManager.tsx:884` `accentBg` rgba companion to the existing `#ad2122` KpiCard exception, and 3 user-configurable `themeColor` defaults in `widgetDefaults.ts`/`NextUpConfigurationPanel.tsx`/`migration.ts`, same class as `Countdown.eventColor`). `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged — now unresolved for **16 consecutive runs** since run 30; still needs a human decision. Clean tree, no commit. | — |
| 2026-07-28 | (discarded) | D3 Settings Labels | A subagent converted `TalkingToolConfigurationPanel.tsx:83` (D3-E16) to `SettingsLabel` using the established `className="mb-0"` override pattern, citing Scoreboard/Calendar/MaterialsWidget precedent — but orchestrator review caught a real regression: the original heading is `text-xs` (12px) while `SettingsLabel` hardcodes `text-xxs` (10px, `tailwind.config.js:218`); the `mb-0` override only fixes margin, not font-size. Discarded via `git reset --hard` before shipping; D3-E16 reaffirmed with the size-mismatch reasoning made explicit. Also hit the stuck-background-wait failure mode (own nested `pnpm test`) — recovered via `SendMessage` resume with an explicit synchronous-blocking instruction, completed cleanly on the retry. No PR. | — |
| 2026-07-28 | — | D4 Import Paths | Aligned — plc `no-restricted-imports` regex re-diffed against current `PLC_SECTIONS` (10 ids), still in sync; fresh repo-wide sweep (including previously-unnamed `i18n/`, `locales/`, `types/`, `marketplace-assets/`) found zero genuine new cross-directory relative imports. Clean tree, no commit. | — |
| 2026-07-28 | — | D5 Toast Arch | Aligned — exhaustive sweep of local Toast/message-state patterns repo-wide (43 hits) found nothing new; both run-42/44 fixes (SpecialistScheduleConfigurationModal, CalendarConfigurationModal) re-confirmed holding; one new candidate (`WidgetBuilder/index.tsx` `saveMessage`) checked and confirmed inline-in-body, not global-overlay imitation — left alone. Clean tree, no commit. | — |
| 2026-07-27 | — | D1 Empty States | Aligned — fresh sweep (git log since 2026-07-25 for `components/widgets/`, only 2 unrelated commits found — NumberLine/Settings D3 conversion, SyntaxFramer appearance-tab move — neither touches front-face empty states) plus a full grep sweep (hardcoded icon-size classes, `flex flex-col items-center justify-center` layouts, "get started/to begin/Add a/an" text hints) across `components/widgets/`. Every hit resolved to an already-canonical `ScaledEmptyState` usage or a previously-documented exception (view-only results screens, MusicWidget no-icon EmptyHint family). Standing blocked/VISUAL-RISK items (SmartNotebook Library.tsx:163, ActivityWall:1890, DrawingWidget:1593, MiniApp modals) confirmed untouched, no new technical approach identified. Clean tree, no commit. | — |
| 2026-07-27 | — | D2 Brand Colors | Aligned — 37th consecutive clean run. Fresh full-repo sweep (hex + decimal-RGB) across `components/`, `context/`, `hooks/`, `config/`, `utils/` found nothing new; every hit fell into an already-known exception category. `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged — now unresolved for **15 consecutive runs** since run 30; still needs a human decision (accept as permanent static chrome, or a coordinated `--spart-primary-rgb` re-theme of all 3 Dock brand-blue instances). Also surfaced a stale exceptions-list filename: D2-E8's recorded path `layout/sidebar/StylePanel.tsx` no longer exists — the `DEFAULT_ACCENT_COLOR` constant now resolves only to `components/settingsModal/sections/AppearanceSection.tsx` (same file as D2-E10), likely consolidated in an earlier refactor; corrected in this run's doc pass. Clean tree, no commit. | — |
| 2026-07-27 | `nightly/unify-toast-arch-2026-07-27` | D5 Toast Arch | Shipped — fresh staleness sweep (git log since 2026-07-24 over admin/plc components, plus a repo-wide grep for local toast-state patterns cross-checked against `App.tsx`'s route tree and the known exception list) found one genuine new instance: `CalendarConfigurationModal.tsx`'s local `message` useState + a `<Toast>` rendered as a JSX sibling _outside_ the closing `</Modal>` tag (wrapped in a fragment) — the same "global-overlay imitation" shape as the run-42 `SpecialistScheduleConfigurationModal` fix. `DashboardProvider` reachable (mounted via `FeaturePermissionsManager.tsx`, same as run 42). Converted to `addToast()`; removing the now-unneeded wrapping fragment reindented the JSX body, accounting for most of the diff — orchestrator read the full diff and confirmed no logic/markup change beyond the toast wiring, and independently ran a targeted `eslint --max-warnings 0` on both changed files before opening the PR. Test file updated to mock `useDashboard`. `GlobalPermissionsManager.tsx` (D5-E2) re-confirmed as an existing exception during the same sweep, not new. `pnpm run validate` (585/585 root + 38/38 functions, 7065+721 tests) and `pnpm run build` both green. dev-paul had not moved since worktree creation, no rebase needed. | #2294 (draft)  |
| 2026-07-27 | — | D3 Settings Labels | Aligned — fresh sweep (git log since 2026-07-24 across widget Settings/admin config files, only 3 commits found, all already-known or out-of-scope for D3) plus a repo-wide exact-string + class-token-co-occurrence grep restricted to `components/widgets`/`components/admin`. All 5 hits resolved to already-documented exceptions (RosterModeControl flex-row span, NextUpStudentApp out-of-scope student label) or a previously-decided partial-conversion call (`LunchCount/SubmitReportModal.tsx`'s 3 read-only display spans, PR #2164) — re-confirmed, not reopened, but flagged again as a NEEDS REVIEW backlog row since a product-owner scope call was never formally made. No new genuine instance found; clean tree, no commit. | — |
| 2026-07-27 | — | D4 Import Paths | Aligned — 9th consecutive clean check. Re-diffed all 3 `no-restricted-imports` regexes (`components/plc/**` against current `PLC_SECTIONS` + live directory listing; `components/widgets/**` math-tools rule; `components/admin/Organization/views/**` rule) — no drift in any. Fresh full-repo grep for `'../'`/`'../../'` outside `functions/src` found only previously-catalogued D4-E1/D4-E2 gray-zone instances (all verified pre-existing via `git log -1` per file, none touched after 2026-07-11). Verified enforcement still fires correctly with a targeted `eslint --max-warnings 0` pass over the 3 covered directory trees (clean, zero false positives). No new source directories added since 2026-07-24 requiring coverage. Clean tree, no commit. | — |
| 2026-07-25 | — | D1 Empty States | Aligned — fresh staleness sweep of `components/widgets/` (git-log-recency hint plus a full grep for hand-rolled centered-flex+icon+text patterns and hardcoded icon-size classes). Every hit traced to an already-known category (converted, blocked, or exempted); several new grep hits were confirmed NOT empty states (always-shown upload/drop buttons, action-selection cards, per-card thumbnail placeholders, section/button labels sharing the uppercase-tracking-widest style). Two ambiguous candidates deferred, not converted: `QuizWidget/Widget.tsx:964-1006` view-only monitor notice (no-icon + CTA button, same risk class as the no-icon exception family); `SmartNotebook/components/PageEditorOverlay.tsx:363-380` (an error state, not a missing-data state — arguably outside D1's canon scope entirely). All standing blocked/VISUAL-RISK items reconfirmed unchanged. Clean tree, no commit. | — |
| 2026-07-25 | — | D2 Brand Colors | Aligned — 36th consecutive clean run. Fresh full-repo sweep (hex + decimal-RGB) across the whole tree found nothing new; every hit fell into an already-known exception category (user-configurable defaults, swatch/palette arrays, chart palette entries, gradients, or Cloud Functions email HTML entirely outside the DashboardView CSS-var scope). `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged, correctly left untouched — now unresolved for **13 consecutive runs** since run 30; re-flagged to the human operator in tonight's summary, as this cannot be resolved by any future nightly dispatch without a human decision (accept as permanent static chrome, or scope a coordinated `--spart-primary-rgb` re-theme of all 3 Dock brand-blue instances). Ran full validate anyway per instructions; root unit-test stage hit the sandbox's 10-minute single-call cap twice (Node 22 vs the project's Node 24 requirement adds overhead) but zero failures observed across 1400+ passing markers — all other stages (type-check, lint, format-check, build) green. Clean tree, no commit. | — |
| 2026-07-25 | `nightly/unify-settings-labels-2026-07-25` | D3 Settings Labels | Shipped — resolved the one D3 backlog item left open by run 42's cluster investigation: `NumberLine/Settings.tsx`'s 5 compact add-form labels (`text-xxs font-bold uppercase text-slate-400 mb-1`). Re-investigated per an explicit "bias toward formalizing as an exception over guessing" dispatch framing: an exact-string repo-wide grep found no corroborating reuse anywhere else in the repo, and a structural comparison against sibling compact-add-form files (Schedule, Calendar, RevealGrid Settings) found no matching tier — confirmed genuine accidental drift, not a new deliberate tier. Converted all 5 labels (Marker form: Value/Label; Jump form: Start/End/Label) to `<SettingsLabel htmlFor={id}>` **in one commit**, correctly avoiding the exact "partial conversion creates a within-row visual mismatch" risk run 42 flagged for isolated single-instance conversion. Orchestrator independently read `SettingsLabel.tsx` source to confirm the `htmlFor` pairing is a direct drop-in, and spot-checked eslint/tsc against the changed file directly. `pnpm run validate` (585/585 root + 38/38 functions, 7065+721 tests) and `pnpm run build` both green (subagent ran validate's constituent stages individually due to the sandbox's 10-minute single-call cap). dev-paul had not moved since worktree creation, no rebase needed. | #2279 |
| 2026-07-25 | — | D4 Import Paths | Aligned — 8th consecutive clean check. Re-diffed the `components/plc/**` ESLint regex against both the current `PLC_SECTIONS` array (10 ids, all covered) and the actual `components/plc/` filesystem listing (17 dirs, all covered) — no drift this run. Re-checked the `components/widgets/**` math-tools regex, still correctly scoped. Fresh full-repo grep (not scoped to prior-swept directories) found zero new cross-directory relative-import instances, including in every previously-unswept directory explicitly checked (poll/, landing/, activityWall/, announcements/, backgroundsModal/, boardsModal/, quickAccessModal/, share/, spotify/, remote/, quiz/, videoActivity/, guidedLearning/, dev/) — confirmed the `functions/src` relative imports are correctly out of D4 scope entirely (separate CommonJS project, no `@/` alias configured there). Clean tree, no commit. | — |
| 2026-07-25 | — | D5 Toast Arch | Aligned — fresh repo-wide staleness sweep found no genuine new drift, but surfaced 4 previously-uncatalogued components, all confirmed as legitimate exceptions (not converted) and formalized as D5-E23 (`RequestRolloutPage.tsx` — no DashboardProvider), D5-E24 (`classroomAddon/StudentSpikeRoute.tsx` + `TeacherDiscoveryRoute.tsx` attach flow — no DashboardProvider, same family as E17/E19), D5-E25 (`AssignToClassroomModal.tsx` — inline-in-body), D5-E26 (`Embed/Settings.tsx` — inline contextual feedback + widget-level out-of-scope). Also ruled out a false positive (`UsersView.tsx:1084` is a plain textarea form field, not a toast) and several `fixed bottom/top-` grep hits that are non-notification UI (zoom slider, boards menu, announcement overlay, subs chrome). Standing z-index "renders behind modal" open question re-noted but not acted on, per standing instruction. Clean tree, no commit. | — |
| 2026-07-24 | — | D1 Empty States | Aligned — fresh discovery sweep (git log since 2026-06-15 for recently-touched widget files, plus a full grep for hand-rolled centered-flex+icon+text patterns) found no new hand-rolled empty state outside the already-catalogued exceptions. One new backlog candidate identified but correctly NOT converted: `DrawingWidget/Widget.tsx:1593-1605` "Start drawing" pencil overlay — same `skipScaling: false` container-scaling-model mismatch class as the still-blocked `ActivityWall/Widget.tsx:1890`; likely worth solving together in a future run once a container-scoping fix is designed. Both standing VISUAL-RISK items (SmartNotebook Library, ActivityWall:1890, MiniApp modals) re-confirmed blocked, untouched per explicit instruction. | — |
| 2026-07-24 | — | D2 Brand Colors | Aligned — 35th consecutive clean run. Fresh full-text sweep (hex + decimal-RGB) across `components/`, `context/`, `hooks/`, `config/`, `utils/`; every hit classified into an existing exception category (swatch/palette, chart/data-viz color, or user-configurable default), including a new hit (`MusicWidget/Widget.tsx:348` station-color fallback) correctly recognized as matching the existing `Countdown.eventColor`/`MaterialsWidget.titleColor` exception shape rather than treated as new drift. `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged, correctly left untouched per explicit instruction not to fix in isolation — this item has now sat unresolved since run 30 (12+ runs); flagged to the human operator in this run's summary as genuinely needing a decision. Completed cleanly on first dispatch, no resume needed. | — |
| 2026-07-24 | — | D3 Settings Labels | Resolved a 20+ run-old backlog cluster without shipping code: dispatched specifically to investigate the long-stalled "third tier" `font-bold`-not-`font-black` NEEDS REVIEW items (MinTierSelect, NeedDoPutThenConfigurationPanel, NoteConfigurationPanel, TimeTool/Settings, NumberLine/Settings, assignment-config family, ChecklistConfigurationPanel). Re-grepping by class string (not stale line numbers) across the whole repo, not just the 7 listed files, found the cluster is actually 4 distinct, independently well-evidenced deliberate tiers (60+, 25+, 4, and 7 confirmed instances respectively) plus one genuine outlier. Orchestrator spot-checked all four tiers via independent grep before accepting. Formalized as new Intentional Exceptions D3-E24 (eyebrow tracking-widest tier, app-wide), D3-E25 (admin ConfigurationPanel tier), D3-E26 (permissions-manager tier, MinTierSelect+GlobalPermissionsManager+PeriodSelector), D3-E27 (widget-settings nested toggle-header tier, TimeTool+Webcam) — closing 6 of the 7 backlog rows. `NumberLine/Settings.tsx` (lines 271,286,362,377,392, drifted from 234/247/316/327/338) remains open: no corroborating reuse found elsewhere, likely genuine drift, but unsafe to convert as a single isolated instance (would create a within-row visual mismatch with untouched siblings) — needs a dedicated future run converting all 5 together. No code shipped, no push. | — |
| 2026-07-24 | — | D4 Import Paths | Aligned — thorough fresh sweep (all files added since 2026-06-15 via git log, ~1,284 files grepped for `'../'`; separately all files added/modified in the last 2 weeks; separately a repo-wide `'../../'` two-level-up check, zero matches) found zero new cross-directory relative-import violations — all `'../'` hits fall into the already-documented D4-E2 gray zone (same-widget-folder siblings like `'../WidgetLayout'`, `'../constants'`, `'../hitTest'`, etc., or same-feature `plc/` sibling imports covered by the existing ESLint regex). Confirmed the 3 `no-restricted-imports` guardrails in `eslint.config.js` present and unmodified. Hit the stuck-background-wait failure mode once (see Notes); orchestrator inspected the worktree directly (clean status, zero diff), resumed via SendMessage telling it there was nothing to validate, got a clean final report on one resume. | — |
| 2026-07-24 | `nightly/unify-d5-toast-arch-2026-07-24` | D5 Toast Arch | Shipped — fresh staleness sweep (git log since 2026-06-15/2026-07-10 over admin/plc/*Modal.tsx files) found one genuine new instance not previously catalogued: `SpecialistScheduleConfigurationModal.tsx`'s local `message` useState + a `<Toast>` rendered as a sibling overlay *outside\* the `<Modal>` (via an outer fragment) — confirmed `DashboardProvider` reachable (mounted via `FeaturePermissionsManager.tsx`) and the toast was a global-overlay imitation, not inline-in-body, so it didn't match any of the 22 existing exception shapes. Converted to `addToast()` via `useDashboard()`; removing the now-unneeded outer fragment reindented the whole JSX body, accounting for most of the diff size — orchestrator read the full diff and confirmed no logic/markup change beyond the toast wiring. Hit the stuck-background-wait failure mode once (see Notes); orchestrator found and killed an unrelated orphaned process tree left over from D4's earlier stuck-wait recovery that was competing for CPU with this dimension's own validate run. `pnpm run validate` (585/585 root test files, 38/38 functions test files) and `pnpm run build` both green. dev-paul had not moved since worktree creation, no rebase needed. `claude[bot]` automated PR review: LGTM, no issues. | #2273 |
| 2026-07-23 | — | D1 Empty States | Aligned — attempted the long-standing MiniApp `AssignmentsModal.tsx:123`/`SubmissionsModal.tsx:132` VISUAL-RISK lead (forcing a `container-type: size` boundary onto the modal overlay, mirroring the ActivityWall run-35 fix) but reverted cleanly before committing: unlike ActivityWall's widget front-face, these are full-page/overlay modal surfaces outside the `WidgetRegistry` `skipScaling` container-query model — coupling a widget-scaling concern onto a non-widget modal for a cosmetic empty-state win risked changing the modal's layout/stacking-context behavior, a real scope-creep risk without a clear payoff. Correctly reset (`git reset`) rather than ship. This is a distinct rationale from the item's existing "no container-type boundary" framing — worth citing alongside it. No other new instance found in a fresh sweep. Both standing blocked items (`SmartNotebook/Library.tsx:163`, the two MiniApp modals) re-confirmed unchanged. Hit the stuck-background-wait failure mode once (see Notes); orchestrator inspected the worktree directly, found genuine clean-reset state plus one stray `nohup`-backgrounded validate process, killed it (nothing to validate — zero diff), and got a clean final report on a single resume. | — |
| 2026-07-23 | — | D2 Brand Colors | Aligned — 34th consecutive clean run. Fresh grep (hex + decimal-RGB + CSS4 rgb()) across `components/`, `context/`, `hooks/`, `config/`, `utils/`; every hit classified into a known exception category, including two hits already in canon `var(--spart-primary, ...)` form. `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged, correctly left untouched. Completed cleanly on first dispatch, no resume needed. | — |
| 2026-07-23 | `nightly/unify-settings-labels-2026-07-23` | D3 Settings Labels | Shipped — see Backlog row above (`SyntaxFramer/Settings.tsx:158` "Alignment" → group-heading `SettingsLabel` retrofit, the final candidate in the multi-run retrofit series tracked since run 31). Orchestrator independently read `SettingsLabel.tsx` source and the surrounding button-group markup to confirm the zero-visual-delta and group-heading-shape claims before shipping. Completed cleanly on first dispatch, no resume needed. dev-paul had not moved since worktree creation, so no rebase was needed before opening the PR. | #2263 |
| 2026-07-23 | — | D4 Import Paths | Aligned — re-diffed the `components/plc/**` ESLint regex against both the current `PLC_SECTIONS` array (10 ids, all covered) and the actual filesystem subdirectory listing of `components/plc/` (17 dirs, all covered) — regex holds clean. Also re-diffed the `components/widgets/**` math-tools regex, still correctly scoped. Fresh repo-wide grep found zero multi-level `../../` escapes and zero out-of-gray-zone single-level escapes; explicitly checked files added since 2026-07-01 for any new relative-import anti-pattern — none found. Independently verified via a scoped `pnpm exec eslint eslint.config.js components/plc components/widgets --max-warnings 0` pass (clean). Completed cleanly on first dispatch, no resume needed. | — |
| 2026-07-23 | — | D5 Toast Arch | Aligned — fresh sweep of `components/admin/**` plus all adjacent directories (classroomAddon, landing, legal, lti, poll, subs, converter, settingsModal, boardsModal, backgroundsModal, quickAccessModal, share, classes, plc, layout) found every hit already catalogued or a confirmed false positive, plus two previously-uncatalogued but clearly-fitting inline-notification components, formalized as D5-E21 (`plc/bodies/MembersBody.tsx`) and D5-E22 (`plc/PlcShareTargetModal.tsx`) — both `useDashboard()`-reachable but inline-in-body, same rationale as the existing E13/E16/E18/E19 cluster. Standing z-index "renders behind modal" open question re-noted but explicitly NOT acted on, per standing instruction — still needs the dedicated human/browser-verified pass. Completed cleanly on first dispatch, no resume needed. | — |
| 2026-07-22 | — | D1 Empty States | Aligned — fresh repo-wide search across `components/widgets/**` for hand-rolled empty states, cross-referenced against the 41 files already using `ScaledEmptyState`. All hits matched an existing exception category (no-icon text state, split-pane placeholder, filtered-search result, results/confirmation panel, or the 3 standing container-scope-blocked items — `SmartNotebook/Library.tsx`, `ActivityWall/Widget.tsx:~1878`, `MiniApp/{AssignmentsModal,SubmissionsModal}.tsx`, all re-confirmed unchanged). Two new sightings recorded for the backlog: `DrawingWidget/Widget.tsx:~1593`'s "Start drawing" hint is architecturally exempt (the widget is one of only two kept on CSS-transform scaling rather than container queries, per `WidgetRegistry.ts` — no `container-type: size` context exists for `cqmin` to resolve against); `QuizManager.tsx:~1829` ("No quizzes yet") and `MiniAppManager.tsx:~853/869` (both empties) are icon-in-card-wrapper states in the same blocked class as `SmartNotebook/Library.tsx` (`ScaledEmptyState` has no slot for a decorative bordered/backdrop wrapper around the icon). | — | --- | --- |
| 2026-07-22 | — | D2 Brand Colors | Aligned — 33rd consecutive clean run. Fresh grep (hex + decimal-RGB) across `components/`, `context/`, `hooks/`, `config/`, `utils/`; 21 hits, all classified into known exception categories or already-canonical `var(--spart-primary, ...)` usage. No CSS4 space-separated `rgb()` forms found. `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged, correctly left untouched. | — | --- | --- |
| 2026-07-22 | `nightly/unify-settings-labels-2026-07-22` | D3 Settings Labels | Shipped — see Backlog row above (`SyntaxFramer/Settings.tsx:104` "Mode" → group-heading `SettingsLabel` retrofit, 1st of 2 candidates surfaced by run 39). Orchestrator independently read `SettingsLabel.tsx` source to confirm the zero-visual-delta claim before shipping, and independently re-ran eslint+prettier on the changed file after the subagent's own full validate+build. | #2259 | --- | --- |
| 2026-07-22 | — | D4 Import Paths | Aligned — 6th consecutive clean check. Re-diffed the `components/plc/**` ESLint regex against both `PLC_SECTIONS` (10 ids, all covered) and the actual `components/plc/` filesystem listing (17 dirs, all covered); re-checked the `components/widgets/**` math-tools regex, still correctly scoped. Fresh repo-wide grep found zero multi-level `../../` escapes; all 40 single-level cross-directory hits fall into documented gray zones (plc bodies/tabs→root, pervasive `../WidgetLayout`, and a newly-named-for-the-record `<Widget>/<subfolder>/X.tsx` → `<Widget>/root` convention used consistently across GuidedLearning/QuizWidget/SoundWidget/VideoActivityWidget/Onboarding/DrawingWidget/MathToolInstance — same shape as the accepted plc gray zone, not a new anti-pattern). Independently confirmed both enforcement regexes fire correctly via a scoped `eslint --rule no-restricted-imports:error` pass (exit 0, zero violations). | — | --- | --- |
| 2026-07-22 | — | D5 Toast Arch | Aligned — fresh sweep of `components/admin/**` plus adjacent directories (classroomAddon, landing, legal, lti, poll, subs, converter) found every hit already catalogued, a confirmed false positive (form fields/props named `message`/`error` that aren't toasts — `RequestRolloutPage.tsx`, `UsersView.tsx`'s invite-email field, several admin panel inline error banners), or correct-by-design (`subs/SubsDashboardProvider.tsx`'s `addToast`-shaped shim deliberately NOOPs since subs users can't see toasts; `AssignToClassroomModal.tsx` receives `addToast` via prop delegation, not local state). Standing z-index "renders behind modal" open question re-noted but not re-investigated per standing instruction — still needs the dedicated human/browser-verified pass. | — | --- | --- |
| 2026-07-21 | — | D1 Empty States | Aligned — fresh repo-wide search across `components/widgets/**` for hand-rolled empty states. A subagent's first attempt converted `ExpectationsWidget/Widget.tsx`'s "No expectation categories enabled" text-only state to `ScaledEmptyState` — the orchestrator caught this via direct worktree inspection (not the subagent's own self-report) as a match for the already-catalogued D1-E16 exception (adds an icon that wasn't there — a real visual change, not preserving) and sent it back to revert and re-search. The redo systematically checked ~10 additional candidates (ShuffleList, MathTools filtered-search, VideoActivityWidget Creator, IconOrImageInput, MusicWidget EmptyHint family, WrittenResponseGrader, GuidedLearningResults, CatalystSetPickerPopover, DrawingWidget canvas overlay, QuizLiveMonitor podium) and correctly classified every one as an existing exception category (no-icon text state, filtered-search result, interaction affordance, or a standing blocked container-scope item) — no new fixable instance exists this run. All 3 standing blocked items (SmartNotebook/Library.tsx, ActivityWall ~1877-1890, MiniApp Assignments/SubmissionsModal) re-confirmed unchanged. | — | — | — |
| 2026-07-21 | — | D2 Brand Colors | Aligned — 32nd consecutive clean run. Fresh grep (hex + decimal-RGB) across `components/`, `context/`, `hooks/`, `config/`, `utils/`; all 21 hits classified into known exception categories (palette/swatch arrays, user-configurable picker defaults, admin picker-default constants, chart/KPI accents, static gradient design, deterministic class-color palette) or already-canonical `var(--spart-primary, ...)` usage. `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged, correctly left untouched. | — | — | — |
| 2026-07-21 | `nightly/unify-settings-labels-2026-07-21` | D3 Settings Labels | Shipped — see Backlog row above (RevealGrid/Settings.tsx:363 "Reveal Mode" → group-heading `SettingsLabel` retrofit, the final candidate in this file). Orchestrator independently re-verified validate+build in the worktree (real PIDs found via `ps aux` and waited on synchronously, since the subagent's own background-command wrapper reports a misleadingly-immediate exit) before opening the PR, rather than trusting the subagent's report alone. | #2254 | — | — |
| 2026-07-21 | — | D4 Import Paths | Aligned — re-diffed the `components/plc/**` ESLint regex against both the current `PLC_SECTIONS` array (10 ids, all covered) and the actual filesystem subdirectory listing of `components/plc/` (17 dirs, all covered) — 5th consecutive clean check. Fresh repo-wide grep for single-level and multi-level relative cross-directory imports found zero new anti-pattern instances; explicitly re-swept smaller top-level directories (classroomAddon, landing, legal, lti, poll, subs, converter, plus several more) — all already using `@/` alias. Confirmed `functions/src/` has no `@/` alias configured and is a separate package, correctly out of D4 scope. | — | — | — |
| 2026-07-21 | — | D5 Toast Arch | Aligned — fresh sweep of `components/admin/**` plus adjacent directories found every hit already catalogued. One previously-uncatalogued file inspected: `components/lti/LtiLaunchPage.tsx` — confirmed NOT a toast/notification pattern (its `error`/`phase` state drives the entire page's rendered body, not an ephemeral overlay) and confirmed `useDashboard()`/`addToast` unreachable regardless (mounted under `AuthProvider` only, no `DashboardProvider`) — noted here for hygiene so a future run doesn't re-investigate it from scratch. Standing z-index "renders behind modal" open question re-noticed but not re-investigated per standing instruction — still needs the dedicated human/browser-verified pass. | — | — | — |
| 2026-07-20 | — | D1 Empty States | Aligned — fresh repo-wide grep of `components/widgets/**` (structural + phrase heuristics), cross-referenced against files already importing `ScaledEmptyState`; also checked for new widget files since 2026-07-01 (only test/util files added, no new front-face component). Every remaining hand-rolled hit individually inspected and classified into a documented exception shape (settings sub-panel, modal overlay w/o `container-type: size`, no-icon state, split-pane placeholder, stylized library card, results/confirmation summary, interaction affordance). All 3 standing blocked items (`SmartNotebook/Library.tsx`, `ActivityWall/Widget.tsx:~1877-1890`, `MiniApp/{AssignmentsModal,SubmissionsModal}.tsx`) re-confirmed unchanged and still correctly blocked. | — |
| 2026-07-20 | — | D2 Brand Colors | Aligned — 31st consecutive clean run. Fresh grep (hex + decimal-RGB + CSS4 rgb()) across `components/`, `context/`, `hooks/`, `config/`, `utils/`; every hit mapped to a known exception category (user-configurable picker defaults, swatch/palette arrays, chart/KPI accents, gradient design, admin picker-default constants). `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged, correctly left untouched (coordinated 3-instance fix needed, not a single-file patch). | — |
| 2026-07-20 | `nightly/unify-settings-labels-2026-07-20` | D3 Settings Labels | Shipped — retrofitted `RevealGrid/Settings.tsx:314` "Game Mode" `SettingsLabel` (2nd of the 4 candidates surfaced by run 36) to as="span" id scoped to `widget.id` + role="group" aria-labelledby on the 2-button mode-selector group, matching the exact widget.id-interpolation convention already used one block above in the same file for "Columns". Zero-visual-delta confirmed against `SettingsLabel.tsx` source by both the subagent and the orchestrator independently. Subagent ran validate's constituent stages individually after the combined `pnpm run validate` exceeded a 10-minute single-Bash-call cap under concurrent sibling-worktree load — all stages green with real exit codes. validate ✅ 6986/6986 root + 706/706 functions, build ✅ (orchestrator independently re-ran type-check + eslint on the changed file — clean) — PR #2245 (draft). 2 candidates remain: `RevealGrid/Settings.tsx:357` "Reveal Mode", `SyntaxFramer/Settings.tsx:104` "Mode", `:158` "Alignment". | #2245 |
| 2026-07-20 | — | D4 Import Paths | Aligned — re-diffed the `components/plc/**` ESLint regex against the current `PLC_SECTIONS` array (all 10 ids covered, 4th consecutive clean check) and cross-checked against the actual filesystem subdirectories of `components/plc/` (17 dirs, all covered). Fresh repo-wide grep found zero multi-level `../../` escapes and zero out-of-gray-zone single-level escapes; explicitly re-swept the smaller/newer top-level directories (`classroomAddon`, `landing`, `legal`, `lti`, `poll`, `subs`, `converter` — all clean). Noted (not acted on): several widgets have relative imports reaching one level up to a file within their own widget's directory tree (not a sibling feature escape) — same gray-zone rationale as the existing `WidgetLayout` exception, pervasive across 6+ widgets, pre-existing. | — |
| 2026-07-20 | — | D5 Toast Arch | Aligned — staleness scan of `components/admin/**` plus all adjacent directories (including `classroomAddon/`, `landing/`, `legal/`, `lti/`, `poll/`, `subs/`, `converter/`) found every hit already catalogued or a confirmed false positive (`UsersView.tsx`/`RequestRolloutPage.tsx` message state are form fields, not toasts). Found and fixed one doc-hygiene item: D5-E19's catalogued path (`components/lti/TeacherReviewRoute.tsx`) was stale — the file moved to `components/classroomAddon/TeacherReviewRoute.tsx` in a prior unrelated refactor; content/pattern unchanged, path corrected in this doc pass. Standing z-index "renders behind modal" open question not re-investigated per standing instruction — still needs the dedicated human/browser-verified pass. | — |
| 2026-07-19 | — | D1 Empty States | Aligned — fresh repo-wide sweep of `components/widgets/**` (grep for "no data" text heuristics, cross-referenced against every file already importing `ScaledEmptyState`) found no new hand-rolled instance and no brand-new widget files added since 2026-07-01 (`git log --diff-filter=A --since="2026-07-01" -- '*Widget.tsx'` empty). Individually re-confirmed the handful of files still lacking the import all match catalogued exceptions (D1-E15 `CustomWidget`, D1-E16 `ExpectationsWidget`, D1-E18 `DrawingWidget` "Start drawing" overlay) or aren't UI states at all (`LunchCount/Widget.tsx` "No submission URL configured" is an `addToast()` error message, not rendered empty-state UI). Both standing VISUAL-RISK items (`SmartNotebook/components/Library.tsx`, `ActivityWall/Widget.tsx:~1877`) re-confirmed unchanged. | — |
| 2026-07-19 | — | D2 Brand Colors | Aligned — 30th consecutive clean run. Fresh grep (hex + decimal-RGB + CSS4 space-separated `rgb()` syntax) across `components/`, `context/`, `hooks/`, `config/`, `utils/`: 25 hex + 2 decimal hits, all mapping to catalogued exception categories (swatch/palette arrays, chart/KPI accents, gradient design, admin picker defaults, user-configurable field defaults, `DEFAULT_GLOBAL_STYLE` theme source-of-truth). Traced `MusicWidget/Widget.tsx:348`'s per-station fallback color to `Stations/components/StationEditor.tsx:163-176`'s own color-swatch/`<input type="color">` picker — confirms it's a user-owned per-station value (same category as Countdown `eventColor`), not a new instance. `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged, correctly left untouched. | — |
| 2026-07-19 | `nightly/unify-settings-labels-2026-07-19` | D3 Settings Labels | Shipped — retrofitted `random/RandomSettings.tsx:363` "Operation Mode" `SettingsLabel` (heads a `grid grid-cols-4` of 4 sibling mode-selector buttons, no 1:1 control) to `as="span" id={operationModeId}` (via `useId()`, matching this file's own `groupSizeId`/`animationStyleId`/etc. convention — `RandomSettings` renders per-widget-instance, so a static id would risk duplicate-DOM-id collisions per the run-32 gotcha) + `role="group" aria-labelledby` on the button-grid container. This is the 1st of the 4 candidates surfaced by run 36. Zero-visual-delta independently re-confirmed by the orchestrator against `SettingsLabel.tsx` source (`combinedClasses` built identically regardless of `as`). validate ✅ 6953/6953 root + 706/706 functions, build ✅ (orchestrator independently re-ran both after the subagent's own validation — clean) — PR #2236 (draft). 3 candidates remain: `RevealGrid/Settings.tsx:314` "Game Mode", `:357` "Reveal Mode", `SyntaxFramer/Settings.tsx:104` "Mode", `:158` "Alignment". | #2236 |
| 2026-07-19 | — | D4 Import Paths | Aligned — re-diffed the `components/plc/**` ESLint regex (`eslint.config.js:176`) against the current `PLC_SECTIONS` array in `sections.ts` (10 ids: home/meeting/assessments/sharedData/docs/todos/sharedBoards/members/resources/settings) — all present, no gap this time (the run-28/34/2026-07-15 drift class has now held clean twice in a row). Also re-diffed the `components/widgets/**` math-tools rule and the `components/admin/Organization/views/**` rule — both still correct. Fresh repo-wide grep for multi-level `../../` escapes and single-level out-of-gray-zone escapes found zero hits; explicitly checked the newer/smaller directories (`classroomAddon`, `landing`, `legal`, `lti`, `poll`) again — still flat, no cross-dir imports. Full validate run split into individual stage commands (combined `pnpm run validate` hit the 10-minute per-command timeout under concurrent sibling-worktree CPU load) — all stages green, plus a standalone full-repo `pnpm exec eslint . --max-warnings 0` (exit 0) and `pnpm run build` (exit 0). | — |
| 2026-07-19 | — | D5 Toast Arch | Aligned — staleness scan of `components/admin/**` plus the full usual adjacent-directory list found every local-Toast/message-state hit already catalogued (`GraphicOrganizerConfigurationModal.tsx` D5-E3, `LinkShortenerManager.tsx` D5-E13, `OrganizationPanel.tsx` D5-E16, `InstructionalRoutines/LibraryManager.tsx` D5-E14) or a confirmed false positive (`landing/RequestRolloutPage.tsx`'s `message` is a plain form textarea, not a toast; `Organization/views/UsersView.tsx`'s `message`/`setMessage` is an invite-dialog "custom message" field explicitly delegated to the parent `OrganizationPanel.tsx`'s toast, i.e. still D5-E16). Standing z-index "renders behind modal" open question not re-investigated per standing instruction — still needs the dedicated human/browser-verified pass. | — |
| 2026-07-17 | — | D1 Empty States | Aligned — fresh grep across `components/widgets/**` found no new hand-rolled instances since run 35. Re-examined both standing VISUAL-RISK items: `SmartNotebook/components/Library.tsx` (icon-in-white-card wrapper) still can't be reproduced via `ScaledEmptyState`'s `iconClassName`/`titleClassName`/`subtitleClassName` overrides alone (no prop to wrap just the icon) — left untouched. `ActivityWall/Widget.tsx` "Responses will appear here..." (~line 1880): confirmed the run-35 scoped-`container-type: size`-wrapper technique transfers cleanly (sibling populated branches are unaffected), but the instance has an icon and no existing title — converting would require inventing new title copy (e.g. "No Responses Yet"), which is a content decision, not a mechanical refactor. Declined to invent copy; flagged as the closest-to-actionable backlog item for a human to pick the title text. `MiniApp/{AssignmentsModal,SubmissionsModal}.tsx` downgraded from "not yet attempted" to "not applicable" — both are text-only, no icon, disqualified by the established no-icon exception rule regardless of container-query work. | — |
| 2026-07-17 | — | D2 Brand Colors | Aligned — 29th consecutive clean run. Fresh grep (hex + decimal-RGB) across all source; every hit maps to a known exception category. Named two previously-unnamed-but-same-shape instances for the record: `MusicWidget/Widget.tsx:348` per-station fallback color and `NextUpConfigurationPanel.tsx` building-level `themeColor` default — both pre-existing (June 3), both already covered by the "user-configurable color-picker default" bucket. `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present, unchanged, correctly left untouched. | — |
| 2026-07-17 | `nightly/unify-settings-labels-2026-07-17` | D3 Settings Labels | Shipped — retrofitted `MusicWidget/Settings.tsx:137` "Source" `SettingsLabel` (heads a 2-button `SOURCE_OPTIONS` group, no 1:1 control) to `as="span" id={sourceLabelId}` (via `useId()`, matching this file's own `layoutLabelId` convention) + `role="group"/aria-labelledby`, matching the PRs #2144/#2168/#2183/#2197/#2207/#2218 pattern. This closes the "left for a future run" candidate flagged at the end of run 35 — all originally-known group-heading retrofit candidates in this file are now converted. Zero-visual-delta confirmed against `SettingsLabel.tsx` source (`combinedClasses` computed once, identical for both `as` branches) by both the subagent and the orchestrator independently. **Orchestrator note:** the dispatched subagent hit the recurring stuck-background-wait failure mode (see Notes) after finishing its grep sweep but before running validate; the orchestrator inspected the worktree directly (diff looked correct, validate was genuinely still running, not stuck/dead), then resumed it via `SendMessage` with an explicit blocking instruction plus a grounded "your diff looks right" observation — it completed cleanly on the first resume. Both the subagent and a separate D4 subagent independently discovered and flagged that the dispatch prompts' suggested `/tmp/validate.log` path was a SHARED, non-worktree-scoped path that collided across concurrently-running dimension worktrees — each redirected to a worktree-local log instead. validate ✅ 6924/6924 root + 703/703 functions, build ✅ — PR #2224 (draft). New candidates spotted for a future run: `random/RandomSettings.tsx:363` "Operation Mode", `RevealGrid/Settings.tsx:314` "Game Mode" and `:357` "Reveal Mode", `SyntaxFramer/Settings.tsx:104` "Mode" and `:158` "Alignment" (`Scoreboard/Settings.tsx:182` "Layout" already uses `role="radiogroup" aria-label="..."` directly — a different, already-accessible pattern, likely not worth converting). | #2224 |
| 2026-07-17 | — | D4 Import Paths | Aligned — re-diffed the `components/plc/**` ESLint regex against `PLC_SECTIONS` (no gap, 2nd consecutive clean check after run 35). Fresh full-repo grep for multi-level relative imports, single-level escapes outside the D4-E2 gray zone, and new top-level directories found nothing new since 2026-07-16. `functions/` reconfirmed structurally out of scope (no `@/` alias in its tsconfig). Independently discovered and worked around the same `/tmp/validate.log` path collision noted in the D3 row above. validate ✅ + 703/703, build ✅. | — |
| 2026-07-17 | — | D5 Toast Arch | Aligned — staleness scan of `components/admin/**` plus all adjacent directories found every local-Toast/message-state hit already catalogued or a confirmed false positive. Named `CalendarConfigurationModal.tsx`'s local `<Toast>` for the record (matches the accepted modal-body-inline exception shape exactly; not previously named by file). Re-confirmed the standing z-index "renders behind modal" open question is unchanged (`toast: 14000` still above all modal tiers) — still needs the dedicated human/browser-verified pass, not acted on. | — |
| 2026-07-16 | `nightly/unify-empty-states-2026-07-16` | D1 Empty States | Shipped — converted the long-standing VISUAL-RISK `ActivityWall/Widget.tsx:1561` "No activities yet" hand-rolled state to `ScaledEmptyState`, finally resolving the container-scoping block first documented in run 26 (PR #2143, closed unmerged): added a `container-type: size` boundary scoped only to the empty-state branch (verified NOT shared with the populated-list branch, so the run-26 "rescoping affects populated content" risk doesn't apply), preserved the card background via the outer wrapper div, and preserved icon/text colors via className overrides — independently verified against 4 existing precedents for the same nested-boundary pattern (`Checklist/Widget.tsx`, `MaterialsWidget/index.tsx`, `Stations/components/StationCard.tsx`, `ConceptWeb/Widget.tsx`) before shipping. **Orchestrator repair:** the dispatched subagent first converted `QuizEditor.tsx:191` ("No questions yet" context-pane box) — a text-only, no-icon state — without recognizing it matched the established no-icon exception family (D1-E9/E15/E16/E17: converting adds an unwanted icon + forces uppercase title casing). Orchestrator caught this via direct diff review, sent the subagent back with the specific precedent citation; it agreed, reverted the QuizEditor change, and shipped ActivityWall instead. New exception formalized: D1-E23. Orchestrator also independently re-ran `pnpm run validate` (redirected to a file, not piped through `tail`, since the subagent flagged its own tail-piped exit-code check as unreliable) — genuinely green. validate ✅ 703/703 root + 703/703 functions, build ✅ — PR #2219 (draft, tagged visual-risk for a rendered-preview double-check despite the fix). | #2219 |
| 2026-07-16 | — | D2 Brand Colors | Aligned — 28th consecutive clean run. Fresh grep (hex + decimal-RGB) across all source; every hit maps to a known exception category (user-configurable defaults, swatch/palette arrays, chart/KPI colors, gradient design stops, admin picker-default constants) or test fixtures/out-of-scope files. Noted a minor doc-wording drift: the "StylePanel `DEFAULT_ACCENT_COLOR`" exception (D2-E8) now lives as `AppearanceSection.tsx`'s constant + `types.ts`'s `DEFAULT_GLOBAL_STYLE` source-of-truth — no `StylePanel.tsx` file currently exists. Standing `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present and correctly left untouched. | — |
| 2026-07-16 | `nightly/unify-settings-labels-2026-07-16` | D3 Settings Labels | Shipped — retrofitted `MusicWidget/Settings.tsx:183` "Layout" `SettingsLabel` (heads a 3-button layout-selector group, no 1:1 control) to `as="span" id={`music-layout-label-${widget.id}`}` + `role="group"/aria-labelledby`, matching the PRs #2144/#2168/#2183/#2197/#2207 pattern. Used `widget.id`-interpolation (not `useId()`) since `widget` was already in scope. Zero-visual-delta confirmed against `SettingsLabel.tsx` source. This closes the "3/4 more candidates" retrofit series tracked since run 31 (DrawingWidget, RevealGrid, RandomSettings, now MusicWidget Layout all done). New candidate spotted in the same file: `MusicWidget/Settings.tsx:137` "Source" label (same orphaned-label shape) — left for a future run. validate ✅ 703/703 root + 703/703 functions, build ✅ — PR #2218 (draft). | #2218 |
| 2026-07-16 | — | D4 Import Paths | Aligned — re-diffed the `components/plc/**` ESLint `no-restricted-imports` regex in `eslint.config.js` against the current `PLC_SECTIONS` array in `sections.ts`: no drift this time (the run-2026-07-15 fix, PR #2208, holds). Fresh repo-wide grep found zero multi-level (`../../`) escapes, zero out-of-gray-zone single-level escapes, zero relative dynamic `import()`/`require()` in app source. This is the third time this exact regex-drift class has been found (runs 28, 34, and 2026-07-15) — flagged the subagent's proposal to derive the regex programmatically from `PLC_SECTIONS` (e.g. a `tests/rules`-style unit test asserting every id appears in the ESLint config string) as worth prioritizing over continuing to re-check by eye each night; see Notes. | — |
| 2026-07-16 | — | D5 Toast Arch | Aligned — staleness scan of `components/admin/**` plus the usual adjacent directories (including `classroomAddon/`, `landing/`, `legal/`, `lti/`, `poll/`, `subs/`, `converter/`) found every local-Toast/message-state hit already catalogued or a confirmed false positive. Investigated and confirmed two new legitimate-but-uncatalogued patterns, formalized as D5-E19/E20 below: `TeacherReviewRoute.tsx`'s inline iframe status line (DashboardProvider IS available, but the notification is deliberately inline-in-body, not a global overlay) and the `UpdateNotification`/`DriveDisconnectBanner`/`SchoologyLinkNudge` persistent-dismissible-banner trio (a distinct UI tier the ephemeral `addToast` API doesn't support). Standing z-index "renders behind modal" open question re-confirmed unchanged (`toast: 14000` still numerically above all modal tiers; `ToastContainer` still no `createPortal`) — still needs the dedicated human/browser-verified pass, not acted on. | — |
| 2026-07-15 | `nightly/unify-empty-states-2026-07-15` | D1 Empty States | Shipped — converted `QuizManager.tsx`'s `AssignmentsList` active/archive-tab empty state (~line 2065, formerly a "plausible future candidate" from run 33) to `ScaledEmptyState`; verified in-scope front-face content, distinct from the D1-E6 library-tab exception, and confirmed no sub-panel cqmin trap. Orchestrator independently re-verified the claimed "direct precedent" — `VideoActivityWidget/components/VideoActivityManager.tsx:1007` already ships the identical bare-`ScaledEmptyState`-inside-`LibraryShell` pattern in production — before approving; this is worth doing whenever a subagent cites a sibling-file precedent as safety evidence, since `LibraryShell`'s root is `text-slate-800` (a light-surface signal that could plausibly have been the `ExpectationsWidget`-style light-surface trap from run 30's notes) and the precedent check is what actually resolved the question rather than trusting the claim at face value. Two subagent resumes needed (stuck-background-wait, see Notes). validate ✅ 6863/6863 + 703/703, build ✅ — PR #2206 (draft). Deferred: `MiniApp/{AssignmentsModal,SubmissionsModal}.tsx` (blocked, no `container-type: size` boundary in their modal overlays — same class as the ActivityWall visual-risk items); `QuizWidget/components/QuizEditor.tsx` "No questions yet" (same family as the already-exempted `VideoActivityEditor.tsx` pattern, candidate for a future D1-Enn); `CustomWidget/Widget.tsx` "No content configured." (text-only, no icon, would need a design decision); `Catalyst/CatalystSetPickerPopover.tsx` (portal popover, not cqmin-scoped, not a candidate). | #2206 |
| 2026-07-15 | — | D2 Brand Colors | Aligned — 27th consecutive clean run. Fresh grep (hex + decimal-RGB) across all source; every hit maps to a known exception category or already-correct `var()` usage. Standing `Dock.tsx:1586` NEEDS-REVIEW item reconfirmed present and correctly left untouched (coordinated fix needed across 3 sibling Dock instances, not a single-file patch). | — |
| 2026-07-15 | `nightly/unify-settings-labels-2026-07-15` | D3 Settings Labels | Shipped — retrofitted `random/RandomSettings.tsx:393` "Animation Style" `SettingsLabel` (heads a `grid grid-cols-3` of 3 style-selector buttons, no 1:1 control) to `as="span"` + `role="group"`/`aria-labelledby`, matching the PRs #2144/#2168/#2183/#2197 pattern. Used `useId()` (`animationStyleId`) for the id rather than `widget.id` string interpolation — `RandomSettings` renders per-widget-instance (confirmed via `WidgetRegistry.ts`), and `useId()` is this file's own established convention for other label associations (`groupSizeId`, `firstNamesId`, etc.), correctly avoiding the duplicate-DOM-id bug a prior run's `DrawingWidget` retrofit hit. Zero-visual-delta confirmed by both the subagent and the orchestrator directly against `SettingsLabel.tsx` source (`as="span"`/`as="label"` share the identical `combinedClasses`). One subagent resume needed (stuck-background-wait, see Notes). validate ✅ + 703/703, build ✅ — PR #2207 (draft). One candidate remains for a future night: `MusicWidget/Settings.tsx:183` "Layout". | #2207 |
| 2026-07-15 | `nightly/unify-import-paths-2026-07-15` | D4 Import Paths | Shipped (enforcement-only, zero source diff) — fresh repo-wide sweep (multi-level relative imports, single-level escapes, dynamic `import()`/`require()`, the 5 newer top-level `components/` dirs, and `functions/`) found no new anti-pattern instances in source — D4 remains fully aligned on actual code. Found a gap in the _enforcement rule itself_: the `components/plc/**` ESLint regex (intentionally forward-looking to catch escapes into not-yet-created `PLC_SECTIONS` directories) was missing `assessments` and `todos`, both confirmed as real ids in `components/plc/sections.ts`'s `PLC_SECTIONS` array. Widened the regex; independently re-ran a full-repo `pnpm exec eslint . --max-warnings 0` after the change — zero output, no false positives. One subagent resume needed (stuck-background-wait, see Notes). validate ✅ + 703/703, build ✅ — PR #2208 (draft). Backlog notes: `components/classroomAddon/`, `components/landing/`, `components/legal/`, `components/lti/`, `components/poll/` confirmed clean (no relative cross-dir imports at all); `functions/` confirmed structurally out of D4 scope (no `@/` path alias in `functions/tsconfig.json`). | #2208 |
| 2026-07-15 | — | D5 Toast Arch | Aligned — fresh grep across `components/admin/**` plus the usual adjacent dirs, and the 7 newer/smaller directories (`classroomAddon`, `landing`, `legal`, `lti`, `poll`, `subs`, `converter`) explicitly swept for the first time — every hit already catalogued or a confirmed false positive (`classroomAddon/AssignToClassroomModal.tsx` already receives `addToast` via prop; `subs/SubsDashboardProvider.tsx` intentionally supplies a no-op `addToast`; `landing/RequestRolloutPage.tsx`'s `message` state is a form field, unauthenticated route, no DashboardProvider anyway). Re-confirmed the standing z-index-registry question (`toast: 14000` vs `modal: 10000`/`modalNested: 10100`/`modalDeep: 10200`) still holds as described and went one step further (read-only) checking neither `ToastContainer` nor the modal family use `createPortal` — both render in normal tree order under `#dashboard-root`, so a CSS stacking-context nuance (not resolvable via static reading) remains the open question. Not acted on, per standing instruction — still needs a dedicated human-reviewed, browser-verified pass. | — |
| 2026-07-13 | `nightly/unify-empty-states-2026-07-13` | D1 Empty States | Shipped — fresh repo-wide grep across `components/widgets/**` found `QuizWidget/Widget.tsx:797-822`'s two hand-rolled auth-guard states ("Sign in required", "Drive access needed") were an exact duplicate of an already-converted pattern in `VideoActivityWidget/Widget.tsx:355-370` → `ScaledEmptyState` (same icons, same `skipScaling: true` container-query boundary, same already-accepted text-tone visual delta). Also re-confirmed `QuizWidget/components/QuizManager.tsx:1830-1849` ("No quizzes yet") — flagged by the dispatched subagent as a possible new exception — is in fact ALREADY covered by the existing D1-E6 exception (`quiz/QuizManager.tsx` stylized library empty states, 3 instances); no new exception ID needed. `QuizManager.tsx:2065-2074` (assignments-list/archive empty state) flagged as a plausible future candidate, left untouched to keep the diff to one clean change. validate ✅ 6808/6808 + 703/703, build ✅ — PR #2196 (draft; CI expected to show the unrelated pre-existing `docs/routines/unifier.md` format:check failure until #2187 merges, see Notes). | #2196 |
| 2026-07-13 | — | D2 Brand Colors | Aligned — fresh repo-wide grep (hex + decimal-RGB) across all source files found no new hardcoded inline-style theme bypass; every hit maps to a known exception (swatch palettes, user-configurable defaults, chart colors, config source-of-truth, design gradients, out-of-scope server email templates) or the two already-correct `var(--spart-primary/accent, ...)` usages. Standing `Dock.tsx:1586` NEEDS-REVIEW item re-confirmed present, unchanged, and correctly left alone. Backlog note (not acted on, different doc): `docs/scheduled-tasks/ui-unification.md` — a separate, older audit doc — still lists several hex occurrences as open items that are already-resolved exceptions under this routine's current canon; may be worth a cross-reference/cleanup pass by whoever owns that doc. | — |
| 2026-07-13 | `nightly/unify-settings-labels-2026-07-13` | D3 Settings Labels | Shipped — converted `RevealGrid/Settings.tsx:287` "Columns" `SettingsLabel` (heads 4 sibling column-count buttons, no 1:1 control) to ``as="span" id={`revealgrid-columns-label-${widget.id}`}`` + `role="group"/aria-labelledby` on the button-group container — same widget.id-instance-scoping pattern as `DrawingWidget/Settings.tsx` (run 32, PR #2183), verified necessary since `RevealGridSettings` is a per-widget-instance component, not a modal singleton. Zero visual delta confirmed directly against `SettingsLabel.tsx` source. Two remaining confirmed candidates left for future nights: `random/RandomSettings.tsx:393` "Animation Style", `MusicWidget/Settings.tsx:183` "Layout" (one-per-night pacing). validate ✅ 6808/6808 + 703/703, build ✅ — PR #2197 (draft). | #2197 |
| 2026-07-13 | `nightly/unify-import-paths-2026-07-13` | D4 Import Paths | Shipped — fresh repo-wide grep found 7 files in `components/admin/Organization/views/` (`AllOrganizationsView.tsx`, `BuildingsView.tsx`, `DomainsView.tsx`, `OverviewView.tsx`, `RolesView.tsx`, `StudentPageView.tsx`, `UsersView.tsx`) still importing the parent `Organization/types.ts` via relative `'../types'` — a half-finished fix, since a prior D4 pass (PR #2169) already converted these same files' sibling `'../components/primitives'` import to the `@/` alias but missed `'../types'`. Converted all 7 → `@/components/admin/Organization/types`. **Enforcement added**: a `no-restricted-imports` block in `eslint.config.js` scoped to `components/admin/Organization/views/**/*.{ts,tsx}`, mirroring the existing plc/math-tools rules for the same recurring bug class — orchestrator independently re-verified it fires correctly on a planted violation and produces no false positives elsewhere. validate ✅ 6808/6808 + 703/703, build ✅ — PR #2198 (draft). Orchestrator note: one dispatched subagent was killed for running too long stuck on a backgrounded full-repo `eslint --max-warnings 0` run and had to be resumed with an explicit foreground-only instruction (see Notes — this is now the single most common failure mode in this routine, hitting 4 of 5 dispatches this run: D1, D3, D4, D5 all needed at least one resume). | #2198 |
| 2026-07-13 | — | D5 Toast Arch | Aligned — fresh grep across `components/admin/**` plus `settingsModal/`, `boardsModal/`, `backgroundsModal/`, `quickAccessModal/`, `share/`, `classes/`, `plc/`, `layout/` found all local-Toast/message-state hits map to catalogued exceptions; `UsersView.tsx` and `GraphicOrganizerConfigurationModal.tsx` false positives reconfirmed; no new unaudited admin components found. **Important backlog finding (not acted on — needs human, browser-verified review):** traced the actual DOM/stacking-context chain (`config/zIndex.ts`: `toast: 14000` vs `modal: 10000`/`modalNested: 10100`/`modalDeep: 10200`) and found the documented rationale behind ~10 of the D5 exception-list files ("global `addToast`'s `ToastContainer` would render behind the modal's z-index") does not appear to hold under the current z-index registry — `addToast()` should render above these modals, not behind them, based on static analysis alone. Deliberately not acted on this run (10+ files, needs a live-browser stacking-context check before touching this much toast plumbing unattended) — flagged as a dedicated future human-reviewed pass. Only `SaveAsTemplateModal.tsx`/`WidgetBuilder/index.tsx` (genuinely inline, non-`fixed`) and `LinkShortenerManager.tsx`/`OrganizationPanel.tsx` (self-contained toast systems) are unaffected by this doubt. | — |
| 2026-07-12 | — | D1 Empty States | Aligned — fresh repo-wide grep across `components/widgets/**` (not diff-only), using both structural (`items-center justify-center` container) and phrase-based ("No X yet/configured/available") heuristics; every hit cross-checked against the 22 catalogued exceptions and 3 VISUAL-RISK items. Investigated and ruled out several new-looking keyword hits as noise (error/warning states in `Embed/Widget.tsx` and `SmartNotebook/PageEditorOverlay.tsx`, interactive block-renderer components in `CustomWidget/BlockRenderer.tsx`, a decorative default icon in `SeatingChart/FurnitureItemRenderer.tsx`, and ~10 ordinary layout-centering containers). Re-verified `ExpectationsWidget/Widget.tsx:421-439` (D1-E16) still correctly matches its exception shape post the run-31-noted revert. VISUAL-RISK line numbers re-confirmed (`Library.tsx:163`, `ActivityWall/Widget.tsx:1546` and `:1877` — both drifted slightly from run 31's 1561/1890). No new violation, no new exception candidate. | — |
| 2026-07-12 | — | D2 Brand Colors | Aligned — 24+ consecutive aligned run. Fresh grep (hex + decimal-RGB) found 43 hex + 2 decimal hits, all mapping to already-shipped fixes, catalogued exceptions (D2-E1–E15), or config/type-default source-of-truth sites (`config/widgetDefaults.ts:352` NextUp theme-color default, `admin/NextUpConfigurationPanel.tsx:24` matching fallback, `utils/migration.ts:85` time-tool migration default — user-configurable-default rationale, same as D2-E1/E2/E7/E11/E12; `types.ts` `DEFAULT_GLOBAL_STYLE` is the theme source of truth `DashboardView.tsx` reads to populate the CSS vars, correct by definition). Server-side Cloud Functions email-template hex confirmed out of scope. Re-confirmed `Dock.tsx:1586` remains the sole standing NEEDS REVIEW item; left untouched per standing instruction. | — |
| 2026-07-12 | `nightly/unify-settings-labels-2026-07-12` | D3 Settings Labels | Shipped — acted on the candidate retrofit dimension surfaced by run 31 (auditing pre-`as="span"`-era `SettingsLabel` group headings). Verified all 4 named candidates from the run-31 note genuinely match the shape (canonical `SettingsLabel`, default `as="label"`, heads 2+ sibling controls, no 1:1 pairing); converted one — `DrawingWidget/Settings.tsx` "Color Presets" (heads 8 color-swatch `<input type="color">` controls) — to `as="span"` + `role="group"`/`aria-labelledby` on the swatch container, matching the PRs #2144/#2168 pattern. **Orchestrator repair post-PR:** `claude[bot]` review caught a real bug — unlike the 3 prior `as="span"` conversions (all modal singletons), `DrawingSettings` renders per-widget-instance inside `DraggableWindow`, so the original static `id="drawing-color-presets-label"` would collide (invalid duplicate DOM id, unreliable `aria-labelledby`) if two DrawingWidget instances are both flipped to settings simultaneously. Fixed by scoping the id to the widget instance: ``id={\`drawing-color-presets-label-${widget.id}\`}``. Re-validated clean after the fix. validate ✅ 6784/6784 + 703/703, build ✅ — PR #2183. 3 more confirmed candidates left for future runs (see D3 backlog); one-per-night pacing per routine rule. **New gotcha for future D3 group-heading retrofits: always check whether the component is a modal singleton or a per-instance/per-widget component before hardcoding a static `id` — per-instance components need the id scoped by `widget.id` (or similar) to avoid duplicate-DOM-id collisions.** | #2183 |
| 2026-07-12 | `nightly/unify-import-paths-2026-07-12` | D4 Import Paths | Shipped — fresh repo-wide grep (all `'../` escapes outside `functions/`, plus a check of misc small root-level directories and a re-audit of the `components/plc/**` ESLint rule's directory list against `sections.ts`'s canonical ids — no gap found) turned up `components/widgets/MathToolInstance/Widget.tsx` (11 imports) and `Settings.tsx` (1 import) using relative `'../math-tools/...'` into the sibling `components/widgets/math-tools/` shared directory, while the sibling widget `MathTools/Widget.tsx` already used the canonical alias for the same modules → `@/components/widgets/math-tools/...`. **Enforcement added**: a new `no-restricted-imports` block in `eslint.config.js` scoped to `components/widgets/**/*.{ts,tsx}` targeting `math-tools/` escapes — first time this recurring bug class (previously only seen in `components/plc/**`) showed up elsewhere. **Orchestrator repair:** the subagent's commit also edited this doc file, based on a stale `dev-paul` checkout (run-30 content — the actual current state is this still-unmerged run-31 doc branch); reverted that file from the branch before opening the PR to avoid the doc-duplication failure mode documented below, leaving the code+enforcement diff untouched. Orchestrator independently re-ran `pnpm exec eslint . --max-warnings 0` repo-wide after the revert — clean, no false positives. validate ✅ 6784/6784 + 703/703, build ✅ — PR #2184. | #2184 |
| 2026-07-12 | — | D5 Toast Arch | Aligned — fresh grep across `components/admin/**` plus `settingsModal/`, `boardsModal/`, `backgroundsModal/`, `quickAccessModal/`, `share/`, `classes/`, `plc/`, `layout/` for local-Toast-import and `message`/`toast`-state patterns; every hit maps 1:1 to the 18 catalogued exceptions (including idiosyncratic ones like `WidgetBuilder/index.tsx`'s `saveMessage`, `LtiDeepLinkPicker.tsx`'s `errorMsg`). Confirmed zero commits touched any D5-scoped directory since run 31's 2026-07-11 cutoff. `Organization/views/UsersView.tsx`'s `message`/`setMessage` re-confirmed as an invite-form text field, not a toast (recurring false positive across several prior runs' greps). | — |
| 2026-07-11 | — | D1 Empty States | Aligned — fresh repo-wide grep across `components/widgets/**` (not diff-only) found every hand-rolled empty-state hit already catalogued (either a converted `ScaledEmptyState`, a VISUAL-RISK item, or one of the D1-E1–E22 exceptions); nothing new. Re-verified `ExpectationsWidget/Widget.tsx:421-439` (D1-E16) is still the correct text-only form post the pre-run revert (commit 08b86c2). No new exception candidates. | — |
| 2026-07-11 | — | D2 Brand Colors | Aligned — 23+ consecutive aligned run. Fresh grep (hex + decimal-RGB) found 57 hits across 27 files, all already-shipped fixes, catalogued exceptions, or config/type/comment source-of-truth defaults (not consumer sites). Re-confirmed `Dock.tsx:1586` `rgba(45, 63, 137, ...)` remains a valid NEEDS REVIEW item (base `className` is the static `bg-brand-blue-primary` Tailwind class, not the CSS var — converting just the inline-style overlay would desync it from its own base class and sibling icons). New exception-shaped pattern noted (not filed as a new ID, same shape as existing D2-E3/E7): `components/admin/Organization/views/StudentPageView.tsx:19-20` `ACCENT_PRESETS` swatch array. | — |
| 2026-07-11 | — | D3 Settings Labels | Aligned — byte-exact canonical-string grep found only already-catalogued files (`RosterModeControl.tsx` D3-E5, `NextUpStudentApp.tsx` D3-E11, `LunchCount/SubmitReportModal.tsx` display captions + the already-shipped "Additional Notes" `SettingsLabel htmlFor` from PR #2164). No new group-heading (`as="span"`) conversion targets found among hand-rolled (not-yet-`SettingsLabel`) labels. New backlog item surfaced (not acted on — large scope, needs a dedicated future run): a sizeable population of pre-existing `<SettingsLabel>` usages (default `as="label"`, no `htmlFor`) head groups of sibling controls rather than pairing 1:1 (e.g. `DrawingWidget/Settings.tsx` "Color Presets", `RevealGrid/Settings.tsx` "Columns", `random/RandomSettings.tsx` "Animation Style", `MusicWidget/Settings.tsx` "Layout", dozens more) — these predate the `as="span"` canon (PR #2141) and were never retrofitted; only the 3 instances newly converted from raw HTML in runs 26/29 got `as="span"`. Logged to backlog as a candidate future D3 retrofit dimension. | — |
| 2026-07-11 | `nightly/unify-import-paths-2026-07-11` | D4 Import Paths | Shipped — `i18n/index.ts` had 4 cross-directory relative imports (`'../locales/*.json'`, root-level `i18n/` → sibling root-level `locales/`) → `@/locales/*.json`. First-ever D4 sweep of `i18n/` (never explicitly scoped by any prior run). Orchestrator note: the dispatched subagent hit the recurring stuck-background-wait failure mode (see Notes) after finding the fix — orchestrator found its lingering `tsc --noEmit` process still running in the worktree, waited for it to exit (avoiding a duplicate concurrent type-check), then ran `pnpm run validate` + `pnpm run build` directly, both green, and committed/pushed/opened the PR itself. validate ✅ 6784/6784 + 703/703, build ✅ — PR #2174 (draft, gemini-code-assist + claude[bot] both reviewed with no changes requested). | #2174 |
| 2026-07-11 | — | D5 Toast Arch | Aligned — confirmed zero commits touched any D5-scoped directory (admin/settingsModal/boardsModal/backgroundsModal/quickAccessModal/share/classes/plc) since the doc's 2026-07-09 update. Fresh grep across all 8 directories found 16 genuine local-Toast/message-state hits, all mapping 1:1 to the 18 catalogued exceptions. No new instance. | — |
| 2026-07-09 | `nightly/unify-empty-states-2026-07-09` | D1 Empty States | Shipped — `ExpectationsWidget/Widget.tsx` hand-rolled "No expectation categories enabled" full-bleed disabled-state → `ScaledEmptyState` (icon reused from the widget's own dock entry, `LayoutGrid`). Orchestrator repair post-PR: gemini-code-assist correctly flagged that ExpectationsWidget is a light-surface widget (`bg-white` buttons, `text-slate-800` headers throughout, transparent root with no dark-dashboard card) — `ScaledEmptyState`'s dark-tuned default text colors (`text-slate-200/300`) would be a real contrast regression here, not an improvement as first assumed. Fixed by passing `iconClassName`/`titleClassName`/`subtitleClassName` overrides (`text-slate-500`/`text-slate-800`/`text-slate-500`) matching the widget's own established header/button palette. Also formalized 4 long-standing NEEDS-REVIEW items as proper exceptions: D1-E19 (QuizLiveMonitor podium, nested-panel), D1-E20 (GuidedLearningResults, already covered by D1-E4), D1-E21 (PersonalSpotifyNowPlayingTab, same family as D1-E10), D1-E22 (VideoActivityEditor selection-placeholder, same family as D1-E6). validate ✅ 559/559+37/37 (6729+703 tests), build ✅ — PR #2167. | #2167 |
| 2026-07-09 | — | D2 Brand Colors | Aligned — 22+ consecutive aligned run. Investigated the run-29 `Dock.tsx:1586` `rgba(45, 63, 137, ...)` lead in depth: confirmed `bg-brand-blue-primary` (static Tailwind, `tailwind.config.js`) and `--spart-primary` (admin-configurable CSS var, set by `DashboardView.tsx`) are genuinely two independent, never-reconciled color systems — the rgba() sits on the collapsed-dock FAB, which cross-fades with an expanded view where 2 sibling icons use the static Tailwind class at the same anchor. All 3 currently render identically only because none tracks admin theme; converting just the rgba() would make the FAB visibly diverge from its siblings the moment an admin customizes their theme. Declined as a one-line fix (not filed as a new exception ID either, since the color plausibly _should_ track admin theme eventually) — logged as a backlog planning item requiring a coordinated 3-site fix, not a nightly-scope unification. Fresh grep of hex/decimal-RGB brand-color equivalents found no other new instance. | — |
| 2026-07-09 | `nightly/unify-settings-labels-2026-07-09` | D3 Settings Labels | Shipped — retrofitted the 3 known orphaned-`<label>` `SettingsLabel` conversions from run 26 (PR #2144) to the newly-available `as="span"` group-heading pattern (added outside this routine, PR #2141 review fix, commit 558dd3e — see Canon update above): `MagicLayoutModal.tsx` "Try these" (+ `role="group"`/`aria-labelledby` on the chip container), `InstructionalRoutines/LibraryManager.tsx` "Default Steps" (+ `role="group"`/`aria-labelledby` on the steps-list container), `MiniApp/components/SaveAsWidgetModal.tsx` "Preview" (`as="span"` only — single display caption, not a control group, so no `role="group"` wrapper). Pure accessibility-semantics fix, zero visual delta (`as="span"` renders the identical class string). `LunchCount/SubmitReportModal.tsx` correctly left untouched (already fixed in separate PR #2164). validate ✅ 559/559+37/37 (6729+703 tests), build ✅ — PR #2168. | #2168 |
| 2026-07-09 | `nightly/unify-import-paths-2026-07-09` | D4 Import Paths | Shipped — `components/admin/Organization/views/` (8 files) — a newer admin subtree not covered by any prior D4 sweep — had `'../components/primitives'` cross-directory relative imports → `@/components/admin/Organization/components/primitives`. Found via a fresh repo-wide grep for relative imports escaping their own directory. Orchestrator repair: `StudentPageView.tsx`'s converted import exceeded Prettier's print width; `eslint --fix` applied to wrap it multi-line. No enforcement added (single new directory, not a recurring bug class like plc/\*). Rebased onto latest `dev-paul` (which landed the run-28 `PlcHome.tsx` fix + new plc/ `no-restricted-imports` rule, PR #2155, mid-run) — unaffected, re-validated clean. validate ✅ 559/559+37/37 (6729+703 tests), build ✅ — PR #2169. | #2169 |
| 2026-07-09 | — | D5 Toast Arch | Aligned — staleness scan of admin/config/manager/modal files added since 2026-06-25 found 8 new files (`Analytics/LinksPanel.tsx`, `ShortenUrlButton.tsx`, `DeactivatedScreen.tsx`, `SubCollectionBoardScreen.tsx`, plus 4 data/logic-only files); none introduces a local-Toast global-overlay pattern that should be `addToast` — `ShortenUrlButton.tsx`'s local `error` state is a plain inline validation `<p>`, never a toast at all. No new D5-E exception needed. | — |
| 2026-07-08 | — | D1 Empty States | Aligned — found and coordinated open sibling doc PR #2156 (run 28, still unmerged) before starting; treated its branch tip as authoritative context per the run-27 protocol. Manually-created worktree (not `isolation:worktree`) was fresh. Fresh repo-wide grep across `components/widgets/**` found no clean unconverted instance. Caught the subagent misreporting an already-catalogued exception (`PersonalSpotifyLibraryTab.tsx`, D1-E10) as a "new" NEEDS-REVIEW lead — it was just line drift (123→118), corrected during orchestrator review rather than added as a duplicate. VISUAL-RISK line numbers refreshed (SmartNotebook Library 173→163; ActivityWall 1890 unchanged). | — |
| 2026-07-08 | — | D2 Brand Colors | Aligned — 21+ consecutive aligned run. Fresh repo-wide grep of hex + decimal-RGB brand-color equivalents found no genuine violation. Surfaced one new NEEDS-REVIEW lead prior hex-only sweeps missed: `Dock.tsx:1586`'s `rgba(45, 63, 137, ...)` on the collapsed dock FAB — correctly not auto-fixed (two sibling buttons use the same static, non-theme-tracking `bg-brand-blue-primary` Tailwind class, so isolated conversion would create new inconsistency, not remove it); logged to backlog for human triage (16th exception vs. coordinated re-theme). Also noted two swatch/color-picker-default instances in `StudentPageView.tsx`/`NextUpConfigurationPanel.tsx` that fit the existing D2-E3/E7 exception shape (not added as new IDs — same pattern, no new rationale needed) and an out-of-scope real bug in `MusicManager.tsx` (seeds new stations with a Tailwind class string as a color value, not a hex/CSS color — invalid data, unrelated to CSS-var unification, flagged for a future non-unifier fix, not tracked here). | — |
| 2026-07-08 | `nightly/unify-settings-labels-2026-07-08` | D3 Settings Labels | Shipped — resolved the run-9→28 NEEDS REVIEW backlog item on `LunchCount/SubmitReportModal.tsx` by splitting its byte-exact matches: 3 read-only display `<span>`s stayed out of scope, the "Additional Notes" `<label htmlFor>` above a single `<textarea>` converted to `<SettingsLabel htmlFor="report-notes">`. **Correction post-ship:** the PR description originally claimed the label's new `mb-2` collapses with the textarea's `space-y-2`-derived `margin-top` (net gap unchanged, 8px). gemini/claude PR review caught this as wrong; empirically re-verified via a real Chromium (Playwright, `getBoundingClientRect()`): `<textarea>`'s UA-stylesheet default is `inline-block`, not `block`, so it does NOT participate in margin collapsing — the actual gap is 16px, not 8px (additive, not collapsed). Judged acceptable as a minor visual delta consistent with the established `SettingsLabel` convention elsewhere (`RandomSettings.tsx:535`) and the existing "mb-1→mb-2 canonical alignment" precedent (PRs #1783/#1870) — not reverted, PR description corrected. Also did NOT apply the reviewer's suggested `className="mb-0"` diff — see the pre-existing run-25 Tailwind-specificity gotcha below; that override is dead code. validate ✅ (lint + tsc re-verified independently on the changed file; full suite green per authoring pass) + build ✅ — PR #2164. | #2164 |
| 2026-07-08 | — | D4 Import Paths | Aligned — explicitly deferred to the still-open, unmerged PR #2155 (run 28's `PlcHome.tsx` fix + new plc/ ESLint enforcement) rather than re-doing or re-reporting that work. Fresh sweep of files changed since run 28 found only one new cross-directory import (`TimeToolWidget.tsx:18`), which is the pre-approved `'../WidgetLayout'` gray zone (D4-E2), not a violation. Zero true multi-level (`../../`) violations exist anywhere in the repo. | — |
| 2026-07-08 | — | D5 Toast Arch | Aligned — fresh grep across `components/admin/**` (122 files) plus other teacher-facing modal/panel directories (settingsModal, boardsModal, backgroundsModal, quickAccessModal, share, classes, plc, layout) found zero new local-Toast/message-state instances; the one non-catalogued grep hit (`UsersView.tsx` `message` state) reconfirmed as a false positive (invite-email body text field, not a toast). Zero new components added under `components/**` since run 28. | — |
| 2026-07-07 | — | D1 Empty States | Aligned — dispatch hit the recurring worktree-isolation staleness bug twice (harness worktree cleaned up mid-run with no salvageable diff after the subagent got stuck backgrounding validate); redispatched into an orchestrator-created manual worktree cut fresh from `origin/dev-paul`. Fresh repo-wide grep across `components/widgets/**` found no clean unconverted instance. Also found and reverted a stray uncommitted edit to `QuizWidget/Widget.tsx` sitting in the worktree (see Notes) after determining it matched the D1-E4 rationale rather than converting it. Catalogued 5 new exceptions: D1-E14 (QuizWidget monitor notice, analogous to D1-E4), D1-E15/E16/E17 (no-icon text states in CustomWidget/ExpectationsWidget/CatalystSetPickerPopover, analogous to D1-E9/E10), D1-E18 (DrawingWidget `skipScaling:false` overlay, out of ScaledEmptyState's container-query model). | — |
| 2026-07-07 | — | D2 Brand Colors | Aligned — 20+ consecutive aligned run. Fresh repo-wide grep of `#2d3f89`/`#ad2122` (hex + decimal-RGB equivalents) across all source files; every hit maps to an already-shipped fix or one of the 15 catalogued exceptions. No new violation, no new exception category needed. | — |
| 2026-07-07 | — | D3 Settings Labels | Aligned — fresh repo-wide chained-substring grep for the exact canonical anti-pattern (text-xxs + font-black + text-slate-400 + uppercase + tracking-widest) found only the canon definition itself and 3 already-catalogued exceptions/NEEDS-REVIEW rows; nothing new to convert. Incidentally surfaced ~20 additional files matching the existing D3-E7/E8 "font-bold not font-black" lighter-weight tier (see Notes) — not converted, logged for a future doc pass to broaden those exceptions' example lists. | — |
| 2026-07-07 | `nightly/unify-import-paths-2026-07-07` | D4 Import Paths | Shipped — fixed the run-26/27-deferred `components/plc/home/PlcHome.tsx:23` cross-subdirectory relative import (`'../sections'` → `@/components/plc/sections`), the last straggler of a bug class that has now recurred 6 times. Added the enforcement previously only proposed: a `no-restricted-imports` ESLint rule scoped to `components/plc/**/*.{ts,tsx}` blocking relative imports that escape into a sibling plc subdirectory, verified against a planted repro and a clean full-repo lint run. gemini-code-assist review on PR #2155 caught a real gap (regex missing the `members`/`sharedBoards` canonical sections from `sections.ts`) — verified and fixed same-day. Orchestrator note: this dimension's dispatch also hit the worktree-staleness bug (self-detected, redispatched into a manual worktree) and the background-wait bug (resumed via SendMessage with an explicit synchronous-execution instruction). validate ✅ 6726/6726 + 703/703, build ✅, lint ✅ (including post-review fix) — PR #2155 | #2155 |
| 2026-07-07 | — | D5 Toast Arch | Aligned — repo-wide grep across `components/admin/**` (122 files) plus a case-insensitive "toast" sweep found every local Toast/message-state instance maps to the 17 catalogued exceptions; a handful of additional string hits (`WidgetBuilder/ConnectionsTab.tsx`, `UsersView.tsx`) were confirmed unrelated (action-type literal, stale comment). No new admin components added in the past week. This was the only dimension dispatch this run whose `isolation: worktree` base was fresh (not stale) — confirming the staleness is per-worktree-creation, not systemic. | — |
| 2026-07-06 | `nightly/unify-empty-states-2026-07-06` | D1 Empty States | Shipped — fresh repo-wide grep across `components/widgets/**` found one unconverted hand-rolled empty state, unrelated to run 26's reverted ActivityWall attempt: `random/RandomGroups.tsx:622` "Click Randomize to Group" → `ScaledEmptyState` with `iconClassName`/`titleClassName` overrides preserving icon opacity/color and caption italic/color (uppercase+font-black title casing is the established canon delta). Also resolved both run-25 NEEDS REVIEW items: `QuizLiveMonitor.tsx` "No scores yet" and `GuidedLearningResults.tsx` "No responses yet..." classified as new exceptions D1-E12/D1-E13. Independently re-verified `ActivityWall/Widget.tsx:1890` as genuine VISUAL-RISK before discovering run 26 had already reached the same conclusion with concrete regression evidence (PR #2143) — merged the two writeups. Orchestrator repair: subagent's worktree was stale (base `0adda31` — same commit seen stale in runs 24/25) — extracted the verified diff, discarded the worktree, reapplied on a fresh branch cut from current `dev-paul`. validate ✅ 6644/6644 + 703/703, build ✅ — PR #2152 | #2152 |
| 2026-07-06 | — | D2 Brand Colors | Aligned — fresh repo-wide grep of `#2d3f89`/`#ad2122` across the whole app (not diff-only) reconfirmed every hit already fixed or matches a catalogued exception, including run 26's new D2-E15 (`config/fonts.ts`/`studentClassColors.ts` swatch arrays) — independently found the `fonts.ts` half of that same exception before discovering run 26's more complete entry already covered it; no new exception added to avoid a duplicate/conflicting D2-E15. | — |
| 2026-07-06 | — | D3 Settings Labels | Rejected — a D3 subagent proposed converting `NextUpConfigurationPanel.tsx` and `PdfWidget/Settings.tsx` labels to `SettingsLabel`, reasoning the `text-slate-500` (vs canonical `text-slate-400`) was accidental drift from 3 already-shipped sibling building-selector modals (PR #1885). Orchestrator review rejected both: a repo-wide grep found the exact `text-slate-500` + font-black + uppercase + tracking-widest combination reused independently in `SeatingChartSidebar.tsx` (unrelated widget settings sidebar) and `DraggableWindow.tsx` (app chrome) — an established second label tier, not a typo. Converting would have changed text color (and, for the `text-xs` PdfWidget instance, font size 12px→10px) — real visual regressions. Catalogued as new exception D3-E19, which also resolved run 26's standalone `SaveAsWidgetModal.tsx:100` NEEDS REVIEW entry (same tier, found independently that run). Discarded both files' changes and the subagent's worktree (no PR opened). | — |
| 2026-07-06 | `nightly/unify-import-paths-2026-07-06` | D4 Import Paths | Shipped — fresh repo-wide grep (not diff-only) found stragglers missed by prior sweeps, distinct from run 26's `plc/resources/` fix: a runtime `await import('../../hooks/useClickOutside')` in `DraggableWindow.test.tsx`, a type-only `import('../types')` in `utils/quizDriveService.ts`, 3 Playwright e2e specs (`app.spec.ts`, `smoke.spec.ts`, `sharing.spec.ts`), and `scripts/prerender-legal.tsx` (+ added `resolve.alias` to `vite.prerender.config.ts`) → `@/` alias. Corrected a stale doc note: Playwright 1.58 _does_ resolve `@/` via `tsconfig.json` paths (verified via `playwright test --list`), so `tests/e2e/` is back in D4 scope. Did not touch the still-open `PlcHome.tsx:23` NEEDS FIX item from run 26 (out of this run's grep results, left for the next pass). Independently verified beyond validate: `playwright test --list` resolves the 3 converted specs; `vite build --config vite.prerender.config.ts && node dist-ssr/prerender-legal.js` runs end-to-end. validate ✅ 6644/6644 + 703/703, build ✅ — PR #2153 | #2153 |
| 2026-07-06 | — | D5 Toast Arch | Aligned — repo-wide grep across `components/admin/**` (including prop-drilled `showMessage` callback chains) found all local-Toast/message-state instances map to catalogued exceptions. One newly-scanned component, `components/admin/WidgetBuilder/index.tsx` (`saveMessage` state), confirmed as a legitimate new exception (D5-E18) — `useDashboard()` is reachable but the message renders inline in the component's own `fixed inset-0 z-50` modal body, same rationale as D5-E13/E16. | — |
| 2026-07-05 | `nightly/unify-empty-states-2026-07-05` | D1 Empty States | Attempted, then reverted — converted `ActivityWall/Widget.tsx:1877` ("Responses will appear here") to `ScaledEmptyState`, validate+build green, opened PR #2143. Automated review caught a real regression: `ScaledEmptyState`'s `cqmin` icon sizing resolves against the whole-widget `container-type: size` boundary, not the smaller sub-panel this state lives in, so the icon roughly doubles in size (24px→48px cap at default widget dimensions). Closed PR #2143 without merging; reclassified the backlog item as VISUAL-RISK with the root cause documented so a future attempt doesn't repeat it. Two new NEEDS REVIEW candidates found and catalogued: `MusicWidget/PersonalSpotifyNowPlayingTab.tsx:65`, `VideoActivityWidget/components/VideoActivityEditor.tsx:393`. | closed (#2143) |
| 2026-07-05 | — | D2 Brand Colors | Aligned — fresh repo-wide grep of `#2d3f89`/`#ad2122` (56 hits) across all `*.ts`/`*.tsx`/`*.css`/`*.html`/`*.json`; every hit maps to an already-shipped fix or catalogued exception. New exception catalogued: `utils/studentClassColors.ts` `CLASS_COLOR_PALETTE` + `config/fonts.ts` `FONT_COLOR_PRESETS` (fixed categorical/swatch palettes, added as D2-E15). | — |
| 2026-07-05 | `nightly/unify-settings-labels-2026-07-05` | D3 Settings Labels | Shipped — repo-wide grep (chained-substring intersection, since the available regex engine doesn't support lookaheads) for the exact canonical anti-pattern found 5 previously-missed instances across 4 files outside the literal Settings.tsx/admin/ scope: `MagicLayoutModal.tsx`, `MiniAppEditorModal.tsx` (×2, with new `useId()`/`htmlFor` pairing), `SaveAsWidgetModal.tsx`, `InstructionalRoutines/LibraryManager.tsx` → `SettingsLabel`. Also fixed a pre-existing `import/first` lint issue in LibraryManager.tsx surfaced by automated PR review. Two new NEEDS REVIEW cluster members catalogued: `ChecklistConfigurationPanel.tsx` (8 instances, joins the lighter-weight font-bold cluster) and `SaveAsWidgetModal.tsx:100` (a third near-miss variant). validate ✅ 6644/6644 + 703/703 — PR #2144 | #2144 |
| 2026-07-05 | `nightly/unify-import-paths-2026-07-05` | D4 Import Paths | Shipped — new `components/plc/resources/` directory (added ~2026-07-04) had the recurring plc/ cross-subdirectory relative-import anti-pattern at `PlcResourcesBody.tsx:26` → `@/components/plc/sections`. Found and deferred an identical pre-existing bug at `components/plc/home/PlcHome.tsx:23` to keep scope contained. validate ✅ 6644/6644 + 703/703 — PR #2142. Proposed (not implemented) enforcement: an ESLint `no-restricted-imports` rule scoped to `components/plc/*/**` — this bug class has now recurred 6 times. | #2142 |
| 2026-07-05 | — | D5 Toast Arch | Aligned — checked the 3 new admin files added since run 25 (`PlcResourcesManager.tsx`, `NumberLineConfigurationPanel.tsx`, `ChecklistConfigurationPanel.tsx`); `PlcResourcesManager.tsx`/`PlcRecoveryPanel.tsx` already use `addToast` correctly, the other two have no notification pattern at all. Repo-wide grep across `components/admin/**` found 15 local-Toast/message-state files, all mapping to catalogued exceptions. No new instance. | — |
| 2026-07-04 | — | D1 Empty States | Aligned — 9th consecutive aligned run. Diff since run 24 (stickers lock-guard fix, SpecialistSchedule border-width fix, SegmentedControl extraction) confirmed D1-irrelevant. Fresh repo-wide grep across all `components/widgets/**` found no new hand-rolled empty states. Two new text-only near-miss candidates found and catalogued as NEEDS REVIEW: `QuizWidget/components/QuizLiveMonitor.tsx:2887` ("No scores yet") and `GuidedLearning/components/GuidedLearningResults.tsx:331` ("No responses yet..."). All 3 VISUAL-RISK line numbers re-verified unchanged (SmartNotebook Library:173, ActivityWall:1555/1884). | — |
| 2026-07-04 | — | D2 Brand Colors | Aligned — full repo-wide grep of `#2d3f89`/`#ad2122` (hex only, no rgb equivalents) across app + functions; every hit maps to an already-shipped fix or catalogued exception (D2-E1 through E14). Subagent flagged `components/admin/Analytics/LinksPanel.tsx:263` as a new candidate exception, but this is already covered by D2-E13 (added run 21) — no new exception needed, just re-confirmed. New out-of-scope observations noted: `config/widgetDefaults.ts`/`utils/migration.ts` themeColor defaults are config-object values (not inline `style` props, out of D2's scope regardless); `functions/src/*.ts` email-template hex is server-side HTML, can't reach the dashboard's CSS custom property at all. | — |
| 2026-07-04 | `nightly/unify-settings-labels-2026-07-04` | D3 Settings Labels | Shipped — repo-wide grep for the exact canonical anti-pattern string across `components/admin/**` (not caught by prior diff-only checks) found 8 instances in 4 admin library modals missed by all prior sweeps: `MiniAppLibraryModal.tsx`, `PdfLibraryModal.tsx`, `StickerLibraryModal.tsx`, `WorkSymbolsConfigurationModal.tsx` → `SettingsLabel`. Orchestrator repair: subagent's worktree was stale (based on commit `0adda31`, ~30 commits behind `origin/dev-paul`'s `9e08914`) and had used `className="mb-1"` overrides on 2 of the 4 files — dead code per the documented Tailwind-specificity gotcha (mb-2 always wins). Discarded the stale worktree, reapplied the diff on a fresh branch cut from current `dev-paul` with the dead `mb-1` overrides stripped (bare `<SettingsLabel>`, letting canonical `mb-2` apply — consistent with prior "mb-1→mb-2 canonical alignment" precedent from PRs #1783/#1870). validate ✅ 5985/5985 + 703/703 — PR #2131 | #2131 |
| 2026-07-04 | `nightly/unify-import-paths-2026-07-04` | D4 Import Paths | Shipped — subagent's worktree was NOT stale this time (correctly based on current `dev-paul` `9e08914`). Found 4 files in `context/` (`DashboardContext.bringToFront.test.tsx`, `.immediate.test.tsx`, `dashboardCanvasStore.test.tsx`, `toolVisibility.test.tsx`) each still had a plain `import { Dashboard, WidgetData, ... } from '../types'` — run 23's sweep of these same 4 files only converted their `vi.mock(...)` path strings and missed the sibling plain import statement. Converted to `@/types`; confirmed no other cross-directory relative imports remain in `context/*.test.tsx`. validate ✅ 5985/5985 + 703/703 — PR #2132 | #2132 |
| 2026-07-04 | — | D5 Toast Arch | Aligned — checked `components/admin/Organization/components/primitives.tsx` specifically (touched by the SegmentedControl extraction) and confirmed its `OrgToast` component (D5-E16) is untouched by that refactor. Repo-wide grep across `components/admin/**` found 13 local-Toast-import files + 3 local message/toast `useState` files, all mapping 1:1 to catalogued exceptions (D5-E2 through E13, E15, E16). No newly-added admin components found. | — | | PR  |
| ---------- | ------------------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| 2026-07-03 | — | D1 Empty States | Aligned — 7th consecutive aligned run. No files changed in `components/widgets/` since run 23; fresh grep found two new near-miss text-only/no-icon patterns (MusicWidget `EmptyHint` ×3, MiniApp Assignments/Submissions "No X yet" cards) — catalogued as D1-E10/D1-E11; VISUAL-RISK line numbers re-verified (SmartNotebook Library:173 unchanged; ActivityWall converged to 1555/1884 — consolidated 5 drifted backlog rows down to 2) | — |
| 2026-07-03 | — | D2 Brand Colors | Aligned — repo-wide grep of 26 `#2d3f89`/`#ad2122` hits across 16 files, all map to already-shipped fixes or catalogued exceptions; one new instance found and catalogued as D2-E14 (`Countdown/Settings.tsx:179`, same user-owned-value rationale as D2-E1/E12) | — |
| 2026-07-03 | — | D3 Settings Labels | Aligned — only 2 files changed in `*Settings.tsx`/admin config scope since run 23 (AdminSettings.tsx, StarterPackConfigurationModal.tsx, both from an unrelated Escape-key bugfix, no label markup touched); repo-wide grep for the exact canonical anti-pattern string found zero new instances; re-confirmed the NumberLine/Settings.tsx and 5-component "assignment config" family NEEDS REVIEW clusters are both already catalogued in this doc (subagent flagged them as undocumented but they were already present — line numbers drifted for NumberLine, from 234/247/316/327/338 to 202/215/284/295/306, due to unrelated file changes; not updated in backlog table this run, flagged here for the next run to refresh) | — |
| 2026-07-03 | `nightly/unify-import-paths-2026-07-03` | D4 Import Paths | Shipped — swept the entire `config/` directory (5 files, 7 imports: `tools.ts` ×3, `widgetGradeLevels.ts`, `instructionalRoutines.ts`, `expectationsData.ts`, `buildings.ts` ×2) → `@/` alias; this was run 23's deferred backlog item. Note: all 5 dispatched subagent worktrees were unexpectedly based on a stale commit (`0adda31`, well behind `origin/dev-paul` HEAD `a39c9b7`) rather than current `dev-paul` — a worse case of the known "worktree base discrepancy" gotcha (previously only a few commits of drift, this time ~30). D1/D2/D3/D5 shipped nothing so it was harmless for them; for D4, the orchestrator discarded the stale worktree/branch, independently re-applied the agent's already-verified 7-line diff on a fresh branch cut from current `dev-paul`, and re-ran full validate there. validate ✅ 5969/5969 + 703/703 — PR #2129 | #2129 |
| 2026-07-03 | — | D5 Toast Arch | Aligned — only one PR touched `components/admin/` since run 23 (#2101, Escape-key/screen-record fix, no toast logic); full grep of `components/admin/` for local Toast/message-state patterns found 15 files, all mapping to catalogued exceptions D5-E2–E16; `Organization/views/UsersView.tsx` `message` state re-confirmed as an email-invite form field, not a toast | — |
| 2026-07-02 | — | D1 Empty States | Aligned — 6th consecutive aligned run. Staleness scan of 22 files changed since run 22 (2026-06-29); QuizWidget/QuizManager/VideoActivityManager guard+loading states, GraphicOrganizer template layout containers, and admin/PLC/lti files all confirmed out of D1 scope; broader repo grep of editor "no X yet" modal states (GuidedLearningEditor, VideoActivityEditor, MiniApp Assignments/Submissions modals) confirmed design-intentional (D1-E6-style); SmartNotebook Library VISUAL-RISK line drifted 165→173 (updated in backlog); ActivityWall VISUAL-RISK lines unchanged (1561, 1890) | — |
| 2026-07-02 | — | D2 Brand Colors | Aligned — of the 19 changed files only AnalyticsManager.tsx and types.ts contain brand-color hex strings, both pre-existing catalogued exceptions (D2-E6/E13, D2-E8-style DEFAULT_GLOBAL_STYLE) with zero color-related diff since run 22; fresh repo-wide grep found only already-catalogued instances; no new violations | — |
| 2026-07-02 | — | D3 Settings Labels | Aligned — new admin panel `TimeToolConfigurationPanel.tsx` checked carefully, already uses SettingsLabel correctly throughout; NumberLine/Settings.tsx NEEDS REVIEW confirmed unchanged; new near-miss cluster found across 5 related "assignment config" components (QuizManager, VideoActivityManager, both PlcNewXAssignmentModal, PlcAssignmentConfigModal) using consistent `font-bold`-not-`font-black` lighter-weight labels — added to backlog as NEEDS REVIEW (D3-E19 candidate), not converted | — |
| 2026-07-02 | `nightly/unify-import-paths-2026-07-02` | D4 Import Paths | Shipped — subagent found+converted 7 of 14 test files with relative `vi.mock('../...')` paths, missed 7 more (DashboardContext\_\*.test.tsx ×5, tests/hooks/{usePlcInvitations,useStorage}, context/\*.test.tsx ×4, hooks/{useFeaturePermissions,usePlcAutoPullSync}); orchestrator repair completed the sweep to all 14 files (20 call-sites) → `@/` alias; `../WidgetLayout` (D4-E2) correctly left alone; validate ✅ 5921/5921 + 703/703 — PR #2126. New backlog item: `config/tools.ts` cross-dir imports (config/→components/), deferred to avoid same-night scope creep | #2126 |
| 2026-07-02 | — | D5 Toast Arch | Aligned — 3 new PLC assignment-config components already correctly use `addToast`; `TimeToolConfigurationPanel.tsx` has no notification pattern; `LtiDeepLinkPicker.tsx` local `errorMsg` state confirmed as a legitimate new exception (mounted under AuthProvider only, no DashboardProvider in route tree per App.tsx) — catalogued as D5-E17 | — |
| 2026-06-29 | — | D1 Empty States | Aligned — staleness scan of 5 changed source files (AnalyticsManager.tsx, aiFeatureLabels.ts, ActivityWall/Widget.tsx, NumberLine/Settings.tsx, quizDriveService.ts); ActivityWall VISUAL-RISK line numbers drifted again: 1555→1546 and 1884→1879 (updated in backlog); NumberLine/Settings.tsx and AnalyticsManager.tsx are not widget front-face content (admin analytics + widget settings back-face); aiFeatureLabels.ts is a pure constant (no JSX); all 3 VISUAL-RISK items unchanged (SmartNotebook Library:165, ActivityWall:1546, ActivityWall:1879) | — |
| 2026-06-29 | — | D2 Brand Colors | Aligned — no new inline brand-color instances in any modified file; aiFeatureLabels.ts is a pure constant with no inline styles; quizDriveService.ts is a utility with no inline styles; AnalyticsManager.tsx pre-existing exceptions (D2-E6, D2-E13) unchanged | — |
| 2026-06-29 | — | D3 Settings Labels | Aligned — new NEEDS REVIEW item found: NumberLine/Settings.tsx lines 234, 247, 316, 327, 338 have lighter-weight sub-labels (`text-xxs font-bold uppercase text-slate-400 mb-1`) inside compact add-form boxes; file correctly uses SettingsLabel for section headers (lines 84, 229, 311); sub-labels are likely analogous to D3-E17 (RevealGrid compact sub-labels) but human decision needed; input field labels at lines 87/122/157/194 (`text-xs font-semibold`) are NOT the canonical anti-pattern; pre-existing since PR #1798 (2026-06-01), missed in earlier scans — added to D3 backlog as NEEDS REVIEW | — |
| 2026-06-29 | — | D4 Import Paths | Aligned — all 5 changed source files use `@/` alias correctly; aiFeatureLabels.ts has no imports; quizDriveService.ts uses `@/hooks/useQuizSession`, `@/config/constants`, `@/utils/assignmentExportShared` and `./driveAuthErrors` (same-dir, correct); no new cross-directory relative imports introduced | — |
| 2026-06-29 | — | D5 Toast Arch | Aligned — no new admin components added since run 21; AnalyticsManager.tsx modification is an AI label fix with no Toast pattern changes; aiFeatureLabels.ts is a pure constant; no local Toast regressions in any modified file | — |
| 2026-06-28 | — | D1 Empty States | Aligned — ActivityWall VISUAL-RISK line numbers drifted: 1555→1561 and 1884→1890 (Widget.tsx modified for sub-share fixes); SmartNotebook Library VISUAL-RISK unchanged; no new widget files with hand-rolled empty states | — |
| 2026-06-28 | — | D2 Brand Colors | Aligned — no new inline brand-color instances in any new or modified file; NumberLine/Settings.tsx, ActivityWall/Widget.tsx, new share/admin/auth/subs files all clean | — |
| 2026-06-28 | — | D3 Settings Labels | Aligned — NumberLine/Settings.tsx has 5 compact sub-form labels (`text-xxs font-bold uppercase text-slate-400 mb-1`) in "Add Marker" / "Add Jump" form cards; file already uses SettingsLabel for section headers (lines 84, 229, 311) — intentional two-tier hierarchy; same NEEDS REVIEW pattern as MinTierSelect/NeedDoPutThen/NoteConfig; added to D3 backlog as NEEDS REVIEW; no cleanly actionable canonical anti-pattern this run | — |
| 2026-06-28 | — | D4 Import Paths | Aligned — 13 new files scanned: hooks/useSubstituteShares.ts, hooks/useReconcileExpiredSubShares.ts, tests/components/admin/Analytics/AiFeatureLabels.test.ts, tests/components/admin/Organization/UsersView.bulkRoleBuilding.test.tsx, tests/context/AuthContext.deactivation.test.tsx, tests/hooks/useSubstituteShares.test.ts, tests/rules/inactiveMemberLockout.test.ts, tests/rules/superAdminRoleId.test.ts, components/admin/Analytics/LinksPanel.tsx, components/admin/Analytics/aiFeatureLabels.ts, components/admin/ShortenUrlButton.tsx, components/auth/DeactivatedScreen.tsx, components/subs/SubCollectionBoardScreen.tsx; all use @/ aliases; zero violations | — |
| 2026-06-28 | — | D5 Toast Arch | Aligned — new share modals (ShareCollectionLinkCreatorModal, ShareLinkCreatorModal) correctly use addToast; Organization/components/primitives.tsx OrgToast is pre-existing D5-E16; ShortenUrlButton, subs files, auth/DeactivatedScreen have no local Toast patterns; all existing exceptions confirmed stable | — |
| 2026-06-27 | — | D1 Empty States | Aligned — staleness scan of 22 changed files (rules hardening commits #2081/#2085/#2089 + subs feature + auth deactivation + share modals); `components/auth/DeactivatedScreen.tsx` (new) is a light-surface auth screen, out of D1 scope; subs files (`SubBoardScreen`, `SubCollectionBoardScreen`, `SubCollectionsList`) are substitute-teacher UI, out of D1 scope (not widget front-face); all admin/share/hook/context files out of D1 scope; 3 VISUAL-RISK items unchanged (SmartNotebook Library:165, ActivityWall:1555, ActivityWall:1884) | — |
| 2026-06-27 | — | D2 Brand Colors | Aligned — `LinksPanel.tsx:263` `accentColor="#2d3f89"` on KpiCard is a data-visualization palette entry (one of four accent colors: blue/emerald/sky/amber); same rationale as D2-E6; added as D2-E13; no other inline brand-color violations in any new or modified file | — |
| 2026-06-27 | — | D3 Settings Labels | Aligned — no canonical anti-patterns in any admin file modified by rules hardening (Organization views, LinksPanel, LinkShortenerManager, ShortenUrlButton); share modals (`ShareLinkCreatorModal`, `ShareCollectionLinkCreatorModal`) are out of D3 scope (not settings back-face or admin config panels); `DeactivatedScreen.tsx` is out of D3 scope (auth surface); NEEDS REVIEW items unchanged (MinTierSelect, NeedDoPutThen, NoteConfigurationPanel, TimeTool, SubmitReportModal) | — |
| 2026-06-27 | — | D4 Import Paths | Aligned — all 22 changed/new files use `@/` imports correctly; subs components (`SubBoardScreen`, `SubCollectionBoardScreen`, `SubCollectionsList`) each use `@/hooks/...` and `@/types`; `DeactivatedScreen.tsx` uses `@/config/constants` and `@/context/useAuth`; share modals use `@/` throughout; no relative cross-directory imports introduced | — |
| 2026-06-27 | — | D5 Toast Arch | Aligned — `ShareLinkCreatorModal.tsx` correctly uses `addToast` (8 call-sites, no local Toast); `LinkShortenerManager.tsx` pre-existing D5-E13 exception unchanged; no new admin components with local Toast pattern in any of the rules hardening or subs feature files | — |
| 2026-06-26 | — | D1 Empty States | Aligned — no new source files added since run 19; types.ts First5Config addition is a pure type interface (no JSX, no hand-rolled empty states); 3 VISUAL-RISK items unchanged (SmartNotebook Library:165, ActivityWall:1555, ActivityWall:1884) | — |
| 2026-06-26 | — | D2 Brand Colors | Aligned — types.ts First5Config change has no inline brand-color instances; types.ts lines 6588–6589 (`primaryColor: '#2d3f89'`, `accentColor: '#ad2122'`) are DEFAULT_GLOBAL_STYLE constant defaults (same rationale as D2-E8); no new D2 violations | — |
| 2026-06-26 | — | D3 Settings Labels | Aligned — no new Settings or admin config panel files added since run 19; types.ts is a pure type file (out of D3 scope); NEEDS REVIEW items unchanged (MinTierSelect, NeedDoPutThen, NoteConfigurationPanel, TimeTool lighter-weight labels) | — |
| 2026-06-26 | — | D4 Import Paths | Aligned — no new source files added; types.ts has no import statements; staleness-check-only confirms zero new cross-directory relative imports | — |
| 2026-06-26 | — | D5 Toast Arch | Aligned — no new admin components added since run 19; no local Toast regressions in any modified file; all existing exceptions confirmed stable | — |
| 2026-06-25 | — | D1 Empty States | Aligned — staleness scan of modified files (QuizWidget/Widget.tsx, Checklist/Settings.tsx, ClockWidget/Settings.tsx, TimeTool/Settings.tsx, AnnouncementOverlay.tsx, UsersView.tsx); QuizWidget auth-guard states ("Sign in required":802, "Drive access needed":829) are access guards not missing-data empty states — out of D1 scope; "View-only share" monitor state (line 996) is contextual info+button, analogous to D1-E4; loading spinners covered by D1-E2; 3 VISUAL-RISK items unchanged (SmartNotebook Library:165, ActivityWall:1555, ActivityWall:1884) | — |
| 2026-06-25 | — | D2 Brand Colors | Aligned — no new inline brand-color instances in any modified file; AnnouncementOverlay, TypographySettings, Checklist, ClockWidget, TimeTool, QuizWidget all clean | — |
| 2026-06-25 | — | D3 Settings Labels | Aligned — new D3 analysis of modified settings files: Checklist/Settings.tsx lines 215/232 are indigo/emerald callout headings → D3-E14, D3-E15; TalkingToolConfigurationPanel.tsx:83 h4 in flex row → D3-E16; RevealGrid/Settings.tsx text-xxxs sub-labels → D3-E17; SpecialistScheduleConfigurationModal.tsx text-xxxs date grid labels → D3-E18; TimeTool/Settings.tsx 6 lighter-weight `font-bold text-slate-500 uppercase tracking-tight` labels coexist with canonical SettingsLabel usage → new NEEDS REVIEW backlog item; no canonical anti-pattern instance cleanly actionable | — |
| 2026-06-25 | — | D4 Import Paths | Aligned — staleness check of all modified files; UsersView.tsx imports `'../types'` and `'../components/primitives'` are within-feature Organization/ gray zone (D4-E2 analog); no new cross-directory anti-patterns in any modified or added file; no new files added this cycle | — |
| 2026-06-25 | — | D5 Toast Arch | Aligned — no new admin components with local Toast pattern; QuizWidget addToast call at line 188 correctly uses the DashboardContext pattern; AnnouncementOverlay has no Toast usage; all existing exceptions confirmed stable | — |
| 2026-06-24 | `nightly/unify-import-paths-plc-wave5-2026-06-24` | D4 Import Paths | Shipped — PLC Wave 5 regression: 12 cross-subdir imports across 7 files (bodies→viewer×4, bodies→versions×2, bodies→sync×2, docs→viewer×1, home→presence×1, home→activity×1, tabs→settings×1) → `@/components/plc/...`; tsc 0 errors, eslint ✅ — PR #2062 | #2062 |
| 2026-06-24 | — | D1 Empty States | Aligned — no new widget files with hand-rolled empty states; ActivityWall VISUAL-RISK line numbers drifted again: 1543→1555, 1870→1884; SmartNotebook Library VISUAL-RISK unchanged; `types.ts` `#2d3f89` additions are type-default constants (D2 scope) | — |
| 2026-06-24 | — | D2 Brand Colors | Aligned — `types.ts` additions (`primaryColor`/`accentColor` field defaults) are user-configurable type defaults, same rationale as D2-E1/E2; no new inline style violations in any modified file | — |
| 2026-06-24 | — | D3 Settings Labels | 2 new NEEDS REVIEW — `NeedDoPutThenConfigurationPanel.tsx` (6 labels) and `NoteConfigurationPanel.tsx` (6 labels) both use `text-xxs font-bold text-slate-500 uppercase mb-1/mb-2 block`; neither imports SettingsLabel; close to canonical but `font-bold` not `font-black`, `text-slate-500` not `text-slate-400`, missing `tracking-widest`; converting would change visual appearance; added to D3 backlog as NEEDS REVIEW | — |
| 2026-06-24 | — | D5 Toast Arch | Aligned — `PlcResourcesManager.tsx` correctly uses `addToast` from `useDashboard()`; no local Toast in any new admin component | — |
| 2026-06-23 | `nightly/unify-import-paths-plc-meeting-2026-06-23` | D4 Import Paths | Shipped — PLC Wave 3/4 regression: 6 cross-subdir imports in `plc/meeting/` (×5: sharedData×4, sections×1) and `plc/bodies/NotesDocsBody.tsx` (×1: docs) → `@/components/plc/...`; same violation pattern fixed in run 7 (tabs↔bodies); tsc 0 errors, validate ✅ 697 tests | #2055 |
| 2026-06-23 | — | D1 Empty States | Aligned — staleness scan of PLC Wave 3/4 new files (`plc/meeting/`, `plc/activity/`, `plc/comments/`, `plc/presence/`, `plc/search/`, `plc/settings/`, `plc/sync/`, `plc/versions/`, `plc/viewer/`) and `landing/landingIcons.tsx`, `layout/DeepLinkShareImporter.tsx`; none are widget front-face content; 3 VISUAL-RISK items unchanged (SmartNotebook Library:165, ActivityWall:1543, ActivityWall:1870) |
| 2026-06-23 | — | D2 Brand Colors | Aligned — no inline brand-color violations in any new PLC, landing, or layout file; all new files use Tailwind brand classes only |
| 2026-06-23 | — | D3 Settings Labels | Aligned — all `text-xxs` hits in new PLC files are button text (D3-E1) or out-of-scope PLC body/meeting UI; `PlcRecoveryPanel.tsx` (new admin panel) has no label anti-patterns at all; no new canonical anti-pattern instances |
| 2026-06-23 | — | D5 Toast Arch | Aligned — `PlcRecoveryPanel.tsx` correctly uses `addToast` from `useDashboard()`; `PlcVersionHistoryPanel` and meeting components have no local Toast patterns |
| 2026-06-15 | — | D1 Empty States | Aligned — staleness scan of 9 new `components/common/sessionViews/` UI primitives (ActionButton, OverflowMenu, ScorePill, SegmentedTabs, SessionBadge, SessionRow, SessionViewHeader, StatTile + index) and `components/poll/PollVoteApp.tsx`; none are widget-level components subject to D1 scope; no new widget files added; 3 VISUAL-RISK items unchanged (SmartNotebook/Library:165, ActivityWall:1543, ActivityWall:1870) | — |
| 2026-06-15 | — | D2 Brand Colors | Aligned — no new inline style brand-color instances in any new file (sessionViews, poll, utils/scoreColor.ts all clean); QuizResults and VA Results (modified) already use addToast for notifications | — |
| 2026-06-15 | — | D3 Settings Labels | Aligned — no new admin config panels or widget Settings files added; `sessionViews/` and `poll/` are out of D3 scope (not settings back-face or admin config); `PollVoteApp.tsx:123` header label is voter-facing UI (out of scope); fixed D3-E12/E13 numbering collision: GlobalPermissionsManager:1067 moved to D3-E13 in exceptions table | — |
| 2026-06-15 | — | D4 Import Paths | Aligned — all new files use @/ alias for cross-directory imports; `poll/` same-dir imports are D4-E1; `QuizLiveMonitor.tsx` `'../utils/quizScoreboard'` and `'../utils/resolveDisplayName'` are within-QuizWidget-feature gray zone (D4-E2); all sessionViews test files use @/ correctly | — |
| 2026-06-15 | — | D5 Toast Arch | Aligned — no new admin components added since last run; no local Toast patterns in sessionViews or poll (both are non-admin surfaces); QuizResults and VA Results (modified) use addToast correctly | — |
| 2026-06-14 | — | D1 Empty States | Aligned — staleness scan of Remote v2 files (RemoteEmbedControl, RemoteActivityWallControl, useRemoteConnection, MobileRemoteView, RemoteWidgetCard); remote control UI is out of D1 scope (separate dark mobile surface, not widget front-face); ActivityWall VISUAL-RISK items unchanged (line numbers drifted: 1508→1543, 1824→1870) | — |
| 2026-06-14 | — | D2 Brand Colors | Aligned — types.ts additions (primaryColor/accentColor fields and defaults) are type definitions + user-configurable defaults; identical rationale to D2-E1/E2; no new inline style violations | — |
| 2026-06-14 | — | D3 Settings Labels | Aligned — GlobalPermissionsManager purple labels (lines 352, 396, 1109, 1132, 1411, 1415) are intentional purple-themed UI, not the slate-400 canonical anti-pattern (same rationale as D3-E3/E9/E10); label at line 1067 is lighter-weight compact toggle label catalogued as D3-E13 | — |
| 2026-06-14 | — | D4 Import Paths | Aligned — 3 new files (RemoteActivityWallControl, RemoteEmbedControl, useRemoteConnection) all use @/ alias correctly; no violations in any Remote v2 file | — |
| 2026-06-14 | — | D5 Toast Arch | Aligned — GlobalPermissionsManager Toast is pre-existing D5-E2 exception; no new admin components with local Toast in Remote v2 or wide-distro anonymous-join commits | — |
| 2026-06-13 | — | D1 Empty States | Aligned — staleness scan of wide-distro Phase 1-3 new files (MinTierSelect, LandingPage, RequestRolloutPage, userTier.ts); no new widget files; no hand-rolled empty states in any new file; 3 VISUAL-RISK items unchanged | — |
| 2026-06-13 | — | D2 Brand Colors | Aligned — all 4 new files use Tailwind brand classes (not inline style props); no new D2 instances | — |
| 2026-06-13 | — | D3 Settings Labels | Aligned — no canonical anti-pattern found; new NEEDS REVIEW item added: MinTierSelect.tsx:32 uses text-xs/font-bold/slate-500 label (visually distinct from SettingsLabel — human decision needed) | — |
| 2026-06-13 | — | D4 Import Paths | Aligned — all 4 new files use @/ alias; modified context/AuthContext.tsx has no new relative cross-dir imports; D4 exhausted | — |
| 2026-06-13 | — | D5 Toast Arch | Aligned — no new admin components with local Toast; RequestRolloutPage inline error state uses `<p>` (not a Toast component); FeaturePermissionsManager/GlobalPermissionsManager modifications confirmed clean | — |
| 2026-06-11 | `nightly/unify-import-paths-tests-2026-06-11` | D4 Import Paths | Shipped — tests/ root + tests/hooks/ (12 files, ~42 imports): `'../context/...'`, `'../types'`, `'../components/...'`, `'../../hooks/...'` → `@/` alias; vi.mock paths converted; duplicate mock removed; validate ✅ 393 files / 4025 tests | #1941 |
| 2026-06-11 | — | D1 Empty States | Aligned — 3 VISUAL-RISK items unchanged (SmartNotebook Library, ActivityWall:1508, ActivityWall:1824); no new hand-rolled empty states in recently added files | — |
| 2026-06-11 | — | D2 Brand Colors | Aligned — no new inline brand-color instances; all known exceptions still valid | — |
| 2026-06-11 | — | D3 Settings Labels | Aligned — scope exhausted; LunchCount/SubmitReportModal.tsx NEEDS REVIEW item still pending human decision; no new anti-patterns found | — |
| 2026-06-11 | — | D5 Toast Arch | Aligned — no new admin components with local toast; all existing patterns confirmed against exceptions list | — |
| 2026-06-10 | — | D1 Empty States | Aligned — ScreenCaptureModal upload panel (new GL file) is out of scope (interactive action panel, not missing-data empty state); 3 VISUAL-RISK items unchanged, still require designer review | — |
| 2026-06-10 | — | D2 Brand Colors | Aligned — no new inline brand-color instances; D2-E10/11/12 already catalogued by run 11 | — |
| 2026-06-10 | — | D3 Settings Labels | Aligned — scope exhausted; `GuidedLearningEditor.tsx:1487` (`text-xxs font-bold uppercase tracking-wider text-slate-500`) is editor UI (out of scope); `LinkSchoologyModal.tsx:191` brand-blue modal header is out of scope | — |
| 2026-06-10 | — | D4 Import Paths | Aligned — D4 backlog exhausted; all remaining `'../'` imports confirmed within-feature gray zone (Organization/views/, plc/, GuidedLearning/components/, ClockWidget, MaterialsWidget) | — |
| 2026-06-10 | — | D5 Toast Arch | Aligned — no new admin components with local toast in recent GL/editor commits | — |
| 2026-06-09 | `nightly/unify-import-paths-library-2026-06-09` | D4 Import Paths | Shipped — components/common/library/ complete (3 files, 3 imports): ViewOnlyShareModal + AssignModal `'../Modal'` and ImportWizard `'../../Modal'` → `@/components/common/Modal`; D4-E2 `'../types'` preserved; tsc + eslint ✅ | #1909 |
| 2026-06-09 | — | D1 Empty States | Aligned — no new widget files added since 2026-06-08; 3 VISUAL-RISK items unchanged and still require designer review before conversion (SmartNotebook Library white-card wrapper, ActivityWall:1508 card bg, ActivityWall:1824 opacity icon) | — |
| 2026-06-09 | — | D2 Brand Colors | Aligned — no new inline brand-color instances; 3 new exceptions catalogued (D2-E10: AppearanceSection color picker defaults, D2-E11: MaterialsWidget/index.tsx, D2-E12: Countdown/Widget.tsx color picker defaults) | — |
| 2026-06-09 | — | D3 Settings Labels | Aligned — no new Settings or admin config panel files added since 2026-06-08; only open item remains LunchCount/SubmitReportModal.tsx (NEEDS REVIEW, human decision) | — |
| 2026-06-09 | — | D5 Toast Arch | Aligned — no new admin files added since 2026-06-08; all existing patterns confirmed clean or in exceptions list | — |
| 2026-06-08 | `nightly/unify-import-paths-utils-2026-06-08` | D4 Import Paths | Shipped — utils/ directory complete (16 files, 23 imports); `'../types'`, `'../config/...'`, `'../hooks/useQuizSession'`, `'../components/...'` → `@/` alias; same-dir `./` preserved; validate ✅ | #1897 |
| 2026-06-08 | — | D1 Empty States | Aligned — WorkSymbols + SoundboardWidget both already using ScaledEmptyState (stale backlog; SoundboardWidget fixed in #1888 outside nightly routine); 3 VISUAL-RISK items remain but need human visual review before conversion | — |
| 2026-06-08 | — | D2 Brand Colors | Aligned — no new inline brand-color instances; dimension fully enumerated | — |
| 2026-06-08 | — | D3 Settings Labels | Aligned — all OPEN backlog items confirmed stale (already fixed in prior runs); remaining instances are NEEDS REVIEW (SubmitReportModal, human decision needed) or out-of-scope (student-facing); D3 scope exhausted | — |
| 2026-06-08 | — | D5 Toast Arch | Aligned — no new admin components with local toast | — |
| 2026-06-06 | `nightly/unify-empty-states-2026-06-06` | D1 Empty States | Shipped — WorkSymbols "Select a symbol below" hand-rolled state → ScaledEmptyState; both ScaledEmptyState and ImageIcon already imported in file; validate ✅ | #1883 |
| 2026-06-06 | `nightly/unify-settings-labels-2026-06-06` | D3 Settings Labels | Shipped — 4 admin config modal labels → SettingsLabel (Calendar, BloomsTaxonomy, SpecialistSchedule building-selector + TalkingTool Sentence Stems); D3 admin config backlog exhausted; D3-E11 added (NextUpStudentApp); validate ✅ | #1885 |
| 2026-06-06 | `nightly/unify-import-paths-hooks-2026-06-06` | D4 Import Paths | Shipped — hooks/ directory complete (16 files, 43 imports); bonus Prettier fix in useImageUpload.ts; validate ✅ | #1884 |
| 2026-06-06 | — | D2 Brand Colors | Aligned — no new inline brand-color instances; new dirs (classroomAddon/, legal/, lti/) confirmed clean | — |
| 2026-06-06 | — | D5 Toast Arch | Aligned — no new admin components with local toast; UsersView message confirmed email form field (not toast) | — |
| 2026-06-05 | `nightly/unify-settings-labels-2026-06-05` | D3 Settings Labels | Shipped — 10 instances in 3 admin config files → SettingsLabel; expanded D3 scope to include admin config panels; D3-E5/E6/E7/E8/E9/E10 added as intentional exceptions for lighter-weight labels and themed callout headings; validate ✅ | #1870 |
| 2026-06-05 | `nightly/unify-import-paths-context-2026-06-05` | D4 Import Paths | Shipped — context/ directory complete (8 files, 54 imports); all `'../...'` cross-dir imports → `@/` alias; same-dir `./` imports preserved; validate ✅ | #1871 |
| 2026-06-05 | — | D1 Empty States | Aligned — no new widget types added; 5 new backlog items found (WorkSymbols, SoundboardWidget, SmartNotebook Library [run 3 CLOSED was incorrect], ActivityWall ×2) | — |
| 2026-06-05 | — | D2 Brand Colors | Aligned — no new production brand-color inline-style instances | — |
| 2026-06-05 | — | D5 Toast Arch | Aligned — no new admin components with local toast since run 7 | — |
| 2026-06-03 | — | D1 Empty States | Aligned — StickerItemWidget "No Image" placeholder inspected; sticker-specific pink styling, not a missing-data state; classified D1-E5. D1 backlog exhausted. | — |
| 2026-06-03 | — | D2 Brand Colors | Aligned — no new inline brand color instances. MusicWidget D2-E5 confirmed still the only remaining exception. | — |
| 2026-06-03 | `nightly/unify-settings-labels-2026-06-03` | D3 Settings Labels | Shipped — MathToolInstance/Settings.tsx 6 hand-rolled labels → SettingsLabel; mb-1 dead-code overrides dropped; text-brand-blue-light label (~line 59) intentionally left alone; validate ✅ 390/3994 | #1822 |
| 2026-06-03 | `nightly/unify-import-paths-plc-tabs-bodies-2026-06-03` | D4 Import Paths | Shipped — tabs↔bodies cluster (7 files, 9 imports): tabs→bodies (5 files) + bodies→tabs (2 files) converted to @/ alias; gray-zone plc-root imports preserved; validate ✅ 390/3994 | #1823 |
| 2026-06-03 | — | D5 Toast Arch | Aligned — UsersView.tsx `message` state is an email invite form field, not a toast notification. No new D5 violations. | — |
| 2026-06-01 | — | D1 Empty States | Aligned — backlog exhausted (all items shipped or classified as intentional exceptions); no new widgets found with hand-rolled empty states | — |
| 2026-06-01 | — | D2 Brand Colors | Aligned — dimension fully enumerated; no new instances | — |
| 2026-06-01 | `nightly/unify-settings-labels-2026-06-01` | D3 Settings Labels | Shipped — SpecialistSchedule/Settings.tsx 10 hand-rolled labels → SettingsLabel (8 `mb-1 block` + 2 icon-flex variants); mb-1→mb-2 and font-black on icon labels are canonical alignment; validate ✅ 17/336 | #1783 |
| 2026-06-01 | `nightly/unify-import-paths-plc-2026-06-01` | D4 Import Paths | Shipped — plc/home/cards/ 4 files `../../sections` → `@/components/plc/sections`; deepest/clearest anti-pattern in plc/ tree; validate ✅ 17/336 | #1782 |
| 2026-06-01 | — | D5 Toast Arch | Aligned — no new admin components found; dimension exhaustively documented | — |
| 2026-05-31 | `nightly/unify-empty-states-2026-05-31` | D1 Empty States | Shipped — RandomWidget.tsx hand-rolled "No Names Provided"/"Everyone Absent Today" guard → ScaledEmptyState with action prop (conditional "Update attendance" button) | #1770 |
| 2026-05-31 | — | D2 Brand Colors | Aligned — no new production instances; dimension fully enumerated | — |
| 2026-05-31 | `nightly/unify-settings-labels-2026-05-31` | D3 Settings Labels | Shipped — LunchCount/Settings.tsx 4 labels → SettingsLabel; orchestrator repair removed dead className="mb-1.5" overrides (Tailwind mb-2 wins on CSS order); 3 intentional variations left alone (D3-E3, D3-E4, colon separator) | #1771 |
| 2026-05-31 | `nightly/unify-import-paths-2026-05-31` | D4 Import Paths | Shipped — settingsModal/sections/ 4 files, '../SettingsSectionHeader' → @/ alias; no remaining ../ imports in directory | #1772 |
| 2026-05-31 | — | D5 Toast Arch | Aligned — no new admin components found; dimension exhaustively documented | — |
| 2026-05-29 | `nightly/unify-empty-states-2026-05-29` | D1 Empty States | Shipped — VideoActivityWidget/Widget.tsx two hand-rolled guard states → ScaledEmptyState; rebase resolved conflict (getVideoActivityBehavior import added on dev-paul) | #1746 |
| 2026-05-29 | — | D2 Brand Colors | Aligned — MusicWidget fallback (D2-E5) and test assertion confirmed; no new production instances | — |
| 2026-05-29 | `nightly/unify-settings-labels-2026-05-29` | D3 Settings Labels | Shipped — RandomSettings.tsx 7 hand-rolled labels → SettingsLabel (includes icon form for Hash/Puzzle); 4 card-title/button exceptions correctly excluded | #1747 |
| 2026-05-29 | — | D4 Import Paths | Deferred — plc/ was substantially restructured on dev-paul (overview/, grid/ deleted) between bootstrap and run 4; D4 branch discarded; re-audit plc/ new structure next run | — |
| 2026-05-29 | — | D5 Toast Arch | Aligned — 2 new admin components found (SaveAsTemplateModal, OrganizationPanel); both confirmed intentional exceptions (D5-E15, D5-E16) | — |
| 2026-05-28 | `nightly/unify-empty-states-2026-05-28` | D1 Empty States | Shipped — NextUp/Widget.tsx hand-rolled "Queue is not active" state → ScaledEmptyState; SmartNotebook already clean | #1721 |
| 2026-05-28 | — | D2 Brand Colors | Aligned — #ad2122 sweep complete, all instances are intentional exceptions; prior fixes already on dev-paul | — |
| 2026-05-28 | `nightly/unify-settings-labels-2026-05-28` | D3 Settings Labels | Shipped — Calendar/Settings.tsx 3 labels → SettingsLabel; VideoActivityWidget already clean | #1722 |
| 2026-05-28 | `nightly/unify-import-paths-admin-2026-05-28` | D4 Import Paths | Shipped — 15 admin files, 25 imports converted from `'../common/...'` and `'../widgets/...'` to @/ alias | #1723 |
| 2026-05-28 | `nightly/unify-toast-routines-2026-05-28` | D5 Toast Arch | Shipped — InstructionalRoutinesManager local Toast state → addToast; 11 other admin modal patterns confirmed as intentional exceptions | #1724 |
| 2026-05-27 | `nightly/unify-empty-states-2026-05-27-clean` | D1 Empty States | Shipped — MaterialsWidget/index.tsx:180 hand-rolled focused-empty → ScaledEmptyState | #1704 |
| 2026-05-27 | `nightly/unify-brand-colors-2026-05-27-clean` | D2 Brand Colors | Shipped — LunchCount/Widget.tsx:583 `color: '#2d3f89'` → `var(--spart-primary, #2d3f89)` | #1705 |
| 2026-05-27 | `nightly/unify-settings-labels-2026-05-27-clean` | D3 Settings Labels | Shipped — Schedule/Settings.tsx 2 section heading labels → SettingsLabel | #1706 |
| 2026-05-27 | `nightly/unify-import-paths-2026-05-27-clean` | D4 Import Paths | Shipped — 5 imports fixed in layout/widgets/student (Dock, RemoteControlMenu, WidgetRenderer, StudentApp) | #1707 |
| 2026-05-27 | `nightly/unify-toast-arch-2026-05-27-clean` | D5 Toast Arch | Shipped — WorkSymbolsConfigurationModal local Toast → addToast; GraphicOrganizer already clean | #1708 |
| 2026-05-26 | `nightly/unify-brand-color-2026-05-26` | D2 Brand Colors | Shipped — InstructionalRoutines/Widget.tsx:217 step badge `#2d3f89` → `var(--spart-primary, #2d3f89)` | TBD |
| 2026-05-26 | bootstrap | D1 Empty States | Audited — 31 widgets use ScaledEmptyState; 4 hand-rolled instances added to backlog | — |
| 2026-05-26 | bootstrap | D2 Brand Colors | Audited — 2 fixable instances; 1 shipped, 1 needs investigation | — |
| 2026-05-26 | bootstrap | D3 Settings Labels | Audited — ~105 hand-rolled occurrences; backlog seeded | — |
| 2026-05-26 | bootstrap | D4 Import Paths | Audited — 8 relative WidgetLayout imports + 23 relative admin/common/ imports; backlog seeded | — |
| 2026-05-26 | bootstrap | D5 Toast | Audited — addToast is 100% consistent; local Toast in admin modals confirmed as intentional pattern; 3 items to investigate | — |

