# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.pnpm-store
dist
dist-ssr
*.local

# Lock files (using pnpm-lock.yaml)
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase
.firebase
firebase-debug.log
service-account-key.json
scripts/org-seed.json

# Backups
*.backup
firebase_config.md

# Playwright
test-results/
playwright-report/

# Vitest
coverage/
.vitest-reports/
.env
.env.*

# Secrets
*.pem
*.key
*.cert

.gemini/
.codex/
gha-creds-*.json
eslint-output.txt
.claude/scheduled_tasks.lock
.claude/worktrees/
.claude/settings.local.json

# Agent skills installed via the `skills` CLI (project scope).
# Project-committed skills (e.g. admin-widget-config, new-widget) are re-included below.
.agents/
skills-lock.json
.claude/skills/*
!.claude/skills/admin-widget-config/
!.claude/skills/new-widget/

# Playwright MCP scratch
.playwright-mcp/

# Brainstorm visual companion scratch
.superpowers/

# Ad-hoc screenshots
/login-screen.png

# Verification / QA test artifacts
verification/

# Local data / Drive-sync artifacts — never commit (may contain student PII)
Data/
.tmp.driveupload/
# Local in-app quiz exports (".quiz.json" downloads) — quiz content, not repo source
Quizzes/

# Ops-script reports (e.g. scripts/migrateAnnouncements.js) — derived from prod
# Firestore docs (IDs + titles); never commit
scripts/output/
